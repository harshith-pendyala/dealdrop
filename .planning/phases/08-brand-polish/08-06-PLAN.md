---
phase: 08-brand-polish
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04", "08-05"]
files_modified:
  - .planning/phases/08-brand-polish/08-VERIFICATION.md
autonomous: false
requirements: [BRAND-05]
nyquist_compliant: true
tags: [brand, verification, visual-walk, wcag, audit]

must_haves:
  truths:
    - "Accent color renders legibly in both light and dark mode at default / hover / focus states with no contrast regression vs v1.0"
    - "Logo renders crisply at desktop (1x DPR) and 375px viewport (2x DPR via devtools emulation)"
    - "Hero orange-50 gradient does not regress h1 / paragraph text legibility"
    - "Browser tab favicon displays the new orange palette in Chrome and Safari at default zoom"
    - "Repo-wide audits — no lingering 'Made with love', 'Add Product', 'Product added!' strings"
    - "Full automated suite (vitest + lint + build) green BEFORE the visual walk begins"
    - "Audit findings documented in 08-VERIFICATION.md as Phase 7 POL-04-style rows (viewport / mode / surface / result / fix-shipped)"
  artifacts:
    - path: ".planning/phases/08-brand-polish/08-VERIFICATION.md"
      provides: "Audit table covering BRAND-01..05 with viewport/mode/surface/result rows; manual visual walk findings; final phase status"
      contains: "BRAND-05"
      contains_2: "WCAG AA"
      contains_3: "viewport"
  key_links:
    - from: "08-VERIFICATION.md audit rows"
      to: "Phase 7 POL-04 audit table format (.planning/phases/07-polish-deployment/07-VERIFICATION.md)"
      via: "Same row shape for portfolio-bar consistency"
      pattern: "viewport.*mode.*surface.*result"
    - from: "BRAND-05 requirement"
      to: "Visual walk row in 08-VERIFICATION.md"
      via: "Manual audit per D-08 (no Storybook, no snapshot harness)"
      pattern: "BRAND-05"
---

<objective>
Verification plan for Phase 8. Two responsibilities:

1. **Wave 0 scaffolding (Task 1)**: Create `.planning/phases/08-brand-polish/08-VERIFICATION.md` skeleton with the Phase 7 POL-04 audit table format. The file does not exist today (08-VALIDATION.md §"Wave 0 Requirements" lists it as a Wave 0 dependency). The skeleton has frontmatter (status: in_progress), a requirement-verification table covering BRAND-01..05, and an empty visual-walk audit table ready for Task 2's findings.

2. **Manual visual walk (Task 2 — checkpoint:human-verify)**: Run the full automated regression sweep (vitest + lint + build), then walk the running app at desktop and 375px in light + dark mode, recording findings in 08-VERIFICATION.md per D-08 (mirrors Phase 7 D-05 fix-as-found portfolio-bar style).

Purpose: BRAND-05 — "Accent color renders correctly in both light and dark mode with no contrast regression vs v1.0 (legibility preserved on hover/focus states)". Per CONTEXT.md D-08, this is verified via manual walk + spot-check WCAG, NOT Storybook or snapshot tests. The audit row shape is locked to Phase 7's POL-04 format for repo-wide consistency.

Output: 08-VERIFICATION.md with BRAND-01..04 closed via grep+test evidence and BRAND-05 closed via populated visual-walk rows; phase status set to `passed`.

Depends on Plans 02-05 (all the surface modifications must land before the walk begins) — Plan 01 is transitively a dependency through those. files_modified is exclusively the verification doc; zero source overlap with Plans 01-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brand-polish/08-CONTEXT.md
@.planning/phases/08-brand-polish/08-RESEARCH.md
@.planning/phases/08-brand-polish/08-PATTERNS.md
@.planning/phases/08-brand-polish/08-UI-SPEC.md
@.planning/phases/08-brand-polish/08-VALIDATION.md

@.planning/phases/07-polish-deployment/07-VERIFICATION.md

<interfaces>
Phase 7 POL-04 audit row shape (the format Phase 8 mirrors):

| Viewport | Component | Observed break | Fix shipped |
|----------|-----------|----------------|-------------|
| 320px | (all) | None — top-to-bottom walk produced zero visible breaks | n/a |
| 375px | (all) | None | n/a |
| 768px | (all) | None | n/a |
| Desktop | (all) | None | n/a |

Phase 8 row shape (08-VALIDATION.md + RESEARCH.md adapt this for color audit):

| Viewport | Mode | Surface | Result | Fix Shipped |
|----------|------|---------|--------|-------------|
| desktop | light | Track Price button default | pass (orange-500 / zinc-50, ~4.6:1) | — |
| desktop | dark | Track Price button hover | (fill in) | (fill in) |
| 375px | light | ProductCard price | (fill in) | (fill in) |
| 375px | dark | Hero gradient → h1 | pass (orange-50 invisible in dark via dark:from-transparent) | — |
| desktop | dark | Logo on near-black bg | (fill in — RESEARCH.md Pitfall 6) | (fill in) |

WCAG AA targets:
- Normal text: 4.5:1 contrast minimum
- Large text and UI components: 3:1 minimum
- orange-500 oklch(70.5% 0.213 47.604) on zinc-50 background: ~4.6:1 ✓
- orange-400 oklch(75% 0.183 55.934) on zinc-900 background: ~4.8:1 ✓
- orange-700 oklch(55.3% 0.195 38.402) on white card: small-text accent target

Surfaces to walk (exhaustive):
1. Logged-out hero — h1, paragraph, FeatureCard icons, FeatureCard blurbs, gradient warmth
2. Sign-in modal — primary CTA button color, focus ring, hover
3. Dashboard empty state — "Track your first product" heading, paste URL input, "Track Price" button
4. Dashboard with products — ProductCard price color, View Product link, Show Chart button, Remove button (destructive)
5. PriceChart toggle — line color (orange via var(--primary)), dot fill, activeDot
6. Header on every route — logo crisp at 32px, click-to-home works
7. Browser tab favicon — orange D glyph in Chrome AND Safari
8. AddProductDialog — `+ Track Price` trigger, `Track a price` title

Repo-wide audits to run as part of the regression sweep:
- `! grep -rn "Made with love" dealdrop/src dealdrop/app`
- `! grep -rn "Add Product" dealdrop/src`
- `! grep -rn "Product added!" dealdrop/src`
- `! test -f dealdrop/app/favicon.ico`
- `grep -F "background: '#f97316'" dealdrop/app/icon.tsx`
- `grep -F "deal-drop-logo.png" dealdrop/src/components/header/Header.tsx`
- `grep -F "oklch(70.5% 0.213 47.604)" dealdrop/app/globals.css`
- `grep -F "bg-gradient-to-b from-orange-50" dealdrop/src/components/hero/Hero.tsx`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wave 0 — Create 08-VERIFICATION.md skeleton with frontmatter, requirement table, and audit-walk template</name>
  <files>.planning/phases/08-brand-polish/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (canonical Phase 7 format — read first 80 lines to see frontmatter shape, "Requirement Verification" table, and "POL-04 Mobile Audit" section)
    - .planning/phases/08-brand-polish/08-VALIDATION.md (§"Wave 0 Requirements" — 08-VERIFICATION.md is the fourth listed Wave 0 file)
    - .planning/phases/08-brand-polish/08-PATTERNS.md (§"Manual Visual Walk" — exact row shape)
    - .planning/phases/08-brand-polish/08-UI-SPEC.md (§"Accessibility Contract" — surfaces to audit)
  </read_first>
  <action>
    Create `.planning/phases/08-brand-polish/08-VERIFICATION.md` with this exact content:

    ```markdown
    ---
    phase: 08-brand-polish
    verified: in_progress
    status: in_progress
    score: pending
    ---

    # Phase 8: Brand Polish — Verification Report

    **Phase Goal:** Logged-in and logged-out users see a coherent DealDrop brand — logo in the header, branded favicon, a single accent color across primary buttons/links/highlights, and no leftover "Made with love" footer copy.
    **Verified:** in progress
    **Status:** in_progress

    ## Requirement Verification

    | Req | Behavior | Status | Evidence |
    |-----|----------|--------|----------|
    | BRAND-01 | "Made with love" line removed (logged-in + logged-out) | (fill in after walk) | (grep + walk row) |
    | BRAND-02 | DealDrop logo image in app header | (fill in) | (Header.test.tsx + walk row) |
    | BRAND-03 | Browser tab favicon shows DealDrop orange | (fill in) | (build + walk row Chrome + Safari) |
    | BRAND-04 | Single accent color via CSS custom property; consistent across surfaces | (fill in) | (globals.css grep + walk rows for each enumerated surface) |
    | BRAND-05 | Accent legibility in light + dark / default + hover + focus | (fill in) | (visual walk rows below) |

    ## Automated Regression Sweep

    Run BEFORE the visual walk begins. All commands must exit 0.

    | Command | Status | Notes |
    |---------|--------|-------|
    | `cd dealdrop && npm run test` | (fill in) | Full vitest suite |
    | `cd dealdrop && npm run lint` | (fill in) | ESLint (note any pre-existing baseline) |
    | `cd dealdrop && npm run build` | (fill in) | Next.js production build |
    | `! grep -rn "Made with love" dealdrop/src dealdrop/app` | (fill in) | BRAND-01 repo audit |
    | `! grep -rn "Add Product" dealdrop/src` | (fill in) | D-11 source audit |
    | `! grep -rn "Product added!" dealdrop/src` | (fill in) | D-11 toast audit |
    | `! test -f dealdrop/app/favicon.ico` | (fill in) | Phase 7 D-07 directive landed |
    | `grep -F "oklch(70.5% 0.213 47.604)" dealdrop/app/globals.css` | (fill in) | Plan 01 light-mode --primary |
    | `grep -F "oklch(75% 0.183 55.934)" dealdrop/app/globals.css` | (fill in) | Plan 01 dark-mode --primary |
    | `grep -F "deal-drop-logo.png" dealdrop/src/components/header/Header.tsx` | (fill in) | Plan 02 logo asset |
    | `grep -F "background: '#f97316'" dealdrop/app/icon.tsx` | (fill in) | Plan 03 favicon orange |
    | `grep -F "bg-gradient-to-b from-orange-50" dealdrop/src/components/hero/Hero.tsx` | (fill in) | Plan 04 hero gradient |
    | `grep -F "+ Track Price" dealdrop/src/components/dashboard/AddProductDialog.tsx` | (fill in) | Plan 05 CTA rename |

    ## BRAND-05 Manual Visual Walk

    **First walk:** (fill in date)
    **Operator:** operator
    **Tooling:** Chrome DevTools "Responsive" mode at desktop and 375px breakpoints; manual color toggle in OS settings or `prefers-color-scheme` devtools emulation; spot-check WCAG via DevTools Inspect → Computed → Contrast.

    | Viewport | Mode | Surface | Result | Fix Shipped |
    |----------|------|---------|--------|-------------|
    | desktop | light | Header logo at 32px (1x DPR) | (fill in) | (fill in) |
    | desktop | light | Track Price button default (Sign In + Track Price) | (fill in: pass / contrast ratio observed) | (fill in) |
    | desktop | light | Track Price button hover | (fill in) | (fill in) |
    | desktop | light | Track Price button focus (tab to it) | (fill in: ring visible? ring color?) | (fill in) |
    | desktop | light | ProductCard price (`text-primary` orange-500) | (fill in) | (fill in) |
    | desktop | light | PriceChart line (`stroke="var(--primary)"`) | (fill in) | (fill in) |
    | desktop | light | FeatureCard icons (`text-primary`) | (fill in) | (fill in) |
    | desktop | light | Hero gradient warmest stop → h1 contrast | (fill in: ratio against orange-50 #fff7ed) | (fill in) |
    | desktop | dark | Header logo on near-black background | (fill in: legible? RESEARCH.md Pitfall 6) | (fill in) |
    | desktop | dark | Track Price button default (orange-400) | (fill in) | (fill in) |
    | desktop | dark | Track Price button hover | (fill in) | (fill in) |
    | desktop | dark | Track Price button focus | (fill in) | (fill in) |
    | desktop | dark | ProductCard price | (fill in) | (fill in) |
    | desktop | dark | PriceChart line on dark card | (fill in) | (fill in) |
    | desktop | dark | FeatureCard icons | (fill in) | (fill in) |
    | desktop | dark | Hero gradient (should be invisible — `dark:from-transparent`) | (fill in: confirms suppressor works) | (fill in) |
    | 375px | light | Logo crisp at 2x DPR | (fill in) | (fill in) |
    | 375px | light | Track Price button (mobile width) | (fill in) | (fill in) |
    | 375px | light | ProductCard price (mobile grid) | (fill in) | (fill in) |
    | 375px | light | Hero gradient + h1 contrast at narrow viewport | (fill in) | (fill in) |
    | 375px | dark | Logo + buttons + price | (fill in) | (fill in) |
    | (browser-tab) | light | Favicon in Chrome (default zoom) | (fill in: orange D visible?) | (fill in) |
    | (browser-tab) | light | Favicon in Safari | (fill in) | (fill in) |
    | (browser-tab) | dark | Favicon in Chrome (dark theme) | (fill in) | (fill in) |

    ### Methodology Notes

    - DevTools "Responsive" mode (NOT device presets — see Phase 7 POL-04 audit note about UA spoofing breaking OAuth).
    - For dark-mode walk: use `prefers-color-scheme` emulation in DevTools (Rendering tab → Emulate CSS media feature prefers-color-scheme: dark) to avoid toggling OS settings.
    - WCAG AA targets: 4.5:1 normal text, 3:1 large text and UI components.
    - If any row reads `fail`: ship the fix in this same plan (fix-as-found per Phase 7 D-05) and add a note in "Fix Shipped". Do NOT defer to Plan 07 / Phase 9 / a future milestone unless the user explicitly approves.

    ## Sign-Off

    - [ ] All requirement-verification rows show `pass` or `pass (fix shipped)`
    - [ ] All automated regression sweep commands exit 0
    - [ ] All visual-walk rows filled in
    - [ ] Frontmatter `status` updated from `in_progress` to `passed`
    - [ ] Frontmatter `verified` updated to ISO timestamp

    **Approval:** pending
    ```

    The `(fill in)` placeholders are filled by Task 2's manual walk. This file is committed in its skeleton form first so Task 2 has a populated destination.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-brand-polish/08-VERIFICATION.md &amp;&amp; grep -F "BRAND-05" .planning/phases/08-brand-polish/08-VERIFICATION.md &amp;&amp; grep -F "Manual Visual Walk" .planning/phases/08-brand-polish/08-VERIFICATION.md &amp;&amp; grep -F "WCAG AA" .planning/phases/08-brand-polish/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/08-brand-polish/08-VERIFICATION.md` succeeds
    - File starts with YAML frontmatter (`---` on line 1)
    - `grep -c "BRAND-05" .planning/phases/08-brand-polish/08-VERIFICATION.md` returns at least 2 matches
    - `grep -F "Manual Visual Walk" .planning/phases/08-brand-polish/08-VERIFICATION.md` returns at least 1 match
    - `grep -F "Automated Regression Sweep" .planning/phases/08-brand-polish/08-VERIFICATION.md` returns at least 1 match
    - `grep -F "WCAG AA" .planning/phases/08-brand-polish/08-VERIFICATION.md` returns at least 1 match
    - The visual-walk table has at least 20 rows (covering desktop+375px x light+dark x multiple surfaces, plus browser-tab rows)
    - Frontmatter `status: in_progress`
  </acceptance_criteria>
  <done>
    08-VERIFICATION.md skeleton file committed with the requirement table, regression sweep checklist, and audit walk table all populated as templates. Task 2 fills in the placeholders.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run automated regression sweep, then walk the running app and fill in 08-VERIFICATION.md (BRAND-05 / D-08)</name>
  <files>.planning/phases/08-brand-polish/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-brand-polish/08-VERIFICATION.md (the skeleton from Task 1 — populate every `(fill in)` row)
    - .planning/phases/08-brand-polish/08-CONTEXT.md (D-08 — manual walk + spot-check WCAG; portfolio bar; mirror Phase 7 POL-04)
    - .planning/phases/08-brand-polish/08-RESEARCH.md (§"Common Pitfalls" — verify each pitfall did NOT regress: orange-on-near-black legibility, gradient suppression in dark mode, favicon legibility at 32x32)
    - .planning/phases/08-brand-polish/08-UI-SPEC.md (§"Accessibility Contract" lines 269-281 — surfaces to spot-check)
    - .planning/phases/08-brand-polish/08-VALIDATION.md (§"Manual-Only Verifications" — full instructions)
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (POL-04 audit table — same row format Phase 8 mirrors)
  </read_first>

  <what-built>
    Plans 01-05 of Phase 8 have shipped:
    - Plan 01: globals.css `--primary` redefined to verified Tailwind v4 orange-500 / orange-400 oklch values; ProductCard price uses `text-primary`
    - Plan 02: Header.tsx renders the DealDrop PNG logo at 32px tall, wrapped in `<Link href="/">`
    - Plan 03: app/icon.tsx swapped from zinc-900 to orange-500 hex; app/favicon.ico deleted
    - Plan 04: Hero.tsx 'Made with love' deleted; orange-50 gradient added to section
    - Plan 05: 'Add Product' → 'Track Price' rename across AddProductDialog, AddProductForm, ProductGrid.test, AddProductForm.test
    - Header.test.tsx and Hero.test.tsx Wave 0 tests added and green
  </what-built>

  <how-to-verify>
    **Phase A — Automated regression sweep (do this BEFORE the manual walk):**

    1. Start in the repo root. Run:
       ```bash
       cd dealdrop && npm run test
       cd dealdrop && npm run lint
       cd dealdrop && npm run build
       ```
       Confirm all three exit 0. If any fail, STOP and report the failure — do not proceed to the manual walk until the automated suite is green.

    2. Run the repo-wide grep audits:
       ```bash
       ! grep -rn "Made with love" dealdrop/src dealdrop/app
       ! grep -rn "Add Product" dealdrop/src
       ! grep -rn "Product added!" dealdrop/src
       ! test -f dealdrop/app/favicon.ico
       grep -F "oklch(70.5% 0.213 47.604)" dealdrop/app/globals.css
       grep -F "oklch(75% 0.183 55.934)" dealdrop/app/globals.css
       grep -F "deal-drop-logo.png" dealdrop/src/components/header/Header.tsx
       grep -F "background: '#f97316'" dealdrop/app/icon.tsx
       grep -F "bg-gradient-to-b from-orange-50" dealdrop/src/components/hero/Hero.tsx
       grep -F "+ Track Price" dealdrop/src/components/dashboard/AddProductDialog.tsx
       ```
       Each command must report success (zero exit, expected output). Fill in the `Status` column of the "Automated Regression Sweep" table in 08-VERIFICATION.md.

    **Phase B — Manual visual walk (the BRAND-05 deliverable):**

    3. Start the dev server: `cd dealdrop && npm run dev`. Open `http://localhost:3000` in Chrome.

    4. Open Chrome DevTools → Toggle Device Toolbar → switch to "Responsive" mode (NOT device presets — Phase 7 POL-04 documents that device presets spoof UA strings and break Google OAuth). Set viewport to desktop (1280x800 or similar) and to 375px when prompted. Use DevTools Rendering tab → "Emulate CSS media feature prefers-color-scheme" to switch between light and dark mode.

    5. Walk the surfaces in this order, filling in 08-VERIFICATION.md as you go:
       - **Logged-out hero** (visit `/` while signed out)
         - Header logo: crisp at 32px (1x DPR)? Crisp at 2x DPR (devtools "Add device pixel ratio 2")?
         - Hero h1 + paragraph: legible against the warmest gradient stop (orange-50 = `#fff7ed`)?
         - Sign In button (top-right): orange in light mode, lifted-orange in dark mode? Hover state observably distinct? Focus ring visible when tabbed to?
         - FeatureCard icons: orange in light AND dark mode?
         - 'Made with love' line: confirmed absent (visual confirmation)?
       - **Sign-in modal** (click Sign In)
         - Primary CTA button: orange in both modes?
       - **Dashboard with products** (sign in with a Google account that has products)
         - ProductCard price: orange via `text-primary`? Currency adjacency not lost?
         - View Product link, Show Chart button: still neutral (ghost variant — should NOT be orange)?
         - "Track Price" CTA in header: orange?
         - Click "Show Chart": PriceChart line is orange?
       - **Dashboard empty state** (sign in with a fresh Google account that has zero products, OR delete all products)
         - "Track your first product" heading: legible (was already correct copy in v1.0)?
         - Inline "Track Price" button: orange?
       - **AddProductDialog** (click "+ Track Price" trigger)
         - Trigger button text reads `+ Track Price`?
         - Dialog title reads `Track a price`?
         - Form submit button reads `Track Price`?
         - Submit a real URL — toast success reads `Now tracking`?
       - **Browser tab favicon**
         - Open `http://localhost:3000` in Chrome — tab icon shows orange D glyph?
         - Open the same URL in Safari — tab icon shows orange D glyph?
         - Toggle OS dark mode — tab icon still legible?

    6. For every row in 08-VERIFICATION.md's "BRAND-05 Manual Visual Walk" table, fill in:
       - `Result`: `pass` (with optional contrast ratio in parens, e.g. `pass (4.6:1)`) or `fail (description)`
       - `Fix Shipped`: `—` if pass; if fail, ship a tiny fix in the same plan (fix-as-found per D-05) and note the fix here

    7. For each requirement row in the "Requirement Verification" table, set status to `VERIFIED` (with brief evidence pointer).

    8. Update frontmatter:
       - `verified:` to current ISO timestamp (e.g. `2026-05-02T18:30:00Z`)
       - `status:` from `in_progress` to `passed`
       - `score:` to `5/5` (or `5/5 with N fix-shipped` if any fail rows were fixed-as-found)

    9. Save and commit 08-VERIFICATION.md.

    **Stop conditions:**
    - If automated regression sweep fails → STOP, report failure, do not proceed to manual walk
    - If a manual-walk row reads `fail` AND the fix requires more than a 1-line code change → STOP, report to user, propose fix in a follow-up plan rather than fix-as-found
    - If a logo legibility issue surfaces (D-02 deviation: PNG turned out to be symbol-only) → STOP, report to user; do NOT improvise side-by-side wordmark composition
  </how-to-verify>

  <action>
    See <how-to-verify> above for the full procedure. Summary: (Phase A) run automated regression sweep — vitest + lint + build + repo-wide grep audits — and confirm all exit 0. (Phase B) walk the running app at desktop and 375px in light + dark modes per the surface list, filling in every row of the BRAND-05 Manual Visual Walk table in 08-VERIFICATION.md with `pass` (with contrast ratio where applicable) or `fail (fix shipped: ...)`. Update frontmatter `status` to `passed` and `verified` to ISO timestamp. Commit 08-VERIFICATION.md. This is a checkpoint task — `<what-built>`, `<how-to-verify>`, and `<resume-signal>` blocks above carry the substantive instructions; this `<action>` block exists for plan-structure validator compatibility.
  </action>

  <verify>
    <automated>test -f .planning/phases/08-brand-polish/08-VERIFICATION.md \&\& ! grep -F "(fill in)" .planning/phases/08-brand-polish/08-VERIFICATION.md \&\& grep -F "status: passed" .planning/phases/08-brand-polish/08-VERIFICATION.md</automated>
    <human>Operator confirms via resume-signal that walk is complete and 08-VERIFICATION.md is signed off.</human>
  </verify>

  <done>
    08-VERIFICATION.md has zero `(fill in)` placeholders remaining; frontmatter `status: passed`; `verified:` set to ISO timestamp; all 5 BRAND-* requirement rows show VERIFIED; full automated suite green; operator approved via resume-signal.
  </done>

  <resume-signal>
    Type "approved" once 08-VERIFICATION.md has all rows filled, frontmatter status set to `passed`, and the file is committed. Or describe issues that prevent sign-off.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (the user running the manual walk) | Trusted — operator records observed visuals into a markdown file |
| Browser DevTools emulation | Trusted — viewport/dark-mode emulation only; no privileged operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-AUDIT-FALSE-PASS-MEDIUM | Repudiation (operator records 'pass' without actually walking the surface) | Manual walk discipline | mitigate | Acceptance criteria require ALL `(fill in)` placeholders to be replaced (not left as templates). Phase-7-style rigor inherited via the audit-row format. Operator signs off explicitly via `resume-signal: "approved"`. |
| T-08-06-CONTRAST-A11Y-MEDIUM | Information Disclosure (legibility regression slips through if operator misjudges contrast) | BRAND-05 walk | mitigate | DevTools Inspect → Computed → Contrast provides numeric ratio against target background — operator records the ratio in the Result column for color-affected rows. WCAG AA targets are documented in the methodology notes. |

**Security relevance:** None — this plan modifies only a verification markdown file and runs no source-code edits beyond optional fix-as-found patches. No new auth, data, route, env, server, or external surface. Documented per gsd-planner agent contract. Note: `security_enforcement` is not active for this phase per the planning_context (no schema/auth/data work).
</threat_model>

<verification>
- `test -f .planning/phases/08-brand-polish/08-VERIFICATION.md` succeeds
- `grep -F "status: passed" .planning/phases/08-brand-polish/08-VERIFICATION.md` returns at least 1 match (after Task 2 sign-off)
- `! grep -F "(fill in)" .planning/phases/08-brand-polish/08-VERIFICATION.md` (no placeholders left after Task 2)
- `cd dealdrop && npm run test && npm run lint && npm run build` exits 0
- All Phase 8 repo-wide audits pass (Made with love / Add Product / Product added! / favicon.ico)
- BRAND-05 visual-walk rows are populated with `pass` (with contrast ratios where applicable) or `pass (fix shipped: ...)`
</verification>

<success_criteria>
- 08-VERIFICATION.md exists with frontmatter `status: passed`
- All 5 requirement-verification rows show `VERIFIED`
- All automated regression sweep commands documented as exit-0
- All BRAND-05 visual-walk rows populated (no `(fill in)` placeholders remain)
- Phase 8 complete: BRAND-01..05 are closed; Phase 9 (Resend env config) is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/08-brand-polish/08-06-SUMMARY.md` documenting:
- Date and operator of the visual walk
- Number of fail-then-fix-shipped rows (if any) and their resolutions
- Final phase status: `passed` (or `passed with N fix-shipped`)
- Any deviations from CONTEXT.md decisions (D-02 logo-as-symbol-only flag, etc.) — should be `none` if all decisions held
- Pointer to 08-VERIFICATION.md for the full audit
- Confirmation that Phase 9 (Resend env config) is now unblocked
</output>
