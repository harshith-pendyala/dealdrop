---
phase: 08-brand-polish
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - dealdrop/app/icon.tsx
  - dealdrop/app/favicon.ico
autonomous: true
requirements: [BRAND-03]
nyquist_compliant: true
tags: [brand, favicon, next-icon, image-response]

must_haves:
  truths:
    - "Browser tab shows the DealDrop favicon glyph rendered with the new orange palette (orange-500 background, zinc-50 'D' glyph)"
    - "The generic v1.0 zinc-900 background is replaced — no neutral-grey favicon remains"
    - "dealdrop/app/favicon.ico is removed (Phase 7 D-07 directive finally lands; the working-tree leftover would otherwise auto-serve as a stale icon)"
    - "dealdrop/app/icon.tsx remains the canonical Next.js icon route (Path B chosen — wordmark logo doesn't reduce legibly to 32x32)"
    - "Production build still succeeds and emits the icon route"
  artifacts:
    - path: "dealdrop/app/icon.tsx"
      provides: "ImageResponse icon route with orange-500 background and zinc-50 'D' glyph"
      contains: "background: '#f97316'"
      contains_2: "ImageResponse"
    - path: "dealdrop/app/favicon.ico"
      provides: "DELETED — working-tree leftover that Phase 7 D-07 directed removed but never landed in commits"
      verify_absent: true
  key_links:
    - from: "Browser tab favicon request"
      to: "dealdrop/app/icon.tsx ImageResponse route"
      via: "Next.js icon-route file convention (auto-generates <link rel='icon'> in <head>)"
      pattern: "ImageResponse"
    - from: "Phase 7 D-07 directive (delete app/favicon.ico)"
      to: "Phase 8 implementation"
      via: "rm -f dealdrop/app/favicon.ico (working-tree file; Phase 7 deletion never committed)"
      pattern: "favicon-absent"
---

<objective>
Refresh the browser-tab favicon to use the new DealDrop orange palette, AND delete the lingering working-tree `dealdrop/app/favicon.ico` (Phase 7 D-07 directed deletion but the file is non-git-tracked and survived). Choose **D-12 Path B**: keep `dealdrop/app/icon.tsx` as a Next.js ImageResponse route handler and swap its inline `background` value to use orange-500. Path B is preferred because the user-provided wordmark logo (620x210 PNG, ~2.95:1 aspect) does not reduce legibly to a 32x32 square — a single-letter glyph on an orange background is the clearest tab icon.

Purpose: BRAND-03 requires "Browser tab shows the DealDrop logo as the favicon — generic v1.0 icon at `app/icon.tsx` is replaced/retired". D-12 explicitly grants planner discretion between Path A (static `app/icon.png`) and Path B (modify ImageResponse glyph). Path B chosen per the recommendation in the planning_context. Phase 7 D-07's "delete `app/favicon.ico`" directive is honored as part of this plan.

Output: `app/icon.tsx` returns an ImageResponse with an orange-500 background; `app/favicon.ico` is deleted from the working tree; production build still emits the icon route.

Depends on Plan 01 only because Plan 01 establishes the orange palette as the source of truth for brand color — Path B applies the SAME orange (orange-500 hex equivalent) to the favicon background. No file overlap with Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brand-polish/08-CONTEXT.md
@.planning/phases/08-brand-polish/08-RESEARCH.md
@.planning/phases/08-brand-polish/08-PATTERNS.md
@.planning/phases/08-brand-polish/08-UI-SPEC.md
@.planning/phases/08-brand-polish/08-VALIDATION.md

<interfaces>
Existing app/icon.tsx — preserve verbatim except the inline `background` value.
The route uses ImageResponse (Satori-backed). Satori supports flexbox + a CSS subset
ONLY (no grid, no oklch, no CSS custom properties). Plain hex strings are required.

Tailwind v4.2.2 orange-500 hex equivalent: `#f97316` is the canonical orange-500 hex
(Tailwind v3+v4 share the same orange-500 hex). Bright enough to read at 32x32 in
both light and dark browser themes. RESEARCH.md confirms this choice in §"Common
Pitfalls Pitfall 6".

Working-tree file to delete:
- `dealdrop/app/favicon.ico` — 25931 bytes, dated 2026-04-17 (Next.js scaffold leftover).
  NOT git-tracked (Phase 7 D-07 directed deletion but the file is in the working tree only).
  Must be removed with `rm -f` so future builds don't auto-serve a stale icon.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap app/icon.tsx ImageResponse background from zinc-900 to orange-500 hex (BRAND-03 / D-12 Path B)</name>
  <files>dealdrop/app/icon.tsx</files>
  <read_first>
    - dealdrop/app/icon.tsx (CURRENT — read entire file; only the `background:` line changes)
    - .planning/phases/08-brand-polish/08-CONTEXT.md (D-12 — favicon path is Claude's discretion; Path B chosen per planning_context recommendation)
    - .planning/phases/08-brand-polish/08-RESEARCH.md (§"Common Pitfalls Pitfall 6" — use `#f97316` for orange-500 hex; Satori cannot resolve `oklch()`)
    - .planning/phases/08-brand-polish/08-PATTERNS.md (§"app/icon.tsx — Path B" — exact replacement values)
  </read_first>
  <action>
    Edit `dealdrop/app/icon.tsx`. Make exactly ONE substantive substitution and one cosmetic comment update.

    Substantive change: the inline `style={{}}` object on the `<div>` currently contains a line `background: '#18181b', // zinc-900`. Change that line to `background: '#f97316', // orange-500`.

    Cosmetic comment update: on the immediately-following line `color: '#fafafa', // zinc-50`, update the comment text to read `// zinc-50 (passes AA on orange-500)`. The hex value `#fafafa` stays unchanged.

    Do NOT change the `fontSize`, `fontWeight`, `width`, `height`, `display`, `alignItems`, `justifyContent`, `letterSpacing`, or the literal `D` glyph inside the `<div>`. Do NOT change the exported `size` constant (must stay `{ width: 32, height: 32 }`). Do NOT change the exported `contentType` constant (must stay `'image/png'`). Do NOT introduce `oklch()` — Satori (the engine behind ImageResponse) cannot resolve oklch or CSS custom properties; hex strings are required (RESEARCH.md §"Common Pitfalls Pitfall 6").

    Why hex `#f97316` and not the verified oklch from Plan 01: Satori does not support `oklch()`. Tailwind v4 ships orange-500 with the canonical hex `#f97316` (verified in Tailwind palette docs and 08-PATTERNS.md). The visual is the same orange as the rest of the brand cascade — no discrepancy.

    Why keep the `D` glyph: 08-PATTERNS.md §"app/icon.tsx — Path B" explicitly notes that the wordmark logo doesn't reduce legibly to 32x32; a single-letter glyph on a colored background is the clearest tab icon (RESEARCH.md §"Common Pitfalls Pitfall 6").

    After saving, run `cd dealdrop && npm run build`. The Next.js icon route compiles into the production build. Confirm zero new errors.
  </action>
  <verify>
    <automated>cd dealdrop &amp;&amp; grep -F "background: '#f97316'" app/icon.tsx &amp;&amp; ! grep -F "background: '#18181b'" app/icon.tsx &amp;&amp; grep -F "ImageResponse" app/icon.tsx &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "background: '#f97316'" dealdrop/app/icon.tsx` returns at least 1 match
    - `! grep -F "background: '#18181b'" dealdrop/app/icon.tsx` (zinc-900 hex is gone)
    - `grep -F "color: '#fafafa'" dealdrop/app/icon.tsx` returns at least 1 match (zinc-50 retained)
    - `grep "ImageResponse" dealdrop/app/icon.tsx` returns at least 1 match (route handler shape preserved)
    - `grep "width: 32," dealdrop/app/icon.tsx` returns at least 1 match (size export preserved)
    - `grep "height: 32," dealdrop/app/icon.tsx` returns at least 1 match
    - `grep "contentType = 'image/png'" dealdrop/app/icon.tsx` returns at least 1 match
    - `grep -E ">\s*D\s*</div>" dealdrop/app/icon.tsx` returns at least 1 match (D glyph preserved)
    - `! grep "oklch" dealdrop/app/icon.tsx` (Satori-incompatible function not introduced)
    - `cd dealdrop && npm run build` exits 0 (icon route compiles cleanly)
    - `cd dealdrop && npm run lint -- app/icon.tsx` exits 0
  </acceptance_criteria>
  <done>
    `app/icon.tsx` ImageResponse renders a 32x32 PNG with `#f97316` (orange-500) background and `#fafafa` (zinc-50) "D" glyph. `size`, `contentType`, glyph, and inline-style structure preserved. Build green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete dealdrop/app/favicon.ico working-tree leftover (Phase 7 D-07 + RESEARCH.md Pitfall 2)</name>
  <files>dealdrop/app/favicon.ico</files>
  <read_first>
    - .planning/phases/07-polish-deployment/07-CONTEXT.md (§D-07 — original directive: "Delete `app/favicon.ico`")
    - .planning/phases/08-brand-polish/08-CONTEXT.md (§"D-12" — final paragraph: "respect Phase 7 D-07's 'delete the Next.js scaffold favicon.ico' decision")
    - .planning/phases/08-brand-polish/08-RESEARCH.md (§"Common Pitfalls Pitfall 2" — favicon.ico is NOT git-tracked but lives on disk as a 25KB binary; must be deleted explicitly)
  </read_first>
  <action>
    Delete the working-tree file `dealdrop/app/favicon.ico` using `rm -f`:

    ```bash
    rm -f dealdrop/app/favicon.ico
    ```

    Why `rm -f`: the file is NOT git-tracked (verified via `git status` would not list it as tracked). Phase 7 D-07 directed deletion, but because git never saw the file, the deletion never landed in commits and the working-tree binary survived. `rm -f` deletes from the filesystem; no `git rm` needed because git is unaware of the file.

    Why delete it: while `app/icon.tsx` is the active icon route, Next.js will ALSO serve `app/favicon.ico` if present. Two icon files coexisting can cause inconsistent browser-tab behavior (some browsers prefer one, some the other). Deleting `favicon.ico` ensures `app/icon.tsx` is the SOLE source of truth for the tab icon.

    Do NOT delete `dealdrop/app/icon.tsx` (Path A would do that — we are explicitly choosing Path B). Do NOT delete any other file in `app/`. Do NOT add a `app/icon.png` — that would be Path A (rejected per planning_context).

    After deletion, run `cd dealdrop && npm run build` to confirm the icon route still compiles correctly without the .ico file.
  </action>
  <verify>
    <automated>test ! -f dealdrop/app/favicon.ico &amp;&amp; test -f dealdrop/app/icon.tsx &amp;&amp; cd dealdrop &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f dealdrop/app/favicon.ico` succeeds (file is deleted)
    - `test -f dealdrop/app/icon.tsx` succeeds (icon route preserved — Path B, not Path A)
    - `! test -f dealdrop/app/icon.png` (Path A artifact NOT introduced; we chose Path B)
    - `cd dealdrop && npm run build` exits 0
    - `cd dealdrop && ls .next/server/app/ 2>/dev/null | grep -E "icon" || true` shows the icon route compiled into the build (informational; build success implies this)
  </acceptance_criteria>
  <done>
    `dealdrop/app/favicon.ico` no longer exists on disk. `dealdrop/app/icon.tsx` remains. Build green. Phase 7 D-07 directive finally lands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-tab icon request | Next.js icon route serves a static-cacheable PNG; no user input flows in |
| ImageResponse compilation | Satori receives static JSX from source; no runtime template injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-ICON-INJECTION-LOW | Tampering (theoretical — could user input poison the icon?) | `app/icon.tsx` | accept | The ImageResponse JSX uses ONLY hard-coded literal strings (`'#f97316'`, `'#fafafa'`, `'D'`). No `params`, no `searchParams`, no env vars, no database reads. Zero injection surface. |
| T-08-03-FAVICON-STALE-LOW | Information Disclosure (stale generic icon could mislead user about brand identity) | `app/favicon.ico` | mitigate | Task 2 explicitly deletes the working-tree leftover. After this plan, the only icon source is `app/icon.tsx`, which now renders the orange brand color. |

**Security relevance:** None. This plan modifies one inline hex value in a static route handler and deletes one working-tree binary. No new auth, data, route, env, server, or external surface. Documented per gsd-planner agent contract.
</threat_model>

<verification>
- `cd dealdrop && grep -F "background: '#f97316'" app/icon.tsx` returns at least 1 match
- `test ! -f dealdrop/app/favicon.ico` succeeds
- `test -f dealdrop/app/icon.tsx` succeeds
- `cd dealdrop && npm run build` exits 0
- Manual smoke (NOT blocking — Plan 06 captures the audit row): `cd dealdrop && npm run build && npm start`, open `http://localhost:3000`, observe browser tab favicon — orange background, white "D" glyph.
</verification>

<success_criteria>
- `app/icon.tsx` ImageResponse uses `background: '#f97316'` (orange-500) instead of `#18181b` (zinc-900)
- `app/icon.tsx` `size`, `contentType`, glyph, and surrounding inline-style structure are unchanged
- `app/favicon.ico` is deleted from the working tree
- No `app/icon.png` was introduced (Path B, not Path A)
- `npm run build` and `npm run lint` green
</success_criteria>

<output>
After completion, create `.planning/phases/08-brand-polish/08-03-SUMMARY.md` documenting:
- Path B chosen (with reasoning: wordmark doesn't reduce legibly to 32x32)
- The exact 1-line hex swap in app/icon.tsx
- Confirmation that app/favicon.ico was deleted
- Build status
- Note for the BRAND-05 visual walk: confirm tab favicon displays orange in Chrome and Safari at default zoom
</output>
