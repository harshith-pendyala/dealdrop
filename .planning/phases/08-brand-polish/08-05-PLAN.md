---
phase: 08-brand-polish
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - dealdrop/src/components/dashboard/AddProductDialog.tsx
  - dealdrop/src/components/dashboard/AddProductForm.tsx
  - dealdrop/src/components/dashboard/AddProductForm.test.tsx
  - dealdrop/src/components/dashboard/ProductGrid.test.tsx
autonomous: true
requirements: [BRAND-04]
nyquist_compliant: true
tags: [brand, copy, rename, dashboard, cta]

must_haves:
  truths:
    - "Every user-facing 'Add Product' string in dealdrop/src is renamed to 'Track Price' (the primary CTA reads 'Track Price' across dialog trigger, form submit, and dashboard)"
    - "Every user-facing 'Product added!' toast string is renamed to 'Now tracking' (08-UI-SPEC.md line 197)"
    - "Dialog title 'Add a product' is renamed to 'Track a price' (08-UI-SPEC.md line 169)"
    - "Component file names are NOT renamed (D-11 explicitly out of scope) — AddProductForm.tsx, AddProductDialog.tsx keep their identifiers"
    - "Backend identifiers are NOT renamed — SQL columns, /api/cron/check-prices route, sendPriceDropAlert action keep their identifiers (D-11 explicit)"
    - "Test assertions are updated in lockstep with source — repo-wide test suite stays green"
  artifacts:
    - path: "dealdrop/src/components/dashboard/AddProductDialog.tsx"
      provides: "Renamed: '+ Add Product' button → '+ Track Price'; 'Add a product' dialog title → 'Track a price'"
      contains: "+ Track Price"
      contains_2: "Track a price"
    - path: "dealdrop/src/components/dashboard/AddProductForm.tsx"
      provides: "Renamed: 'Track' submit button → 'Track Price'; 'Product added!' toast → 'Now tracking'; JSDoc comment updated"
      contains: "Track Price"
      contains_2: "Now tracking"
    - path: "dealdrop/src/components/dashboard/AddProductForm.test.tsx"
      provides: "Updated assertion at line 96 from 'Product added!' to 'Now tracking'; test name on line 93 also updated"
      contains: "Now tracking"
    - path: "dealdrop/src/components/dashboard/ProductGrid.test.tsx"
      provides: "Updated stub literal at line 19 from '+ Add Product' to '+ Track Price'"
      contains: "+ Track Price"
  key_links:
    - from: "AddProductDialog '+ Track Price' trigger button"
      to: "AddProductForm 'Track Price' submit button"
      via: "Dialog wraps Form; same useActionState flow as v1.0"
      pattern: "Track Price"
    - from: "AddProductForm dispatchToastForState ok=true branch"
      to: "Sonner toast.success call"
      via: "toast.success('Now tracking')"
      pattern: "toast.success.*Now tracking"
---

<objective>
Apply the "Add Product" → "Track Price" copy rename across the four affected source surfaces and the two affected test files (D-11). The rename is product-copy work expanding Phase 8 beyond strict BRAND-01..05, but the user explicitly asked for it during /gsd-discuss-phase (08-CONTEXT.md §"CTA copy rename"). 08-UI-SPEC.md §"Copywriting Contract" enumerates every surface and the post-rename string.

Specific renames (08-UI-SPEC.md table at lines 167-178):
- AddProductDialog trigger button: `+ Add Product` → `+ Track Price`
- AddProductDialog title: `Add a product` → `Track a price`
- AddProductForm submit button: `Track` → `Track Price`
- AddProductForm toast (and its JSDoc comment): `Product added!` → `Now tracking`
- AddProductForm.test.tsx assertion line 96: `'Product added!'` → `'Now tracking'`
- AddProductForm.test.tsx test name line 93: update to match
- ProductGrid.test.tsx stub literal line 19: `+ Add Product` → `+ Track Price`

Per D-11 explicit boundaries:
- DO NOT rename component files (AddProductForm.tsx → TrackPriceForm.tsx is OUT of scope)
- DO NOT rename SQL columns (the `products` table stays)
- DO NOT rename API routes (`/api/cron/check-prices` stays)
- DO NOT rename server actions (`addProduct`, `sendPriceDropAlert` stay)
- DO NOT rename internal variable/function/prop names (`AddProductFormProps`, `addProduct`, `dispatchToastForState`, `REASON_TO_TOAST`, `AddProductActionResult` stay)

Per 08-PATTERNS.md audit, EmptyState.tsx already says "Track your first product" (no rename needed) and InlineAddProductWrapper.tsx has no user-facing strings (no rename needed). EmptyState.test.tsx already asserts the post-rename copy. InlineAddProductWrapper.test.tsx has no 'Add Product' literal. Those three files are AUDIT-ONLY, not modified.

Output: Four source/test files modified; full Vitest suite green; repo-wide `grep -rn "Add Product" dealdrop/src` returns zero matches.

Depends on Plan 01 because the renamed `+ Track Price` button picks up the new orange via the token cascade (Shadcn `<Button variant="default">` reads `bg-primary`). No file overlap with Plans 01-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brand-polish/08-CONTEXT.md
@.planning/phases/08-brand-polish/08-RESEARCH.md
@.planning/phases/08-brand-polish/08-PATTERNS.md
@.planning/phases/08-brand-polish/08-UI-SPEC.md
@.planning/phases/08-brand-polish/08-VALIDATION.md

<interfaces>
Affected source — exact CURRENT lines that change:

`dealdrop/src/components/dashboard/AddProductDialog.tsx:23-29` (CURRENT):
```tsx
<DialogTrigger asChild>
  <Button variant="default">+ Add Product</Button>     {/* line 24 */}
</DialogTrigger>
<DialogContent className="sm:max-w-md">
  <DialogHeader>
    <DialogTitle>Add a product</DialogTitle>           {/* line 28 */}
  </DialogHeader>
```

`dealdrop/src/components/dashboard/AddProductForm.tsx`:
- Line 38 (JSDoc inside the file's leading comment block): `*   - { ok: true }   → toast.success('Product added!')`
- Line 44 (toast call inside dispatchToastForState): `toast.success('Product added!')`
- Line 132 (submit button label inside the JSX): `Track`

Affected tests — exact CURRENT lines:

`dealdrop/src/components/dashboard/AddProductForm.test.tsx`:
- Line 93 (test name): `it('toast: { ok: true } fires toast.success once with "Product added!"', () => {`
- Line 96 (assertion): `expect(toastSuccess).toHaveBeenCalledWith('Product added!')`

`dealdrop/src/components/dashboard/ProductGrid.test.tsx:15-21` (CURRENT — stub mock factory):
```tsx
vi.mock('./AddProductDialog', () => ({
  AddProductDialog: (props: { authed: boolean }) => (
    <button data-testid="add-dialog-stub" data-authed={String(props.authed)}>
      + Add Product                                    {/* line 19 */}
    </button>
  ),
}))
```

NOT touched (audit-only — verified by 08-PATTERNS.md):
- `dealdrop/src/components/dashboard/EmptyState.tsx` — heading already says "Track your first product"
- `dealdrop/src/components/dashboard/EmptyState.test.tsx` — asserts the existing post-rename copy
- `dealdrop/src/components/dashboard/InlineAddProductWrapper.tsx` — no user-facing string literals
- `dealdrop/src/components/dashboard/InlineAddProductWrapper.test.tsx` — no 'Add Product' literal

Toast copy reasoning (08-PATTERNS.md §"AddProductForm.tsx — copy rename"):
08-UI-SPEC.md line 197 specifies `'Now tracking [product name]'`, but the dispatcher
function `dispatchToastForState(state)` does NOT have access to the product name at the
call site (the action result `{ ok: true }` carries no payload). The simplest
portfolio-bar copy is `'Now tracking'` (no interpolation). RESEARCH.md §"Code Examples
Example 7" notes this tradeoff explicitly. Use `'Now tracking'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename source — AddProductDialog and AddProductForm copy swaps (D-11 source side)</name>
  <files>dealdrop/src/components/dashboard/AddProductDialog.tsx, dealdrop/src/components/dashboard/AddProductForm.tsx</files>
  <read_first>
    - dealdrop/src/components/dashboard/AddProductDialog.tsx (CURRENT — read entire file; only 2 string literals change)
    - dealdrop/src/components/dashboard/AddProductForm.tsx (CURRENT — read entire file; the 3 changes are at JSDoc line 38, toast line 44, submit label line 132)
    - dealdrop/src/components/dashboard/AddProductForm.test.tsx (read tests at lines 88-115 to understand what's asserted on the renamed strings — Task 2 will update them)
    - .planning/phases/08-brand-polish/08-CONTEXT.md (D-11 — locked rename + audit list)
    - .planning/phases/08-brand-polish/08-PATTERNS.md (§"AddProductDialog.tsx" + §"AddProductForm.tsx" — exact replacement values)
    - .planning/phases/08-brand-polish/08-UI-SPEC.md (§"Copywriting Contract" lines 158-209 — full inventory)
  </read_first>
  <behavior>
    After this task:
    - AddProductDialog renders `+ Track Price` on the trigger button and `Track a price` as the dialog title.
    - AddProductForm renders `Track Price` as the submit button label.
    - AddProductForm `dispatchToastForState({ ok: true })` calls `toast.success('Now tracking')`.
    - The JSDoc comment in AddProductForm.tsx (around line 38) reflects the new toast string.
    - AddProductForm.test.tsx's `'toast: { ok: true } fires toast.success once with "Product added!"' (line 93) FAILS after this task because the test still asserts `'Product added!'` — Task 2 fixes this.
    - All other AddProductForm.test.tsx tests still pass (the `/Track/i` regex at line 36 still matches `'Track Price'`).
  </behavior>
  <action>
    **Sub-step 1.1: Edit `dealdrop/src/components/dashboard/AddProductDialog.tsx`.**

    Two literal swaps inside the JSX. Do NOT rename the component, do NOT change props, do NOT modify the import statements, do NOT change the `useState` or `<Dialog>`/`<DialogContent>` structure.

    On the `<Button variant="default">` line (line 24), change the inner text:
    - Before: `<Button variant="default">+ Add Product</Button>`
    - After: `<Button variant="default">+ Track Price</Button>`

    On the `<DialogTitle>` line (line 28), change the inner text:
    - Before: `<DialogTitle>Add a product</DialogTitle>`
    - After: `<DialogTitle>Track a price</DialogTitle>`

    **Sub-step 1.2: Edit `dealdrop/src/components/dashboard/AddProductForm.tsx`.**

    Three changes (one is a comment, one is a function call argument, one is the JSX text content). Do NOT rename `dispatchToastForState`, `REASON_TO_TOAST`, `AddProductActionResult`, `AddProductForm`, `PENDING_KEY`, or any other identifier.

    Change A — JSDoc comment (around line 38):
    - Before: `*   - { ok: true }   → toast.success('Product added!')`
    - After: `*   - { ok: true }   → toast.success('Now tracking')`

    Change B — toast call argument inside `dispatchToastForState` (line 44):
    - Before: `toast.success('Product added!')`
    - After: `toast.success('Now tracking')`

    Change C — submit button label (line 132, inside the `<Button type="submit">` JSX):
    - Before: `Track`
    - After: `Track Price`

    Do NOT change the `<Loader2 className="mr-2 h-4 w-4 animate-spin" />` element on the line above (the spinner stays). Do NOT modify the `disabled={pending}` prop. Do NOT add or remove any `'use client'` directive (the file already has `'use client'` at line 1; it stays).

    Why `'Now tracking'` and not `'Now tracking [product name]'` (08-UI-SPEC.md line 197): the dispatcher function has no product-name parameter; the action result `{ ok: true }` carries no payload. RESEARCH.md §"Code Examples Example 7" notes this tradeoff explicitly. `'Now tracking'` is the portfolio-bar simplification.

    After both files are saved, run `cd dealdrop && npx vitest run src/components/dashboard/AddProductForm.test.tsx`. The `dispatchToastForState` toast assertion at line 96 (which still asserts `'Product added!'`) WILL FAIL — that's expected. Task 2 fixes the test.
  </action>
  <verify>
    <automated>cd dealdrop &amp;&amp; grep -F "+ Track Price" src/components/dashboard/AddProductDialog.tsx &amp;&amp; grep -F "Track a price" src/components/dashboard/AddProductDialog.tsx &amp;&amp; grep -F "toast.success('Now tracking')" src/components/dashboard/AddProductForm.tsx &amp;&amp; grep -E ">\s*Track Price\s*$|Track Price$" src/components/dashboard/AddProductForm.tsx &amp;&amp; ! grep -F "+ Add Product" src/components/dashboard/AddProductDialog.tsx &amp;&amp; ! grep -F "Add a product" src/components/dashboard/AddProductDialog.tsx &amp;&amp; ! grep -F "Product added!" src/components/dashboard/AddProductForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "+ Track Price" dealdrop/src/components/dashboard/AddProductDialog.tsx` returns at least 1 match
    - `grep -F "Track a price" dealdrop/src/components/dashboard/AddProductDialog.tsx` returns at least 1 match
    - `! grep -F "+ Add Product" dealdrop/src/components/dashboard/AddProductDialog.tsx` (old trigger gone)
    - `! grep -F "Add a product" dealdrop/src/components/dashboard/AddProductDialog.tsx` (old title gone)
    - `grep -F "toast.success('Now tracking')" dealdrop/src/components/dashboard/AddProductForm.tsx` returns at least 1 match
    - `! grep -F "Product added!" dealdrop/src/components/dashboard/AddProductForm.tsx` (old toast string AND old JSDoc comment both gone)
    - `grep -F "Track Price" dealdrop/src/components/dashboard/AddProductForm.tsx` returns at least 1 match (submit button label)
    - `grep -c "'use client'" dealdrop/src/components/dashboard/AddProductForm.tsx` returns 1 (preserved at line 1)
    - `grep -c "'use client'" dealdrop/src/components/dashboard/AddProductDialog.tsx` returns 1 (preserved)
    - `grep -c "function dispatchToastForState" dealdrop/src/components/dashboard/AddProductForm.tsx` returns 1 (function not renamed)
    - `grep -c "REASON_TO_TOAST" dealdrop/src/components/dashboard/AddProductForm.tsx` returns at least 2 (registry not renamed)
    - `cd dealdrop && npm run lint -- src/components/dashboard/AddProductDialog.tsx src/components/dashboard/AddProductForm.tsx` exits 0
    - `cd dealdrop && npx vitest run src/components/dashboard/AddProductForm.test.tsx 2>&1 | grep -E "Product added"` shows the test failure on the still-old assertion (expected — Task 2 fixes this)
  </acceptance_criteria>
  <done>
    AddProductDialog trigger button reads `+ Track Price`, dialog title reads `Track a price`. AddProductForm toast reads `Now tracking`, submit button reads `Track Price`, JSDoc comment matches. No identifiers renamed; `'use client'` preserved on both. AddProductForm.test.tsx now has one failing assertion (Task 2 fixes it).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update test assertions — AddProductForm.test.tsx and ProductGrid.test.tsx (D-11 test side)</name>
  <files>dealdrop/src/components/dashboard/AddProductForm.test.tsx, dealdrop/src/components/dashboard/ProductGrid.test.tsx</files>
  <read_first>
    - dealdrop/src/components/dashboard/AddProductForm.test.tsx (CURRENT — read lines 80-120 to see the dispatchToastForState block)
    - dealdrop/src/components/dashboard/AddProductForm.tsx (the renamed source from Task 1 — these tests must match it)
    - dealdrop/src/components/dashboard/ProductGrid.test.tsx (CURRENT — read lines 1-30 to see the AddProductDialog stub mock factory)
    - .planning/phases/08-brand-polish/08-PATTERNS.md (§"AddProductForm.test.tsx" + §"ProductGrid.test.tsx" — exact assertion swaps)
  </read_first>
  <behavior>
    After this task:
    - AddProductForm.test.tsx test at line 93 (test name) reads with `"Now tracking"` instead of `"Product added!"`.
    - AddProductForm.test.tsx assertion at line 96 reads `expect(toastSuccess).toHaveBeenCalledWith('Now tracking')`.
    - ProductGrid.test.tsx stub mock factory at line 19 reads `+ Track Price` (purely keeps stub honest — the test doesn't assert on this string but consistency matters).
    - The full vitest suite passes. `cd dealdrop && npm run test` exits 0.
  </behavior>
  <action>
    **Sub-step 2.1: Edit `dealdrop/src/components/dashboard/AddProductForm.test.tsx`.**

    Two literal swaps inside the dispatchToastForState describe block. Do NOT modify any other test, do NOT modify the imports or vi.mock setup.

    Change A — test name on line 93:
    - Before: `it('toast: { ok: true } fires toast.success once with "Product added!"', () => {`
    - After: `it('toast: { ok: true } fires toast.success once with "Now tracking"', () => {`

    Change B — assertion on line 96:
    - Before: `expect(toastSuccess).toHaveBeenCalledWith('Product added!')`
    - After: `expect(toastSuccess).toHaveBeenCalledWith('Now tracking')`

    Do NOT change line 36 (`expect(screen.getByRole('button', { name: /Track/i })).toBeTruthy()`) — the regex `/Track/i` still matches `'Track Price'`. The test stays green for both old and new copy.

    Do NOT change the test names or assertions for the duplicate_url, invalid_url, db_error toast tests (lines 100-115) — they assert on REASON_TO_TOAST entries, not on the success string.

    **Sub-step 2.2: Edit `dealdrop/src/components/dashboard/ProductGrid.test.tsx`.**

    One literal swap inside the AddProductDialog vi.mock factory. Do NOT modify any other mock, do NOT modify any test assertion.

    On the line currently reading `+ Add Product` (line 19, inside the stub `<button>`):
    - Before: `+ Add Product`
    - After: `+ Track Price`

    The test never asserts on this string text (it only asserts `data-authed` and `data-testid`), so this swap is purely "keep the stub honest with shipped copy". The test stays green either way; we update for repo-wide consistency.

    After both files are saved:
    1. Run `cd dealdrop && npx vitest run src/components/dashboard/AddProductForm.test.tsx` — must exit 0 (GREEN, all assertions match new source).
    2. Run `cd dealdrop && npx vitest run src/components/dashboard/ProductGrid.test.tsx` — must exit 0.
    3. Run the FULL suite: `cd dealdrop && npm run test` — must exit 0.
    4. Run repo-wide audit: `! grep -rn "Add Product" dealdrop/src` — must produce zero matches across all source AND tests.
    5. Run repo-wide audit: `! grep -rn "Product added!" dealdrop/src` — must produce zero matches.
  </action>
  <verify>
    <automated>cd dealdrop &amp;&amp; grep -F "expect(toastSuccess).toHaveBeenCalledWith('Now tracking')" src/components/dashboard/AddProductForm.test.tsx &amp;&amp; grep -F "+ Track Price" src/components/dashboard/ProductGrid.test.tsx &amp;&amp; ! grep -F "Product added!" src/components/dashboard/AddProductForm.test.tsx &amp;&amp; ! grep -F "+ Add Product" src/components/dashboard/ProductGrid.test.tsx &amp;&amp; ! grep -rn "Add Product" src &amp;&amp; ! grep -rn "Product added!" src &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "expect(toastSuccess).toHaveBeenCalledWith('Now tracking')" dealdrop/src/components/dashboard/AddProductForm.test.tsx` returns at least 1 match
    - `grep -F '"Now tracking"' dealdrop/src/components/dashboard/AddProductForm.test.tsx` returns at least 1 match (test name updated too)
    - `! grep -F "Product added!" dealdrop/src/components/dashboard/AddProductForm.test.tsx`
    - `grep -F "+ Track Price" dealdrop/src/components/dashboard/ProductGrid.test.tsx` returns at least 1 match
    - `! grep -F "+ Add Product" dealdrop/src/components/dashboard/ProductGrid.test.tsx`
    - Repo-wide source rename complete: `! grep -rn "Add Product" dealdrop/src` returns no matches
    - Repo-wide toast rename complete: `! grep -rn "Product added!" dealdrop/src` returns no matches
    - `cd dealdrop && npx vitest run src/components/dashboard/AddProductForm.test.tsx` exits 0
    - `cd dealdrop && npx vitest run src/components/dashboard/ProductGrid.test.tsx` exits 0
    - `cd dealdrop && npx vitest run src/components/dashboard/EmptyState.test.tsx` exits 0 (audit-only file unaffected)
    - `cd dealdrop && npx vitest run src/components/dashboard/InlineAddProductWrapper.test.tsx` exits 0 (audit-only file unaffected)
    - `cd dealdrop && npm run test` exits 0 (FULL suite green)
    - `cd dealdrop && npm run lint -- src/components/dashboard/AddProductForm.test.tsx src/components/dashboard/ProductGrid.test.tsx` exits 0
    - Component file names verification: `test -f dealdrop/src/components/dashboard/AddProductForm.tsx && test -f dealdrop/src/components/dashboard/AddProductDialog.tsx && test -f dealdrop/src/components/dashboard/InlineAddProductWrapper.tsx` (D-11: component files NOT renamed)
    - Backend identifiers verification: `grep -c "addProduct" dealdrop/src/actions/products.ts || true` returns at least 1 (server action not renamed); `grep -r "/api/cron/check-prices" dealdrop/src 2>/dev/null | wc -l` is at least 1 (route not renamed)
  </acceptance_criteria>
  <done>
    AddProductForm.test.tsx assertion + test name updated; ProductGrid.test.tsx stub literal updated; full vitest suite green; repo-wide grep for 'Add Product' and 'Product added!' returns zero matches; component file names and backend identifiers preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM (rendered text) | Pure source-tree copy change — no input flow |
| Sonner toast call argument | Hard-coded literal string |
| Test runtime (Vitest jsdom) | Assertion strings are hard-coded literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-RENAME-LEAK-LOW | Information Disclosure (theoretical — could a half-rename leave the user with inconsistent CTA copy?) | All four files | mitigate | Repo-wide grep audits (`! grep -rn "Add Product" dealdrop/src` and `! grep -rn "Product added!" dealdrop/src`) are acceptance criteria. Full vitest suite is also a gate. Half-rename is not possible if both grep audits are green. |
| T-08-05-IDENTIFIER-RENAME-LOW | Tampering (theoretical — could rename creep into backend identifiers?) | AddProductForm.tsx, AddProductDialog.tsx | mitigate | Acceptance criteria explicitly verify component file names exist post-rename and that `addProduct` action + `/api/cron/check-prices` route names are preserved. D-11 explicit boundary. |

**Security relevance:** None. This plan modifies user-facing string literals in four files. No new auth, data, route, env, server, external surface, or identifier changes. Documented per gsd-planner agent contract.
</threat_model>

<verification>
- `! grep -rn "Add Product" dealdrop/src` (zero matches across all source and tests)
- `! grep -rn "Product added!" dealdrop/src` (zero matches)
- `! grep -rn "Add a product" dealdrop/src` (zero matches; both old dialog title forms gone)
- `cd dealdrop && npm run test` exits 0 (full vitest suite green)
- `cd dealdrop && npm run lint` exits 0 on touched files
- `cd dealdrop && npm run build` exits 0
- D-11 boundary checks: `test -f dealdrop/src/components/dashboard/AddProductForm.tsx`, `test -f dealdrop/src/components/dashboard/AddProductDialog.tsx`, `test -f dealdrop/src/components/dashboard/InlineAddProductWrapper.tsx`, `test -f dealdrop/src/components/dashboard/EmptyState.tsx` (all four component files preserved)
- Backend identifiers preserved: `grep -r "addProduct" dealdrop/src/actions` shows the action still exists; `grep -r "/api/cron/check-prices" dealdrop` shows the route path still exists
</verification>

<success_criteria>
- AddProductDialog renders `+ Track Price` and `Track a price`
- AddProductForm renders `Track Price` submit button and fires `toast.success('Now tracking')`
- AddProductForm.test.tsx asserts `'Now tracking'` instead of `'Product added!'`
- ProductGrid.test.tsx stub uses `+ Track Price`
- Repo-wide grep audits pass (zero matches for old strings)
- Full vitest suite green
- Component file names and backend identifiers preserved (D-11 boundary)
</success_criteria>

<output>
After completion, create `.planning/phases/08-brand-polish/08-05-SUMMARY.md` documenting:
- The four files modified and the exact line-level diffs
- Repo-wide grep audit output (showing zero matches for old strings)
- Full vitest suite pass output
- Confirmation that EmptyState.tsx, EmptyState.test.tsx, InlineAddProductWrapper.tsx, and InlineAddProductWrapper.test.tsx were AUDIT-ONLY (no edits) — list these by filename
- Confirmation of D-11 boundaries: component file names AND backend identifiers (addProduct, /api/cron/check-prices, sendPriceDropAlert) are unchanged
- Note for the BRAND-05 visual walk: confirm the renamed CTAs read correctly on logged-out hero, dashboard empty state, and dashboard with products
</output>
