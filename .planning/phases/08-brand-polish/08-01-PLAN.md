---
phase: 08-brand-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dealdrop/app/globals.css
  - dealdrop/src/components/dashboard/ProductCard.tsx
autonomous: true
requirements: [BRAND-04]
nyquist_compliant: true
tags: [brand, theming, tailwind, css, tokens]

must_haves:
  truths:
    - "Editing a single CSS custom property (--primary) is the only source of brand color change in source"
    - "Light-mode :root --primary is the verified Tailwind v4 orange-500 oklch value"
    - "Dark-mode --primary is the verified Tailwind v4 orange-400 oklch value (lifted L for legibility)"
    - "ProductCard current_price <p> reads text-primary so it cascades to the new orange"
    - "All other Shadcn tokens (--accent, --secondary, --muted, etc.) are byte-for-byte unchanged"
  artifacts:
    - path: "dealdrop/app/globals.css"
      provides: "Redefined --primary and --primary-foreground in :root and @media (prefers-color-scheme: dark) blocks"
      contains: "oklch(70.5% 0.213 47.604)"
      contains_2: "oklch(75% 0.183 55.934)"
    - path: "dealdrop/src/components/dashboard/ProductCard.tsx"
      provides: "Price <p> uses text-primary so the cascade auto-applies orange-500 to product prices"
      contains: "text-xl font-semibold text-primary"
  key_links:
    - from: "globals.css :root --primary"
      to: "Shadcn <Button variant='default'> bg-primary"
      via: "@theme inline { --color-primary: var(--primary); }"
      pattern: "bg-primary"
    - from: "globals.css :root --primary"
      to: "PriceChart <Line stroke='var(--primary)' />"
      via: "direct var(--primary) consumption"
      pattern: "stroke=\"var\\(--primary\\)\""
    - from: "globals.css :root --primary"
      to: "FeatureCard <Icon className='text-primary' />"
      via: "Tailwind text-primary utility"
      pattern: "text-primary"
    - from: "globals.css :root --primary"
      to: "ProductCard price <p>"
      via: "Tailwind text-primary utility (added in this plan)"
      pattern: "text-xl font-semibold text-primary"
---

<objective>
Wave 1 foundation plan. Redefine the Shadcn `--primary` token in `dealdrop/app/globals.css` from zinc-900/zinc-200 (current v1.0 neutral) to verified Tailwind v4.2.2 stock orange (orange-500 light / orange-400 dark) so every existing `bg-primary`, `text-primary`, `hover:bg-primary/90`, and `var(--primary)` consumer auto-restyles via the cascade with zero per-component class churn. In the same plan, add `text-primary` to the ProductCard price `<p>` so dashboard prices participate in the cascade (the only `--primary` consumer that does NOT already exist in v1.0 source).

Purpose: BRAND-04 requires "a single accent color is applied consistently across primary buttons, links, and key UI highlights, defined as a Tailwind theme token / CSS custom property (so future palette changes are one-line)". D-06 locks the implementation to redefining `--primary` and `--primary-foreground` only — no new tokens, no per-component utility overrides. This plan IS the cascade. Plans 02-05 build on top of this token foundation.

Output: `globals.css` with verified oklch values in both `:root` and `@media (prefers-color-scheme: dark)` blocks; ProductCard price now reads `text-primary`. Buttons, chart line, FeatureCard icons re-tint automatically (no edits to those files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brand-polish/08-CONTEXT.md
@.planning/phases/08-brand-polish/08-RESEARCH.md
@.planning/phases/08-brand-polish/08-PATTERNS.md
@.planning/phases/08-brand-polish/08-UI-SPEC.md
@.planning/phases/08-brand-polish/08-VALIDATION.md

<interfaces>
<!-- Source-of-truth oklch values verified at plan-write time against
     dealdrop/node_modules/tailwindcss/theme.css (Tailwind v4.2.2). -->

Verified Tailwind v4.2.2 oklch palette (from `dealdrop/node_modules/tailwindcss/theme.css`):
```
--color-orange-50:  oklch(98% 0.016 73.684);
--color-orange-400: oklch(75% 0.183 55.934);
--color-orange-500: oklch(70.5% 0.213 47.604);
--color-orange-600: oklch(64.6% 0.222 41.116);
--color-orange-700: oklch(55.3% 0.195 38.402);
```

WARNING: 08-UI-SPEC.md line 108 lists `oklch(0.646 0.222 41.116)` as orange-500 — that
value is actually orange-**600**. RESEARCH.md §"Common Pitfalls Pitfall 1" calls this
out. Use the verified values above.

Existing globals.css cascade plumbing (DO NOT REMOVE):
```css
@theme inline {
  --color-primary-foreground: var(--primary-foreground);
  --color-primary: var(--primary);
}
```

Existing consumers that auto-restyle (NO EDITS REQUIRED):
- `dealdrop/components/ui/button.tsx:12` — default variant `bg-primary text-primary-foreground hover:bg-primary/90`
- `dealdrop/src/components/dashboard/PriceChart.tsx:117-120` — `<Line stroke="var(--primary)" />`, dot/activeDot `fill: 'var(--primary)'`
- `dealdrop/src/components/hero/FeatureCard.tsx:13` — `<Icon className="h-6 w-6 text-primary" />`

Existing ProductCard price (the ONLY consumer that needs a class addition this plan):
```tsx
// dealdrop/src/components/dashboard/ProductCard.tsx:29-31 (CURRENT)
<p className="text-xl font-semibold">
  {formatPrice(product.current_price, product.currency)}
</p>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Redefine --primary and --primary-foreground tokens to verified orange oklch values in globals.css (BRAND-04 / D-06 / D-07)</name>
  <files>dealdrop/app/globals.css</files>
  <read_first>
    - dealdrop/app/globals.css (read entire file — see current values for both blocks before editing)
    - dealdrop/node_modules/tailwindcss/theme.css (verify orange-500 = `oklch(70.5% 0.213 47.604)` and orange-400 = `oklch(75% 0.183 55.934)` — grep "color-orange-500" and "color-orange-400")
    - .planning/phases/08-brand-polish/08-CONTEXT.md (D-06 + D-07 — locked decisions)
    - .planning/phases/08-brand-polish/08-RESEARCH.md (§"Common Pitfalls Pitfall 1" — UI-SPEC has wrong oklch; RESEARCH has correct)
    - .planning/phases/08-brand-polish/08-PATTERNS.md (§"globals.css token redefinition" — exact 4 lines to swap)
  </read_first>
  <action>
    Edit `dealdrop/app/globals.css`. Make exactly four token-value substitutions. Do NOT touch any other token. Do NOT touch `--accent`. Do NOT touch the `@theme inline` block. Do NOT touch any `--sidebar-*`, `--chart-*`, `--secondary`, `--muted`, `--popover`, `--card`, `--border`, `--input`, `--ring`, `--destructive`, or `--radius` value. Do NOT add new lines; only modify the values on the four target lines.

    1. In the `:root` block at the top of the file (currently `dealdrop/app/globals.css:6-39`):
       - Line `--primary: oklch(0.21 0.006 285.885);` → change to `--primary: oklch(70.5% 0.213 47.604);` (Tailwind v4.2.2 orange-500)
       - Line `--primary-foreground: oklch(0.985 0 0);` → leave UNCHANGED (zinc-50 still passes WCAG AA on orange-500; verified in RESEARCH.md §"Pattern 1" and 08-UI-SPEC.md line 109)

    2. In the `@media (prefers-color-scheme: dark) { :root { ... } }` block (currently `dealdrop/app/globals.css:81-114`):
       - Line `--primary: oklch(0.92 0.004 286.32);` → change to `--primary: oklch(75% 0.183 55.934);` (Tailwind v4.2.2 orange-400 — lifted lightness for legibility on near-black background, per D-07)
       - Line `--primary-foreground: oklch(0.21 0.006 285.885);` → change to `--primary-foreground: oklch(0.141 0.005 285.823);` (zinc-950 — flips to near-black for AA contrast on lighter orange-400 dark-mode primary)

    Use the verified oklch values from `dealdrop/node_modules/tailwindcss/theme.css` (cited in 08-RESEARCH.md). Do NOT use the `oklch(0.646 0.222 41.116)` value listed at 08-UI-SPEC.md line 108 — that is orange-600, not orange-500 (RESEARCH.md §"Common Pitfalls Pitfall 1").

    After saving the file, run `cd dealdrop && npm run build` and confirm the build succeeds. The Tailwind v4 PostCSS pipeline will compile the redefined tokens into the production CSS bundle. Build green = oklch syntax accepted by the Tailwind v4 + Lightning CSS resolver. Note: do NOT run `npm install` (no new dependencies in this phase).
  </action>
  <verify>
    <automated>cd dealdrop &amp;&amp; grep -c "^\s*--primary: oklch(70.5% 0.213 47.604);" app/globals.css | grep -q "^1$" &amp;&amp; grep -c "^\s*--primary: oklch(75% 0.183 55.934);" app/globals.css | grep -q "^1$" &amp;&amp; grep -c "^\s*--primary-foreground: oklch(0.141 0.005 285.823);" app/globals.css | grep -q "^1$" &amp;&amp; grep -c "^\s*--accent: oklch(0.967 0.001 286.375);" app/globals.css | grep -q "^1$" &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\s*--primary: oklch(70.5% 0.213 47.604);" dealdrop/app/globals.css` returns exactly `1` (light-mode :root)
    - `grep -c "^\s*--primary: oklch(75% 0.183 55.934);" dealdrop/app/globals.css` returns exactly `1` (dark-mode block)
    - `grep -c "^\s*--primary-foreground: oklch(0.985 0 0);" dealdrop/app/globals.css` returns exactly `1` (light-mode unchanged)
    - `grep -c "^\s*--primary-foreground: oklch(0.141 0.005 285.823);" dealdrop/app/globals.css` returns exactly `1` (dark-mode swapped)
    - `grep "^\s*--accent:" dealdrop/app/globals.css` shows BOTH original Shadcn values (light: `oklch(0.967 0.001 286.375)`, dark: `oklch(0.274 0.006 286.033)`) — `--accent` is untouched
    - `! grep "oklch(0.21 0.006 285.885)" dealdrop/app/globals.css | grep -- "--primary:"` (the OLD light-mode --primary value is gone from the --primary line)
    - `! grep "oklch(0.92 0.004 286.32)" dealdrop/app/globals.css | grep -- "--primary:"` (the OLD dark-mode --primary value is gone from the --primary line)
    - `cd dealdrop && npm run build` exits 0 (oklch syntax compiles cleanly through Tailwind v4 + Lightning CSS)
    - `git diff dealdrop/app/globals.css | grep -c "^[+-]"` shows exactly 6 changed lines (3 `-` removals + 3 `+` additions; the `--primary-foreground` light-mode line is unchanged)
  </acceptance_criteria>
  <done>
    globals.css `--primary` and (dark-mode) `--primary-foreground` swapped to verified Tailwind v4 orange oklch values; light-mode `--primary-foreground` left unchanged; no other token modified; `npm run build` green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply text-primary to ProductCard price &lt;p&gt; so dashboard prices cascade to orange-500 (BRAND-04 / D-05)</name>
  <files>dealdrop/src/components/dashboard/ProductCard.tsx</files>
  <read_first>
    - dealdrop/src/components/dashboard/ProductCard.tsx (read entire file; the price `<p>` is at line 29-31)
    - dealdrop/src/components/dashboard/ProductCard.test.tsx (read first 60 lines to understand test pattern; the existing tests do NOT assert on the className of the price element so this change cannot break them)
    - .planning/phases/08-brand-polish/08-CONTEXT.md (D-05 — orange-500 on prices)
    - .planning/phases/08-brand-polish/08-PATTERNS.md (§"ProductCard.tsx — price text-primary" — exact single-class addition)
    - .planning/phases/08-brand-polish/08-UI-SPEC.md (lines 71, 134 — Price is the Subheading-with-accent alias)
  </read_first>
  <action>
    Edit `dealdrop/src/components/dashboard/ProductCard.tsx`. Locate the price `<p>` at line 29 (currently `<p className="text-xl font-semibold">`). Append the `text-primary` utility class.

    Before:
    ```tsx
    <p className="text-xl font-semibold">
      {formatPrice(product.current_price, product.currency)}
    </p>
    ```

    After:
    ```tsx
    <p className="text-xl font-semibold text-primary">
      {formatPrice(product.current_price, product.currency)}
    </p>
    ```

    `text-primary` resolves via the `@theme inline { --color-primary: var(--primary); }` binding in `globals.css:69-70`, which means orange-500 auto-cascades from the Task 1 redefinition.

    Do NOT add `text-orange-500` directly — that bypasses the token cascade and creates two sources of brand color (RESEARCH.md §"Anti-Patterns to Avoid" + 08-PATTERNS.md §"Token Cascade"). Do NOT touch any other line in ProductCard.tsx — the product name `<p>` (line 28), the image, the View Product button, the chart toggle, and the RemoveProductDialog must stay byte-for-byte identical.

    After saving, run `cd dealdrop && npx vitest run src/components/dashboard/ProductCard.test.tsx` to confirm the existing tests still pass (none assert on the className of the price element, so they will continue to pass).
  </action>
  <verify>
    <automated>cd dealdrop &amp;&amp; grep -c "text-xl font-semibold text-primary" src/components/dashboard/ProductCard.tsx | grep -q "^1$" &amp;&amp; npx vitest run src/components/dashboard/ProductCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "text-xl font-semibold text-primary" dealdrop/src/components/dashboard/ProductCard.tsx` returns exactly `1`
    - `! grep "text-orange-500" dealdrop/src/components/dashboard/ProductCard.tsx` (token-cascade purity — no direct orange utilities)
    - `git diff dealdrop/src/components/dashboard/ProductCard.tsx | grep "^[+-]" | grep -v "^[+-]\{3\}" | wc -l` returns `2` (1 line removed, 1 line added — single-class diff)
    - `cd dealdrop && npx vitest run src/components/dashboard/ProductCard.test.tsx` exits 0 (existing tests unaffected)
    - `cd dealdrop && npx vitest run src/components/dashboard/PriceChart.test.tsx` exits 0 (PriceChart still green; cascade did not regress)
    - `cd dealdrop && npm run build` exits 0
  </acceptance_criteria>
  <done>
    ProductCard price `<p>` reads `text-xl font-semibold text-primary`; ProductCard.test.tsx still green; PriceChart.test.tsx still green; build green. Dashboard prices, Shadcn buttons, chart line, and FeatureCard icons all now render the new orange via the single-token cascade.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser CSS cascade | Color tokens are runtime CSS — no untrusted input crosses this boundary |
| Static asset serving | None in this plan (no asset additions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-CSS-CONTRAST-LOW | Information Disclosure (legibility regression — verifiable risk, not a security threat) | `globals.css` `--primary` token | accept | Per-mode oklch values verified against Tailwind v4 source; AA contrast spot-check is BRAND-05's job (Plan 06 manual walk). orange-500 on zinc-50 ≈ 4.6:1 (passes AA); orange-400 on zinc-900 ≈ 4.8:1 (passes AA). |
| T-08-01-TOKEN-COLLISION-LOW | Tampering (token-name overlap with Shadcn `--accent`) | `globals.css` | mitigate | Action explicitly forbids touching `--accent`. Acceptance criteria asserts both `--accent` values are byte-for-byte unchanged. |

**Security relevance:** None. This plan modifies CSS custom property values only — no auth, data, route, env, server, or external-surface change. No new XSS, SSRF, or injection surface is introduced. Token-cascade purity (no per-component `bg-orange-500`) is a maintainability concern, not a security one. Documented per gsd-planner agent contract.
</threat_model>

<verification>
- `cd dealdrop && grep -c "^\s*--primary: oklch(70.5% 0.213 47.604);" app/globals.css` → `1`
- `cd dealdrop && grep -c "^\s*--primary: oklch(75% 0.183 55.934);" app/globals.css` → `1`
- `cd dealdrop && grep -c "text-xl font-semibold text-primary" src/components/dashboard/ProductCard.tsx` → `1`
- `cd dealdrop && npx vitest run src/components/dashboard/ProductCard.test.tsx src/components/dashboard/PriceChart.test.tsx` exits 0
- `cd dealdrop && npm run build` exits 0
- `cd dealdrop && npm run lint -- app/globals.css src/components/dashboard/ProductCard.tsx` exits 0 (ESLint green on touched files)
</verification>

<success_criteria>
- `--primary` (light mode `:root`) = `oklch(70.5% 0.213 47.604)` — verified Tailwind v4.2.2 orange-500
- `--primary` (dark mode `@media`) = `oklch(75% 0.183 55.934)` — verified Tailwind v4.2.2 orange-400
- `--primary-foreground` (light) unchanged at `oklch(0.985 0 0)` (zinc-50)
- `--primary-foreground` (dark) = `oklch(0.141 0.005 285.823)` (zinc-950 — flips dark on lighter orange-400)
- ProductCard price reads `text-xl font-semibold text-primary` (cascade-respecting)
- All other Shadcn tokens byte-for-byte unchanged
- `npm run build` and `npm run lint` green; existing dashboard tests still green
- BRAND-04 single-source-of-truth contract is now in place; Plans 02-05 build on top of this token foundation
</success_criteria>

<output>
After completion, create `.planning/phases/08-brand-polish/08-01-SUMMARY.md` documenting:
- Exact oklch values used (light + dark)
- The four-line diff in globals.css
- Whether `--primary-foreground` (light) was touched (answer: no — verified zinc-50 still passes AA)
- Confirmation that no other token was modified
- Test runs that stayed green (ProductCard, PriceChart)
- Build status
</output>
