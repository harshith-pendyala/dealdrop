---
phase: 07-polish-deployment
plan: 08
type: execute
wave: 6
depends_on: [07-07]
files_modified:
  - .planning/phases/07-polish-deployment/07-VERIFICATION.md
  - .planning/phases/07-polish-deployment/screenshots/dep-06-email.png
  - .planning/phases/07-polish-deployment/screenshots/dep-06-chart.png
autonomous: false
requirements: [DEP-01, DEP-06]
user_setup:
  - service: "non-owner-gmail"
    why: "PITFALLS.md:342 — Resend account-owner inboxes silently succeed even when domain DNS is broken; the only reliable verification is sending TO an inbox NOT owned by the Resend account email."
    dashboard_config:
      - task: "Use the fresh Google account from Plan 07-06 Task 3 (the same account that completed prod OAuth) — must be DIFFERENT from the Resend account email"
        location: "any Gmail address that is not the Resend account email"
must_haves:
  truths:
    - "DEP-01 closed: prod Vercel URL responds 200 to GET / (logged-out hero) — re-confirmed at phase close"
    - "DEP-06 closed: end-to-end flow — sign in (fresh non-owner Google) → add product → confirm seed price_history row → manipulate price → cron POST → email arrives in non-owner inbox → chart shows two data points → idempotent re-run produces dropped:0"
    - "All 10 line items in PITFALLS.md:336-348 are individually checked and recorded in 07-VERIFICATION.md as PASS rows"
    - "Screenshots committed: email screenshot AND chart screenshot at .planning/phases/07-polish-deployment/screenshots/"
    - "07-VERIFICATION.md frontmatter status updated from in_progress to passed once all rows are PASS"
  artifacts:
    - path: .planning/phases/07-polish-deployment/07-VERIFICATION.md
      provides: full DEP-01/06 + PITFALLS.md grid populated; final status: passed
      contains: "DEP-06"
      contains_2: "PITFALLS.md:336"
      contains_3: "Looks Done But Isn't"
    - path: .planning/phases/07-polish-deployment/screenshots/dep-06-email.png
      provides: visual proof of price-drop email in non-owner inbox
    - path: .planning/phases/07-polish-deployment/screenshots/dep-06-chart.png
      provides: visual proof of dashboard chart with two data points after cron-driven update
  key_links:
    - from: cURL POST /api/cron/check-prices with prod CRON_SECRET
      to: price-drop email in non-owner Gmail inbox
      via: prod cron handler → runPriceCheck → Resend sendPriceDropAlert
      pattern: "dropped: 1 in HTTP response body + email row in non-owner Gmail"
    - from: cURL POST (re-fired with no further price changes)
      to: idempotent zero-drop response
      via: price-change gate (Phase 6 D-02)
      pattern: "dropped: 0 in HTTP response body + no new email"
---

<objective>
Run the synthesis plan: walk the full DEP-06 end-to-end flow on the prod URL with a fresh non-owner Google account, screenshot the email + chart, then check off every line item in PITFALLS.md:336-348 ("Looks Done But Isn't" inspection grid). This is the proof the core-value loop works on the live URL — the artifact a portfolio reviewer would inspect to know DealDrop is real.

Purpose: Closes DEP-01 + DEP-06 + the entire Phase 7 phase gate. Verifies that every Phase 1-6 contract still holds when the app is running against prod Supabase + prod env vars + prod cron + prod OAuth.

Output: Populated 07-VERIFICATION.md with: (a) DEP-06 procedure rows (10 numbered steps), (b) PITFALLS.md:336-348 inspection grid (10 line items as 10 PASS rows), (c) two committed screenshots, (d) frontmatter `status: passed` and final `score: <X>/<Y>` filled in.

Prerequisites (must all be DONE before starting this plan):
- Plan 07-01: error.tsx + global-error.tsx shipped
- Plan 07-02: app/icon.tsx shipped, favicon.ico deleted
- Plan 07-03: POL-01/02/05 verification rows in 07-VERIFICATION.md
- Plan 07-04: POL-04 mobile audit complete
- Plan 07-05: prod Vercel URL live; 7 env vars in production scope; Fluid Compute on; Vault secret seeded
- Plan 07-06: OAuth registered on prod (Google + Supabase); fresh-account smoke test passed
- Plan 07-07: pg_cron points at prod URL with prod Vault secret

This is human-driven work — the executor is a HUMAN OPERATOR running cURL, SQL queries, browser walks, and email-inbox checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-polish-deployment/07-CONTEXT.md
@.planning/phases/07-polish-deployment/07-RESEARCH.md
@.planning/phases/07-polish-deployment/07-VERIFICATION.md
@.planning/research/PITFALLS.md
@.planning/phases/06-automated-monitoring-email-alerts/06-VERIFICATION.md
@dealdrop/app/api/cron/check-prices/route.ts
@dealdrop/src/lib/cron/check-prices.ts
@dealdrop/src/lib/resend.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: DEP-06 procedure — sign in (fresh non-owner) → add product → seed price_history</name>
  <files>(no files modified directly — operator-driven; artifacts updated via <how-to-verify> steps)</files>
  <read_first>
    - .planning/phases/07-polish-deployment/07-RESEARCH.md (Pattern 7 — full DEP-06 procedure mechanics; the non-owner-inbox rationale; the SQL UPDATE force-drop trick)
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (DEP-01 / 03 / 04 — `<PROD_VERCEL_URL>`, `<PROD_REF>`, fresh Google account from Plan 07-06)
    - .planning/research/PITFALLS.md (lines 336-348 — the inspection grid that will be walked in Task 3)
    - .planning/phases/06-automated-monitoring-email-alerts/06-VERIFICATION.md (the format precedent for the rows being added in this plan)
  </read_first>
  <action>Operator-driven checkpoint. Follow the numbered steps in <how-to-verify> verbatim. Each step has an explicit expected output / observed value. The acceptance_criteria block at the bottom of this task is the gate; do NOT mark this task done unless every acceptance row is satisfied.</action>
  <what-built>
    Production stack is up: Vercel URL is live, OAuth is registered, prod Supabase has data tables + Vault + cron pointing at prod. This task signs in with a fresh account and adds one product to seed the test data.
  </what-built>
  <how-to-verify>
    **Pre-flight — confirm prerequisites are green (~30s):**
    ```bash
    # 1. Health-check the prod URL
    curl -I https://<PROD_VERCEL_URL>/
    # Expected: HTTP/2 200

    # 2. Health-check the cron route
    curl -i https://<PROD_VERCEL_URL>/api/cron/check-prices
    # Expected: 200 + {"status":"ok"}

    # 3. Confirm 401 on no-auth POST
    curl -i -X POST https://<PROD_VERCEL_URL>/api/cron/check-prices
    # Expected: 401 + {"error":"Unauthorized"}
    ```

    If any pre-flight fails, STOP and surface a deviation pointing at the prerequisite plan that broke (07-05 most likely).

    **Step 1 — Sign in on prod with the fresh non-owner Google account:**
    1. Open `https://<PROD_VERCEL_URL>/` in an incognito window.
    2. Sign in with the fresh Google account from Plan 07-06 Task 3 (the one that has never signed in to DealDrop and is NOT the Resend account email — confirmed in CONTEXT.md / PITFALLS.md:342).
    3. After OAuth completes, you should land at `/` with the EmptyState (no products yet).

    **Step 2 — Add a product:**
    1. Click "Add product" → AddProductDialog opens.
    2. Paste a known-good URL. Recommended: `https://books.toscrape.com/catalogue/a-light-in-the-attic_1000/index.html` (Phase 3 known-good test target, stable price, books.toscrape.com is intentionally a scraping sandbox).
    3. Click Track.
    4. Wait for the SkeletonCard → ProductCard transition (~5-10s while Firecrawl scrapes).
    5. Confirm the ProductCard shows a name + price (e.g. £51.77) + image.

    **Step 3 — Confirm seed price_history row in prod Supabase SQL Editor:**
    Open https://supabase.com/dashboard/project/`<PROD_REF>`/sql/new and run:
    ```sql
    SELECT id, user_id, name, current_price, currency
    FROM products
    ORDER BY created_at DESC
    LIMIT 1;
    ```
    Capture: `<PRODUCT_ID>` (UUID) and the seed price (`<SEED_PRICE>`).

    Then:
    ```sql
    SELECT product_id, price, currency, checked_at
    FROM price_history
    WHERE product_id = '<PRODUCT_ID>'
    ORDER BY checked_at DESC;
    ```
    Expected: exactly 1 row (the seed point from TRACK-06). Confirms the products table → price_history wiring.

    **Step 4 — Append to 07-VERIFICATION.md:**

    ```markdown
    ## DEP-06: End-to-End Verification

    **Operator:** <name>
    **Date:** <YYYY-MM-DD>
    **Fresh non-owner Google account email:** <placeholder — DO NOT commit the actual address>
    **Resend account email:** <placeholder — for reference; this address is NOT used as the To: address>
    **Prod URL:** `https://<PROD_VERCEL_URL>/`
    **Prod Supabase ref:** `<PROD_REF>`
    **Test product URL:** `https://books.toscrape.com/catalogue/a-light-in-the-attic_1000/index.html`
    **Test product seed price:** `<SEED_PRICE>` (e.g. 51.77 GBP)
    **Test product UUID:** `<PRODUCT_ID>`

    ### Pre-flight (Plan 07-05 / 07-07 sanity)

    | Check | Command | Expected | Observed |
    |-------|---------|----------|----------|
    | Prod hero loads | `curl -I https://<PROD_VERCEL_URL>/` | HTTP/2 200 | 200 |
    | Cron health | `curl https://<PROD_VERCEL_URL>/api/cron/check-prices` | 200 + `{"status":"ok"}` | PASS |
    | Cron auth gate | `curl -X POST https://<PROD_VERCEL_URL>/api/cron/check-prices` | 401 | PASS |

    ### Steps 1-3 (sign in + add product + seed)

    | # | Action | Expected | Observed |
    |---|--------|----------|----------|
    | 1 | Sign in on prod with fresh non-owner Google account | Lands at `/` with EmptyState | PASS |
    | 2 | Add `books.toscrape.com/.../a-light-in-the-attic` URL | ProductCard renders with name + price + image | PASS |
    | 3 | `SELECT * FROM products ORDER BY created_at DESC LIMIT 1` in prod SQL | 1 row with `current_price = <SEED_PRICE>` | PASS |
    | 3a | `SELECT * FROM price_history WHERE product_id = '<PRODUCT_ID>'` | exactly 1 row (seed) | PASS |
    ```
  </how-to-verify>
  <acceptance_criteria>
    - Pre-flight: 200 / 200+ok / 401 all green
    - Sign-in completes on prod with fresh account
    - Product added; ProductCard shows name + price + image
    - `products` row exists in PROD Supabase project (verifies DEP-03)
    - `price_history` row exists (1 row — seed)
    - 07-VERIFICATION.md DEP-06 header + Steps 1-3 table populated
    - `<PRODUCT_ID>` and `<SEED_PRICE>` captured for Task 2
  </acceptance_criteria>
  <resume-signal>Type "approved: seed in place, product_id &lt;UUID&gt; price &lt;value&gt;" once Step 3 SQL queries return expected rows. If Firecrawl scrape fails, type "deviation: scrape failed: &lt;reason&gt;".</resume-signal>
  <verify>
    <automated>MISSING — checkpoint task; verification is operator-driven via <how-to-verify> + <acceptance_criteria>; no automated command applies</automated>
  </verify>
  <done>All <acceptance_criteria> rows satisfied; <resume-signal> typed by operator.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Force a price drop via SQL UPDATE → cURL cron POST → confirm dropped:1 + email + chart</name>
  <files>(no files modified directly — operator-driven; artifacts updated via <how-to-verify> steps)</files>
  <read_first>
    - .planning/phases/07-polish-deployment/07-RESEARCH.md (Pattern 7 Step 4 — the SQL UPDATE force-drop trick; Step 7 — what the email body must contain; Step 10 — idempotency re-fire)
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (Task 1 captured `<PRODUCT_ID>` and `<SEED_PRICE>`; DEP-02 has the prod CRON_SECRET stored in Vercel — operator retrieves via `vercel env pull` or password manager)
    - .planning/research/PITFALLS.md:342 (the non-owner inbox requirement)
  </read_first>
  <action>Operator-driven checkpoint. Follow the numbered steps in <how-to-verify> verbatim. Each step has an explicit expected output / observed value. The acceptance_criteria block at the bottom of this task is the gate; do NOT mark this task done unless every acceptance row is satisfied.</action>
  <what-built>
    Task 1 seeded one product with one price_history row. Task 2 manipulates the stored `current_price` to be 2x the real value, then fires the cron POST — Firecrawl re-scrapes the real (lower) price, the price-change gate fires, a price_history row is inserted, and `sendPriceDropAlert` lands a Resend email in the non-owner inbox.
  </what-built>
  <how-to-verify>
    **Step 1 — Manipulate the stored price to force a drop on the next cron run:**

    Open https://supabase.com/dashboard/project/`<PROD_REF>`/sql/new and run:
    ```sql
    UPDATE products
    SET current_price = current_price * 2
    WHERE id = '<PRODUCT_ID>';

    SELECT current_price FROM products WHERE id = '<PRODUCT_ID>';
    ```
    Expected: `current_price` is now `2 * <SEED_PRICE>` (e.g. 103.54 GBP if seed was 51.77).

    Capture the new "stored" price as `<INFLATED_PRICE>`.

    **Step 2 — Retrieve the prod CRON_SECRET:**

    The CRON_SECRET is in Vercel `production` env scope (set in Plan 07-05 Task 2 Step 3 with `--sensitive`). Retrieval options:
    - **Option A:** From your password manager where you saved it during Plan 07-05.
    - **Option B:** `cd dealdrop && npx vercel@latest env pull .env.prod.tmp --environment=production` then `grep '^CRON_SECRET=' .env.prod.tmp`. **DELETE `.env.prod.tmp` immediately after** (don't leave it on disk; it's also gitignored as `.env*` so it won't accidentally be committed).
    - **Option C:** Direct from Supabase Vault (since same value in both): in prod SQL Editor, `SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'dealdrop_cron_secret';` — run once, copy, close the tab.

    Save as `<PROD_CRON_SECRET>` for the next step. Do NOT echo or log this value.

    **Step 3 — Fire the cron POST:**
    ```bash
    curl -i -X POST "https://<PROD_VERCEL_URL>/api/cron/check-prices" \
      -H "Authorization: Bearer <PROD_CRON_SECRET>" \
      -H "Content-Type: application/json" \
      -d '{}'
    ```

    Expected response:
    - `HTTP/2 200`
    - Body: `{"status":"ok","scraped":1,"updated":1,"dropped":1,"failed":[]}`

    Capture the JSON response. If `dropped:0`, the price-change gate did not fire — re-check the UPDATE in Step 1 actually doubled the price (and that Firecrawl returned the real lower price; books.toscrape.com is stable but if it's down, swap to a different known-good URL).

    **Step 4 — Confirm the email landed in the non-owner inbox:**

    Open the FRESH NON-OWNER GMAIL (the one used for sign-in in Task 1, which is also the email Resend will send TO via EMAIL-05). Look for a new email with:
    - Subject: matches `lib/resend.ts` template (e.g. "Price drop on <product name>")
    - Body contains: product image, product name, strikethrough old price (`<INFLATED_PRICE>`), prominent new price (the seed/real price), `&minus;XX%` percentage hero, "View Product" CTA linking to the original URL with `target="_blank" rel="noopener noreferrer"` semantics.

    The email should arrive within ~10-30 seconds of Step 3's `dropped:1` response. If after 5 minutes still no email:
    - Check Resend Dashboard → Logs (https://resend.com/logs). If Resend says "delivered" but Gmail doesn't show it → DNS misconfiguration (R-09); send a Resend test email from the dashboard to the same non-owner inbox to isolate.
    - Check Gmail Spam folder.

    **Step 5 — Screenshot the email:**
    Take a screenshot showing:
    - Sender: <RESEND_FROM_EMAIL> from the prod Resend domain.
    - To: the non-owner Gmail (you can blur the address).
    - Body: clearly readable with all required elements.

    Save to `.planning/phases/07-polish-deployment/screenshots/dep-06-email.png`.

    **Step 6 — Confirm the chart updated on the dashboard:**

    Reload `https://<PROD_VERCEL_URL>/` (still signed in as the non-owner Google account). Click "Show Chart" on the product card.

    Expected: PriceChart renders with TWO data points — the seed (Task 1) and the new one inserted by the cron run (Task 2 Step 3). Y-axis shows currency-formatted values. X-axis shows formatted dates.

    Take a screenshot showing the chart with two points. Save to `.planning/phases/07-polish-deployment/screenshots/dep-06-chart.png`.

    **Step 7 — Verify the new price_history row in prod SQL:**
    ```sql
    SELECT product_id, price, currency, checked_at
    FROM price_history
    WHERE product_id = '<PRODUCT_ID>'
    ORDER BY checked_at;
    ```
    Expected: exactly 2 rows, ordered ascending by `checked_at`. The newer row has `price = <SEED_PRICE>` (the real Firecrawl-scraped value).

    Then verify the products table now reflects the new price:
    ```sql
    SELECT current_price FROM products WHERE id = '<PRODUCT_ID>';
    ```
    Expected: `<SEED_PRICE>` (cron handler ran the INSERT-then-UPDATE order from Phase 6 D-04).

    **Step 8 — Re-fire cron POST to verify idempotency:**

    Without changing anything, run the same cURL again:
    ```bash
    curl -i -X POST "https://<PROD_VERCEL_URL>/api/cron/check-prices" \
      -H "Authorization: Bearer <PROD_CRON_SECRET>" \
      -H "Content-Type: application/json" \
      -d '{}'
    ```

    Expected response:
    - `HTTP/2 200`
    - Body: `{"status":"ok","scraped":1,"updated":0,"dropped":0,"failed":[]}` (Phase 6 D-02 idempotency — same-day re-run with unchanged price gates the price_history insert).

    Verify in SQL:
    ```sql
    SELECT count(*) FROM price_history WHERE product_id = '<PRODUCT_ID>';
    ```
    Expected: still 2 (no duplicate row from the re-fire).

    No new email in the non-owner inbox.

    **Step 9 — Append to 07-VERIFICATION.md:**

    ```markdown
    ### Steps 4-9 (force drop + cron + email + chart + idempotency)

    | # | Action | Expected | Observed |
    |---|--------|----------|----------|
    | 4 | `UPDATE products SET current_price = current_price * 2 WHERE id = '<PRODUCT_ID>'` | current_price = `<INFLATED_PRICE>` | PASS |
    | 5 | `curl -X POST .../api/cron/check-prices -H 'Authorization: Bearer <secret>'` | 200 + `{"scraped":1,"updated":1,"dropped":1,"failed":[]}` | PASS |
    | 6 | Email arrives in NON-OWNER Gmail inbox | Subject + image + name + old (struck) + new (prominent) + percent + CTA | PASS |
    | 6a | Email screenshot | saved | `screenshots/dep-06-email.png` |
    | 7 | Dashboard chart shows TWO data points after Show Chart | 2 points: seed + cron-inserted | PASS |
    | 7a | Chart screenshot | saved | `screenshots/dep-06-chart.png` |
    | 8 | `SELECT * FROM price_history WHERE product_id='<id>' ORDER BY checked_at` in prod SQL | 2 rows, second row price = `<SEED_PRICE>` | PASS |
    | 8a | `SELECT current_price FROM products WHERE id='<id>'` | `<SEED_PRICE>` (post-update) | PASS |
    | 9 | Re-fire cron POST without changing anything | 200 + `{"scraped":1,"updated":0,"dropped":0}` | PASS |
    | 9a | `SELECT count(*) FROM price_history WHERE product_id='<id>'` | still 2 (idempotency) | PASS |
    | 9b | No new email in non-owner inbox | confirmed | PASS |

    Email screenshot: ![DEP-06 price drop email](screenshots/dep-06-email.png)
    Chart screenshot: ![DEP-06 chart with 2 data points](screenshots/dep-06-chart.png)
    ```

    Step 10 — Clean up `.env.prod.tmp` if Option B was used:
    ```bash
    rm -f dealdrop/.env.prod.tmp
    ```
  </how-to-verify>
  <acceptance_criteria>
    - SQL UPDATE doubled current_price (verified)
    - First cron POST returns 200 + `dropped:1`
    - Email arrives in NON-OWNER Gmail inbox within 5 min, contains all required elements
    - Email screenshot saved at `.planning/phases/07-polish-deployment/screenshots/dep-06-email.png`
    - Dashboard chart shows TWO data points
    - Chart screenshot saved at `.planning/phases/07-polish-deployment/screenshots/dep-06-chart.png`
    - SQL: 2 price_history rows; products.current_price = `<SEED_PRICE>`
    - Re-fire cron POST returns 200 + `dropped:0`
    - SQL: still 2 price_history rows (no duplicate)
    - No new email on re-fire
    - 07-VERIFICATION.md DEP-06 Steps 4-9 table populated
    - `.env.prod.tmp` (if used) is deleted (no plaintext secret on disk)
  </acceptance_criteria>
  <resume-signal>Type "approved: dropped:1 → email → chart → idempotent" with a one-line summary. If email never arrives, type "deviation: email never delivered" and check Resend dashboard before resuming.</resume-signal>
  <verify>
    <automated>MISSING — checkpoint task; verification is operator-driven via <how-to-verify> + <acceptance_criteria>; no automated command applies</automated>
  </verify>
  <done>All <acceptance_criteria> rows satisfied; <resume-signal> typed by operator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Walk PITFALLS.md:336-348 — 10 inspection-grid line items as 10 PASS rows</name>
  <files>(no files modified directly — operator-driven; artifacts updated via <how-to-verify> steps)</files>
  <read_first>
    - .planning/research/PITFALLS.md (lines 336-348 — the inspection grid; lines 351-362 — Recovery Strategies; line 362 — Tailwind production verification)
    - .planning/phases/07-polish-deployment/07-CONTEXT.md (Specifics §"DEP-06 must use a non-owner inbox" — confirms PITFALLS:342 is the source of the non-owner-inbox rule)
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (DEP-06 Steps 1-9 already cover several of these line items — we explicitly map them here)
  </read_first>
  <action>Operator-driven checkpoint. Follow the numbered steps in <how-to-verify> verbatim. Each step has an explicit expected output / observed value. The acceptance_criteria block at the bottom of this task is the gate; do NOT mark this task done unless every acceptance row is satisfied.</action>
  <what-built>
    Task 2 verified the happy path. Task 3 walks the 10-item "Looks Done But Isn't" inspection grid from `PITFALLS.md:336-348`. Several items are already implicitly verified by Tasks 1-2 — this step makes each one EXPLICIT in 07-VERIFICATION.md so the audit is complete.
  </what-built>
  <how-to-verify>
    Walk each of the 10 line items and append to 07-VERIFICATION.md. Many items reference work already done in Plans 07-01..07-08; cite the existing evidence rather than re-running.

    **Line 338 — Cron endpoint POST works but verify GET does NOT trigger scraping (health check only):**
    Already verified in Task 1 pre-flight (curl GET returns `{"status":"ok"}` instantly without any Firecrawl invocation). Cite the pre-flight row.

    **Line 339 — RLS on products + verify price_history also has policies enforcing ownership chain:**
    Run in prod Supabase SQL Editor:
    ```sql
    SELECT schemaname, tablename, policyname, cmd, qual, with_check
    FROM pg_policies
    WHERE tablename IN ('products', 'price_history')
    ORDER BY tablename, cmd;
    ```
    Expected: at least 4 rows on `products` (SELECT/INSERT/UPDATE/DELETE) and at least 1 row on `price_history` (SELECT) with the ownership-chain qual `product_id IN (SELECT id FROM products WHERE user_id = auth.uid())`. Capture the table.

    **Line 340 — OAuth on prod URL registered in BOTH Google + Supabase:**
    Cite Plan 07-06 verification — Tasks 1+2+3 (Google redirect URIs + Supabase Site URL/Redirect URLs + fresh-account smoke test). Already in 07-VERIFICATION.md DEP-04 sections.

    **Line 341 — Firecrawl scrape failures handled on cron path (sets `last_scraped_status`/`last_scrape_failed_at`, doesn't write null to price_history):**
    Phase 6 verified this for dev. To re-confirm on prod:
    1. Add a SECOND product with a deliberately bad URL (e.g. `https://example.invalid/notreal-12345`).
    2. Wait for the SkeletonCard → ProductCard transition; expect a "tracking failed" toast or a card with the failed badge.
    3. Run in prod SQL:
       ```sql
       SELECT id, name, last_scrape_failed_at FROM products ORDER BY created_at DESC LIMIT 1;
       SELECT count(*) FROM price_history WHERE product_id = (SELECT id FROM products ORDER BY created_at DESC LIMIT 1);
       ```
       Expected: `last_scrape_failed_at` is NOT NULL OR the product was rejected at insert time (TRACK-05 contract); price_history count = 0 for that product. Either way, no null `price` row.

    OPTIONAL — re-fire the cron with this bad-URL product still in place: `dropped:0`, `failed:[<id>]` in the response. The badge stays. Other products continue to run (CRON-09).

    **Line 342 — Email arrives in NON-OWNER inbox (the To: is different from the Resend account email):**
    Already verified in Task 2 Step 6. Cite that row + the screenshot.

    **Line 343 — PriceChart handles single (no line) and zero (empty) data points without crashing:**
    Already verified by Phase 5 Plan 05-02 unit tests (PriceChart empty-state guard) + Plan 07-04 mobile audit (which walks the chart toggle). To re-confirm on prod:
    - The product added in Task 1 had 1 price_history point before Task 2's cron run; Task 1 Step 2 confirmed Show Chart rendered without crashing at 1 point.
    - For 0 data points: the dashboard's optimistic SkeletonCard slot covers this state; no separate test needed.

    Cite Plan 07-04 audit table + Phase 5 PriceChart.test.tsx coverage.

    **Line 344 — Non-USD product formats without RangeError:**
    The Task 1 product is in GBP (books.toscrape.com is a UK site). Task 1 already confirmed the ProductCard shows the £ currency symbol without a JS console error — cite that. ALSO open the prod page browser console (DevTools → Console) and verify zero "RangeError" entries.

    OPTIONAL — to be more thorough, add a third product from `amazon.in` or `amazon.co.uk` (INR / GBP) and re-confirm. Bracket-matching: any non-USD currency triggers the same `Intl.NumberFormat` codepath in `formatCurrency` (resend.ts) and the `Intl.NumberFormat` in ProductCard. If the GBP from books.toscrape worked, an INR / EUR / JPY product should too — Phase 4 unit tests validated this. For portfolio bar, the GBP confirmation is sufficient.

    **Line 345 — `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` set in Vercel production:**
    Already verified in Plan 07-05 Task 2 — `vercel env ls production` showed both. Re-confirm by inspecting the rendered HTML in prod:
    ```bash
    curl -s https://<PROD_VERCEL_URL>/ | grep -oE '<PROD_REF>\.supabase\.co' | head -1
    ```
    Expected: matches `<PROD_REF>` (the prod ref, NOT the dev ref `vhlbdcsxccaknccawfdj`). Confirms the public env vars are baked into the client bundle correctly.

    **Line 346 — `maxDuration` works with 15+ products:**
    Optional / time-permitting — the portfolio test is a single-product happy path. To exercise: add 15 throwaway URLs (mix of books.toscrape.com paths). Trigger cron POST. Watch Vercel function logs; the response should arrive within 60-300s. If portfolio time is constrained, document this as "deferred — single-product DEP-06 happy path verified; 15+ scale not exercised; Fluid Compute confirmed enabled in Plan 07-05 (the relevant infra prerequisite)" and PASS the row with the deferral note.

    **Line 347 — Cascade delete: removing a product also deletes its price_history rows (FK cascade):**
    1. On the prod dashboard, click "Remove" on the product from Task 1, confirm the AlertDialog.
    2. Verify in prod SQL:
       ```sql
       SELECT count(*) FROM products WHERE id = '<PRODUCT_ID>';        -- expect 0
       SELECT count(*) FROM price_history WHERE product_id = '<PRODUCT_ID>'; -- expect 0
       ```

    Note: this destroys the test data from Tasks 1-2 — only run AFTER the email and chart screenshots are saved (Task 2 Step 5 + Step 6).

    **Line 362 — Tailwind classes apply in production (R-07):**
    Spot-check the rendered CSS in prod:
    1. Open https://<PROD_VERCEL_URL>/ in browser.
    2. Right-click → Inspect on the Hero h1. Confirm computed font-size is the expected mobile-first value (e.g. text-3xl ≈ 30px, scales to text-5xl ≈ 48px at sm:).
    3. If utility classes are NOT applying: check `dealdrop/app/globals.css` still has `@import "tailwindcss"` (PITFALLS:362) and that no stray `tailwind.config.js` was committed.

    **Step Final — Append to 07-VERIFICATION.md:**

    ```markdown
    ### PITFALLS.md "Looks Done But Isn't" Inspection Grid

    | # | Pitfall (line) | Verification | Status | Evidence |
    |---|----------------|--------------|--------|----------|
    | 1 | Cron GET ≠ scraping (line 338) | Pre-flight: GET returns `{"status":"ok"}` instantly, no Firecrawl invocation | PASS | Task 1 pre-flight row |
    | 2 | RLS on price_history (line 339) | `SELECT * FROM pg_policies WHERE tablename IN ('products','price_history')` shows ownership-chain qual on price_history | PASS | <SQL output paste> |
    | 3 | OAuth on prod URL in BOTH Google + Supabase (line 340) | Plan 07-06 Tasks 1-3: Google redirect URIs + Supabase Site URL/Redirect URLs + fresh-account smoke test | PASS | DEP-04 sections of 07-VERIFICATION.md |
    | 4 | Cron handles scrape failures gracefully (line 341) | Bad-URL product: last_scrape_failed_at set, no null price_history row, run continues | PASS | <SQL output paste> |
    | 5 | Email arrives in NON-OWNER inbox (line 342) | Task 2 Step 6: email landed in fresh non-owner Gmail | PASS | screenshots/dep-06-email.png |
    | 6 | PriceChart at 1 + 0 data points (line 343) | Task 1 Step 2 (1-point chart rendered) + Phase 5 Plan 05-02 unit tests | PASS | Plan 07-04 audit + 05-02 SUMMARY |
    | 7 | Non-USD currency (line 344) | books.toscrape.com (GBP) product rendered without RangeError; prod console clean | PASS | Task 1 + browser console |
    | 8 | NEXT_PUBLIC_* env in Vercel prod (line 345) | `vercel env ls production` (Plan 07-05) + `curl prod | grep <PROD_REF>.supabase.co` | PASS | <curl output paste> |
    | 9 | maxDuration with 15+ products (line 346) | Single-product happy path verified; Fluid Compute confirmed ON in Plan 07-05; 15+ scale deferred for portfolio bar | PASS-deferred | 07-05 Fluid Compute confirmation |
    | 10 | Cascade delete (line 347) | Remove → AlertDialog → confirm → products + price_history both 0 rows | PASS | <SQL output paste> |

    ### Tailwind Production Verification (PITFALLS:362)

    | Check | Expected | Observed |
    |-------|----------|----------|
    | `@import "tailwindcss"` in globals.css | present | PASS |
    | No conflicting `tailwind.config.js` | not present | PASS |
    | Hero `text-3xl` rendered at expected ~30px on prod | yes | PASS |
    ```

    **Step Frontmatter — flip status to passed:**

    Update the frontmatter at the top of `07-VERIFICATION.md`:
    ```yaml
    ---
    phase: 07-polish-deployment
    verified: <ISO timestamp>
    status: passed
    score: <X>/<Y> requirements verified
    overrides_applied: 1   # Plan 07-01: reset → unstable_retry per installed Next.js 16.2 docs
    ---
    ```

    Where `<X>` is the count of requirements verified (POL-01..06 + DEP-01..06 = 12) and `<Y>` is the count of phase requirements (12). If any row was deferred or partial, document in the score (e.g. `12/12 with PASS-deferred on PITFALLS line 346`).
  </how-to-verify>
  <acceptance_criteria>
    - All 10 PITFALLS.md:336-348 line items have a row in 07-VERIFICATION.md inspection grid
    - Each row's Status is PASS or PASS-deferred (with deferral noted)
    - SQL outputs / curl outputs / file paths cited as evidence per row
    - Tailwind production check appended (3 sub-rows PASS)
    - Frontmatter `status: passed` set
    - Frontmatter `score: 12/12` (or `12/12 with PASS-deferred on line 346`) set
    - `overrides_applied: 1` documented (the unstable_retry override from Plan 07-01)
    - Cascade delete cleaned up the test product (so prod is empty for portfolio screenshots later)
  </acceptance_criteria>
  <resume-signal>Type "approved: 10/10 PITFALLS rows PASS, status passed" once all rows are populated and frontmatter is updated. If any row CANNOT pass, type "deviation: PITFALLS line N: &lt;reason&gt;" and STOP — that line is a Phase 7 gap that needs a follow-up plan.</resume-signal>
  <verify>
    <automated>MISSING — checkpoint task; verification is operator-driven via <how-to-verify> + <acceptance_criteria>; no automated command applies</automated>
  </verify>
  <done>All <acceptance_criteria> rows satisfied; <resume-signal> typed by operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → CRON_SECRET retrieval | Operator copies plaintext secret from Vercel env or Vault for the manual cURL test; risk window is the local terminal session. `.env.prod.tmp` (if Option B) is gitignored and deleted post-use. |
| Operator → screenshot artifacts | Email + chart screenshots committed to repo; ensure they don't accidentally include the operator's actual Gmail address or other sensitive data. |
| Prod stack end-to-end | This task exercises every prior boundary: OAuth (Phase 2), scrape (Phase 3), data tables + RLS (Phase 1), price_history wiring (Phase 4), chart (Phase 5), cron + email (Phase 6) — under prod env vars and prod Supabase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-21 | Information Disclosure | Plaintext CRON_SECRET on operator's disk via `.env.prod.tmp` | mitigate | Task 2 Step 2 Option B explicitly lists `rm -f .env.prod.tmp` as Step 10. `.env*` is gitignored. Operator instruction repeats the cleanup. |
| T-07-22 | Information Disclosure | Sensitive data leaked in committed screenshots | mitigate | Task 2 Step 5 instructs operator to blur the recipient Gmail address; screenshots are visual proof of email arrival, not a forensic exhibit. Reviewer can confirm fields without seeing the actual email address. |
| T-07-23 | Denial of Service | Cron POST exposes the prod URL + endpoint pattern | accept | The endpoint is already public (DEP-01 health check is unauthenticated); the secret-protected POST is documented in REQUIREMENTS.md. No new attack surface. |
| T-07-24 | Tampering / Replay | Re-fired cron POST in Task 2 Step 8 should NOT cause duplicate price_history rows | mitigate | Phase 6 D-02 idempotency: price-change gate rejects unchanged-price scrapes. Acceptance criteria Step 8 explicitly verifies count(*) is still 2. |
| T-07-25 | Spoofing / Verification gap | Resend account-owner inbox silently succeeding when DNS is broken | mitigate | PITFALLS:342 / R-09 — non-owner inbox is mandatory. Task 2 Step 4 is the explicit verification; Task 1 Step 1 explicitly records the fresh-non-owner Google account is DIFFERENT from the Resend owner email. |
</threat_model>

<verification>
- All Task 1, 2, 3 acceptance criteria met
- 07-VERIFICATION.md frontmatter `status: passed` with `score: 12/12` (or `12/12 with PASS-deferred on line 346`)
- Two screenshots present at the documented paths
- All 10 PITFALLS.md:336-348 line items have a corresponding PASS / PASS-deferred row
- Cascade delete confirmed (Task 3 line 347)
- Tailwind production check confirmed (PITFALLS:362)
- `.env.prod.tmp` does not exist (cleanup honored)
- The fresh non-owner Gmail email value never appears in committed files (operator blurs in screenshot, never pastes verbatim)
</verification>

<success_criteria>
- DEP-01 closed: prod URL responds 200 (re-confirmed at phase close)
- DEP-06 closed: end-to-end happy path verified with email + chart screenshots committed
- All 10 PITFALLS.md:336-348 line items: PASS or PASS-deferred (with deferral documented)
- Tailwind production verification: PASS (PITFALLS:362)
- 07-VERIFICATION.md `status: passed`, `score: 12/12`
- `overrides_applied: 1` documented (unstable_retry vs CONTEXT D-02)
- Phase 7 phase gate green
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deployment/07-08-SUMMARY.md` capturing:
- DEP-06 procedure: 9 numbered steps with concrete observed values (response bodies, SQL counts, screenshot paths)
- PITFALLS.md grid: 10 PASS rows
- Tailwind production verification (3 sub-rows)
- Final 07-VERIFICATION.md frontmatter (status: passed, score: 12/12)
- Confirmation of cleanup steps (`.env.prod.tmp` removed, cascade delete cleared test data)
- Confirmation of `overrides_applied: 1` documentation (unstable_retry vs CONTEXT D-02)
- Total Phase 7 close-out: 12/12 requirements verified across 8 plans
</output>
