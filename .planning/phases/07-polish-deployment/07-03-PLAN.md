---
phase: 07-polish-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-polish-deployment/07-VERIFICATION.md
autonomous: false
requirements: [POL-01, POL-02, POL-05]
must_haves:
  truths:
    - "POL-01 (Sonner Toaster mounted in root layout) verified shipped from Phase 2 — appended as VERIFIED row in 07-VERIFICATION.md"
    - "POL-02 (SkeletonCard renders in optimistic-pending slot during add-product submission) verified working — appended as VERIFIED row"
    - "POL-05 (DealDrop title + description metadata in app/layout.tsx) verified working — appended as VERIFIED row"
    - "Phase 7 does NOT re-implement Sonner, the optimistic slot, or the metadata"
  artifacts:
    - path: .planning/phases/07-polish-deployment/07-VERIFICATION.md
      provides: phase verification document with POL-01/02/05 verified rows
      contains: "POL-01"
      contains_2: "POL-02"
      contains_3: "POL-05"
  key_links:
    - from: dealdrop/app/layout.tsx
      to: "<Toaster /> + title/description metadata"
      via: existing imports + exports unchanged
      pattern: "<Toaster.*position"
    - from: dealdrop/src/components/dashboard/ProductGrid.tsx
      to: SkeletonCard via useOptimistic slot
      via: existing wiring from Phase 4 Plan 04-07
      pattern: "useOptimistic"
---

<objective>
Verify the three already-shipped polish items (POL-01 Sonner, POL-02 loading-skeleton, POL-05 metadata) and record findings in `07-VERIFICATION.md`. **No code changes** — all three were closed in earlier phases (POL-01 in Phase 2 D-13, POL-02 in Phase 4 Plan 04-07, POL-05 in Phase 1 Plan 01-01 / FND-08). This plan exists so the verification is auditable instead of implicit.

Purpose: Closes the three "already shipped, confirm" requirements without re-touching shipped code (which would be a regression risk).

Output: `07-VERIFICATION.md` seeded with three VERIFIED rows + a manual-UAT note for POL-02. Closes POL-01, POL-02, POL-05.

Hard rule (per RESEARCH.md "Out of Scope (Reaffirmed)"): if any task in this plan touches `dealdrop/app/layout.tsx` or installs Sonner, that's a planning bug — STOP and surface a deviation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-polish-deployment/07-CONTEXT.md
@.planning/phases/07-polish-deployment/07-RESEARCH.md
@.planning/phases/07-polish-deployment/07-VALIDATION.md
@.planning/phases/06-automated-monitoring-email-alerts/06-VERIFICATION.md
@dealdrop/app/layout.tsx
@dealdrop/src/components/dashboard/ProductGrid.tsx
@dealdrop/src/components/dashboard/SkeletonCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify POL-01 + POL-05 via static greps and record VERIFIED rows</name>
  <read_first>
    - dealdrop/app/layout.tsx (POL-01 mount target line ~41; POL-05 metadata target lines 19-22)
    - .planning/phases/07-polish-deployment/07-VALIDATION.md (the per-task verification map, lines 42, 48 — exact grep commands)
    - .planning/phases/06-automated-monitoring-email-alerts/06-VERIFICATION.md (format precedent for verification documents — frontmatter, Goal Achievement table, Key Link Verification table)
    - .planning/REQUIREMENTS.md (lines 224, 228 — POL-01 traceability footnote moving it to Phase 2; POL-05 acceptance)
  </read_first>
  <action>
    Step A — Run the POL-01 grep:

    ```bash
    cd dealdrop && grep -n '<Toaster' app/layout.tsx
    ```

    Expected: matches at line 41 (`<Toaster position="top-center" richColors />`). Record the matching line.

    Step B — Run the POL-05 greps:

    ```bash
    cd dealdrop && grep -n 'DealDrop — Universal Price Tracker' app/layout.tsx
    cd dealdrop && grep -n 'Track products from any e-commerce site' app/layout.tsx
    ```

    Expected: title at line 20, description at line 21. Record both matching lines.

    Step C — Create `.planning/phases/07-polish-deployment/07-VERIFICATION.md` with this content (mirror the Phase 6 format):

    ```markdown
    ---
    phase: 07-polish-deployment
    verified: <ISO timestamp>
    status: in_progress
    score: pending
    ---

    # Phase 7: Polish & Deployment — Verification Report

    **Phase Goal:** DealDrop is deployed to Vercel production, looks professional on mobile and desktop, handles errors gracefully, and passes an end-to-end manual test of the full sign-up → add product → price-drop alert flow.
    **Verified:** in progress (rolling)
    **Status:** in_progress

    ## Requirement Verification

    | Req | Behavior | Status | Evidence |
    |-----|----------|--------|----------|
    | POL-01 | Sonner Toaster mounted in root layout | VERIFIED (Phase 2 D-13) | `dealdrop/app/layout.tsx:41` — `<Toaster position="top-center" richColors />` (grep confirms). Phase 7 did NOT modify `app/layout.tsx`. |
    | POL-02 | Loading skeleton during add-product submission | VERIFIED (Phase 4 Plan 04-07) | `dealdrop/src/components/dashboard/ProductGrid.tsx` uses `useOptimistic`; `SkeletonCard` renders in the optimistic-pending slot while a new product is being added. Manual UAT confirms (Task 2 below). |
    | POL-05 | Metadata title + description reflect DealDrop | VERIFIED | `dealdrop/app/layout.tsx:20` — `title: "DealDrop — Universal Price Tracker"`. Line 21 — `description: "Track products from any e-commerce site. Get email alerts the moment the price drops."` (grep confirms both). No "Create Next App" placeholders. |

    (POL-03, POL-04, POL-06, DEP-01..06 to be appended by their respective plans.)
    ```

    Replace `<ISO timestamp>` with the current UTC time in `YYYY-MM-DDTHH:MM:SSZ` format (use `date -u +%Y-%m-%dT%H:%M:%SZ`).

    Important guard rails:
    - Do NOT modify `dealdrop/app/layout.tsx` in this plan.
    - Do NOT install or remove any npm package.
    - Do NOT create or modify a `Toaster` mount.
  </action>
  <verify>
    <automated># Run from repo root. App-file greps from inside dealdrop/, VERIFICATION.md checks from repo root.
cd dealdrop &amp;&amp; grep -q '&lt;Toaster' app/layout.tsx &amp;&amp; grep -q 'DealDrop — Universal Price Tracker' app/layout.tsx &amp;&amp; grep -q 'Track products from any e-commerce site' app/layout.tsx &amp;&amp; cd .. &amp;&amp; test -f .planning/phases/07-polish-deployment/07-VERIFICATION.md &amp;&amp; grep -q 'POL-01' .planning/phases/07-polish-deployment/07-VERIFICATION.md &amp;&amp; grep -q 'POL-05' .planning/phases/07-polish-deployment/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `dealdrop/app/layout.tsx` UNCHANGED (no diff vs HEAD for this file)
    - `.planning/phases/07-polish-deployment/07-VERIFICATION.md` exists
    - VERIFICATION.md contains a `POL-01` row marked VERIFIED with file:line citation
    - VERIFICATION.md contains a `POL-05` row marked VERIFIED with file:line citation
    - VERIFICATION.md contains a `POL-02` row marked VERIFIED (Task 2 will append the manual UAT evidence)
    - All three grep commands succeed
  </acceptance_criteria>
  <done>
    POL-01 + POL-05 captured as VERIFIED rows in 07-VERIFICATION.md with grep evidence; layout.tsx untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT for POL-02 (SkeletonCard renders during add-product submission)</name>
  <files>(no files modified directly — operator-driven; artifacts updated via <how-to-verify> steps)</files>
  <read_first>
    - dealdrop/src/components/dashboard/ProductGrid.tsx (the `useOptimistic` slot wiring at lines ~95-110)
    - dealdrop/src/components/dashboard/SkeletonCard.tsx (the placeholder being rendered)
    - dealdrop/src/components/dashboard/AddProductDialog.tsx (the form opening dialog)
    - .planning/phases/07-polish-deployment/07-VALIDATION.md (line 43 — the manual UAT instruction)
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (the file Task 1 created)
  </read_first>
  <action>Operator-driven checkpoint. Follow the numbered steps in <how-to-verify> verbatim. Each step has an explicit expected output / observed value. The acceptance_criteria block at the bottom of this task is the gate; do NOT mark this task done unless every acceptance row is satisfied.</action>
  <what-built>
    POL-02 was wired in Phase 4 Plan 04-07 — `ProductGrid` uses `useOptimistic` to render a `SkeletonCard` in the new-product slot while the `addProduct` Server Action is in flight. No new code. This task is a visual confirmation that the existing wiring still works.
  </what-built>
  <how-to-verify>
    1. Run the dev server:
       ```bash
       cd dealdrop && npm run dev
       ```
    2. Open `http://localhost:3000` in a browser. Sign in with a Google account if prompted (already-registered dev OAuth from Phase 2).
    3. Click "Add product" to open the `AddProductDialog`.
    4. Paste a product URL (e.g. `https://books.toscrape.com/catalogue/a-light-in-the-attic_1000/index.html` — the Phase 3 known-good test target).
    5. Click the Track button.
    6. **OBSERVE: a SkeletonCard placeholder (greyish pulsing card) appears in the dashboard grid while the scrape is in flight (~3-10 seconds), then is replaced by the real ProductCard with name + price + image.**
    7. If the SkeletonCard does NOT appear (the slot stays empty until the ProductCard pops in, OR the SkeletonCard never disappears), file a deviation and STOP — POL-02 is broken; this becomes a follow-up bugfix plan.
    8. After confirming, append a row to `.planning/phases/07-polish-deployment/07-VERIFICATION.md` immediately under the existing POL-02 row:

       ```markdown
       ## POL-02 Manual UAT

       | Step | Action | Observed | Expected | Status |
       |------|--------|----------|----------|--------|
       | 1 | Open AddProductDialog, paste books.toscrape.com URL, click Track | SkeletonCard appears in grid while scrape in flight | SkeletonCard appears | PASS |
       | 2 | Wait for scrape to complete | SkeletonCard replaced by real ProductCard with name + price + image | Replaced cleanly | PASS |

       **Date:** <YYYY-MM-DD>
       **Operator:** <user name or "operator">
       ```

    9. Stop the dev server (Ctrl+C).
  </how-to-verify>
  <acceptance_criteria>
    - SkeletonCard observed in dashboard during scrape
    - SkeletonCard replaced by real ProductCard after scrape completes
    - VERIFICATION.md updated with POL-02 manual UAT table
    - No code changes made to `ProductGrid.tsx`, `SkeletonCard.tsx`, or `AddProductDialog.tsx`
  </acceptance_criteria>
  <resume-signal>Type "approved" once SkeletonCard observed and POL-02 row appended to 07-VERIFICATION.md. If broken, type "deviation: POL-02 broken" and stop.</resume-signal>
  <verify>
    <automated>MISSING — checkpoint task; verification is operator-driven via <how-to-verify> + <acceptance_criteria>; no automated command applies</automated>
  </verify>
  <done>All <acceptance_criteria> rows satisfied; <resume-signal> typed by operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries — Phase 7 Plan 03 is verification-only. POL-01/02/05 trust boundaries were established in Phases 2 and 4 and remain unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | DoS via accidental regression | dealdrop/app/layout.tsx | accept | Plan 07-03 explicitly does not modify layout.tsx; static grep verifies the existing mount + metadata. Acceptance criteria asserts file unchanged. |
</threat_model>

<verification>
- `cd dealdrop && grep -q '<Toaster' app/layout.tsx` (POL-01)
- `cd dealdrop && grep -q 'DealDrop — Universal Price Tracker' app/layout.tsx` (POL-05 title)
- `cd dealdrop && grep -q 'Track products from any e-commerce site' app/layout.tsx` (POL-05 description)
- `test -f .planning/phases/07-polish-deployment/07-VERIFICATION.md` (artifact created)
- `git diff dealdrop/app/layout.tsx` returns empty (no modifications)
- POL-02 manual UAT row present in 07-VERIFICATION.md
</verification>

<success_criteria>
- 07-VERIFICATION.md created with frontmatter + POL-01/02/05 VERIFIED rows
- POL-02 manual UAT confirmed visually (SkeletonCard during add)
- Zero code changes (no installs, no edits to layout.tsx, ProductGrid.tsx, SkeletonCard.tsx)
- POL-01, POL-02, POL-05 closed
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deployment/07-03-SUMMARY.md` capturing:
- The three grep results (POL-01 line, POL-05 title line, POL-05 description line)
- The POL-02 manual UAT outcome
- Confirmation of zero code modifications
- 07-VERIFICATION.md contents added by this plan
</output>
