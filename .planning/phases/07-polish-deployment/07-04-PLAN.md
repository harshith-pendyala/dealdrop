---
phase: 07-polish-deployment
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-03]
files_modified:
  - dealdrop/src/components/hero/Hero.tsx
  - dealdrop/src/components/header/Header.tsx
  - dealdrop/src/components/dashboard/DashboardShell.tsx
  - dealdrop/src/components/dashboard/ProductGrid.tsx
  - dealdrop/src/components/dashboard/ProductCard.tsx
  - dealdrop/src/components/dashboard/AddProductDialog.tsx
  - dealdrop/src/components/auth/AuthModal.tsx
  - .planning/phases/07-polish-deployment/07-VERIFICATION.md
autonomous: false
requirements: [POL-04]
must_haves:
  truths:
    - "A second top-to-bottom audit walk at 320px produces ZERO new visible breaks (component pass)"
    - "Every break observed during the first walk is recorded in 07-VERIFICATION.md as a 'viewport / observed break / fix shipped' row"
    - "Each fix is a single Tailwind class change in the established sm:/md:/lg: vocabulary — no new breakpoints, no container queries, no @theme blocks"
    - "Total breaks at 320px <= 6 (deviation flag fires above 6 per CONTEXT.md D-05)"
  artifacts:
    - path: .planning/phases/07-polish-deployment/07-VERIFICATION.md
      provides: POL-04 audit table appended (viewport / component / observed / fix rows)
      contains: "POL-04 Mobile Audit"
    - path: "dealdrop/src/components/{hero,header,dashboard,auth}/*.tsx"
      provides: empirical Tailwind class tweaks (set discovered during audit)
      note: "files_modified set above is the SUPERSET of files possibly touched; the actual set is empirical"
  key_links:
    - from: VERIFICATION.md audit table
      to: each modified component
      via: row "viewport / observed break / fix shipped"
      pattern: "320|375|768"
---

<objective>
Run the POL-04 mobile audit per CONTEXT.md D-05: walk hero → header Sign In → AuthModal → DashboardShell empty state → AddProductDialog → ProductCard → Show Chart toggle → RemoveProductDialog at 320 / 375 / 768 / desktop, fix each visible break with a single Tailwind class tweak in the established responsive vocabulary, and document every break + fix as a row in 07-VERIFICATION.md. Includes the new error.tsx fallback (Plan 07-01 must ship first so it's part of the walk).

Purpose: The portfolio reviewer browses on a phone at 320px without overflow, illegible text, or stacked-when-it-shouldn't-stack layout.

Output: An empirical set of file modifications (within the listed `files_modified` superset) + an audit table in 07-VERIFICATION.md.

Hard rules (from CONTEXT.md D-05 + RESEARCH.md R-11):
- Single Tailwind tweak per break (default budget).
- More than ~6 distinct breaks at 320px = STOP and flag a deviation to the user before tweaking.
- Use existing prefixes only: `sm:` (640px), `md:` (768px), `lg:` (1024px).
- NO new abstractions. NO container queries (`@container`). NO `@theme` blocks. NO Playwright. NO viewport-snapshot tests.
- Audit ENDS when a full top-to-bottom walk produces zero new breaks at 320px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-polish-deployment/07-CONTEXT.md
@.planning/phases/07-polish-deployment/07-RESEARCH.md
@.planning/phases/07-polish-deployment/07-PATTERNS.md
@.planning/phases/07-polish-deployment/07-VERIFICATION.md
@.planning/research/PITFALLS.md
@dealdrop/app/layout.tsx
@dealdrop/app/error.tsx
@dealdrop/src/components/hero/Hero.tsx
@dealdrop/src/components/header/Header.tsx
@dealdrop/src/components/dashboard/DashboardShell.tsx
@dealdrop/src/components/dashboard/ProductGrid.tsx
@dealdrop/src/components/dashboard/ProductCard.tsx
@dealdrop/src/components/dashboard/AddProductDialog.tsx
@dealdrop/src/components/dashboard/RemoveProductDialog.tsx
@dealdrop/src/components/dashboard/EmptyState.tsx
@dealdrop/src/components/auth/AuthModal.tsx

<interfaces>
<!-- Established responsive vocabulary already used in the codebase. Do NOT introduce new prefixes. -->

| File | Existing pattern | Locked vocabulary |
|------|------------------|-------------------|
| Hero.tsx:6 | `px-4 sm:px-6 lg:px-8 pt-12 sm:pt-16 pb-12 sm:pb-16` | mobile-first padding scale |
| Hero.tsx:7 | `text-3xl sm:text-5xl` | mobile-first typography scale |
| Hero.tsx:14 | `grid-cols-1 sm:grid-cols-3 gap-4 sm:gap-6` | mobile-first grid columns |
| Header.tsx:12 | `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 ...` | container + responsive horizontal padding |
| DashboardShell.tsx:12 | `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-16` | matches Header container width |
| ProductGrid.tsx:98 | `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6` | 3-tier responsive grid |
| AuthModal.tsx:40 / AddProductDialog.tsx:26 | `<DialogContent className="sm:max-w-md">` | dialog max-width mobile-first |

Common fix patterns (apply only if observed — RESEARCH §Pattern 6):

| Symptom | Tailwind fix |
|---------|--------------|
| Header overflow at 320px | `flex-wrap` on inner div + `truncate` on logo span |
| ProductCard image squashed in flex | replace `min-w-*` with `min-w-0` on flex children |
| AddProductDialog body cut off at 320px | `max-w-[90vw] sm:max-w-md` (extend existing `sm:max-w-md`) |
| ProductCard footer buttons wrap badly | `flex-wrap gap-2` |
| Long URL forces horizontal scroll | `truncate` + `min-w-0` on flex parent + `break-words` on text |
| Show Chart label too long at 320px | `text-xs sm:text-sm` (mobile-down scale) |
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: First audit walk (320 / 375 / 768) — record breaks; do NOT tweak yet</name>
  <files>(no files modified directly — operator-driven; artifacts updated via <how-to-verify> steps)</files>
  <read_first>
    - .planning/phases/07-polish-deployment/07-CONTEXT.md (D-05 — fix-as-found audit, NO opportunistic refactor)
    - .planning/phases/07-polish-deployment/07-RESEARCH.md (Pattern 6 — DevTools workflow, common Tailwind v4 break-on-narrow patterns)
    - .planning/phases/07-polish-deployment/07-PATTERNS.md (Established responsive vocabulary section, lines 528-562)
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (the file to append the audit table to)
  </read_first>
  <action>Operator-driven checkpoint. Follow the numbered steps in <how-to-verify> verbatim. Each step has an explicit expected output / observed value. The acceptance_criteria block at the bottom of this task is the gate; do NOT mark this task done unless every acceptance row is satisfied.</action>
  <what-built>
    Plan 07-01 just shipped error.tsx + global-error.tsx. The full app surface — Hero, Header, AuthModal, DashboardShell, EmptyState, AddProductDialog, ProductCard, PriceChart toggle, RemoveProductDialog, AND the new error fallback — is now ready for a single end-to-end mobile walk.
  </what-built>
  <how-to-verify>
    Step A — Start dev server:
    ```bash
    cd dealdrop && npm run dev
    ```

    Step B — Open Chrome DevTools (Cmd+Opt+I on macOS), toggle device toolbar (Cmd+Shift+M). Set viewport to:
    1. **320×568** (iPhone SE / smallest portfolio target — REQUIREMENTS.md HERO-05).
    2. **375×812** (iPhone 12).
    3. **768×1024** (iPad portrait).
    4. Desktop (no constraint; Tailwind `lg:` is 1024px+).

    Step C — At each viewport (320 first), walk this exact path and note any visible break:

    1. Open `http://localhost:3000/` (logged-out state — Hero + Header).
    2. Click "Sign In" → AuthModal opens.
    3. Close AuthModal (Escape or click outside). Sign in with Google (or skip if already signed-in dev session).
    4. Logged-in dashboard with NO products → EmptyState renders.
    5. Click "Add product" → AddProductDialog opens.
    6. Paste a URL, submit → SkeletonCard appears, then ProductCard renders.
    7. Click "Show Chart" on ProductCard → PriceChart expands inline.
    8. Click "Show Chart" again → chart hides.
    9. Click "Remove" → RemoveProductDialog (AlertDialog) opens. Cancel.
    10. Force the error fallback: temporarily inject `if (typeof window !== 'undefined' && window.location.search.includes('boom')) throw new Error('test')` in `dealdrop/src/components/dashboard/ProductGrid.tsx` (revert in Task 3 — DO NOT COMMIT). Visit `/?boom`. Observe `app/error.tsx` Card fallback. Click "Try again" + "Go home". Revert the throw.

    Step D — For each visible break, capture:
    - Viewport (320 / 375 / 768)
    - Component (e.g. "Header", "ProductCard")
    - Observed break (e.g. "Sign In button overflows logo at 320")
    - Proposed Tailwind fix (single class tweak in established vocabulary — pick from the table in <interfaces>)

    DO NOT make any edits in this task — just collect observations.

    Step E — If the count of distinct breaks at 320px is **greater than 6**, STOP. Surface a deviation flag with the list of all breaks. The user may approve a focused refactor of the offending component. Do NOT autonomously refactor.

    Step F — Append a draft audit table to `.planning/phases/07-polish-deployment/07-VERIFICATION.md`:

    ```markdown
    ## POL-04 Mobile Audit

    **First walk:** <YYYY-MM-DD>
    **Operator:** <user>

    | Viewport | Component | Observed break | Proposed fix |
    |----------|-----------|----------------|--------------|
    | 320px | Header | <e.g. Sign In button text overflows logo> | <e.g. `flex-wrap` on header inner div + `truncate` on logo> |
    | ... | ... | ... | ... |

    Total breaks at 320px: <N> (limit: 6)
    ```

    Step G — Stop dev server (Ctrl+C).
  </how-to-verify>
  <acceptance_criteria>
    - Audit table appended to 07-VERIFICATION.md with at least the column headers
    - Each break has a viewport, component, observed-break, proposed-fix entry
    - Total break count at 320px <= 6 (or deviation flagged and STOPPED)
    - Zero code modifications made to source files in this task
    - The temporary `throw` for error.tsx test is REVERTED in `ProductGrid.tsx` before stopping the dev server
  </acceptance_criteria>
  <resume-signal>Type "approved" with the break count and proposed-fix list to proceed to Task 2 (apply fixes). If &gt;6 breaks at 320px, type "deviation: N breaks" and STOP.</resume-signal>
  <verify>
    <automated>MISSING — checkpoint task; verification is operator-driven via <how-to-verify> + <acceptance_criteria>; no automated command applies</automated>
  </verify>
  <done>All <acceptance_criteria> rows satisfied; <resume-signal> typed by operator.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Tailwind class tweaks (one per recorded break)</name>
  <read_first>
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (the audit table from Task 1 — the SOURCE OF TRUTH for what to change)
    - .planning/phases/07-polish-deployment/07-PATTERNS.md (lines 528-562 — locked vocabulary; lines 547-555 — common-fix table)
    - The specific component file(s) listed in the audit table's "Component" column
  </read_first>
  <action>
    For EACH row in the audit table from Task 1, apply the proposed Tailwind class tweak.

    Rules:
    - Single class tweak per break (one or two utility classes, not a refactor).
    - Use existing prefixes only: `sm:` (640px), `md:` (768px), `lg:` (1024px).
    - NO new abstractions, NO container queries (`@container`), NO `@theme` blocks.
    - Each fix is independent — apply, save, no batching.
    - After EACH file edit, run `cd dealdrop && npm run lint` to catch syntax errors immediately.

    Example fix application (illustrative — actual fixes come from Task 1's table):

    File: `dealdrop/src/components/header/Header.tsx`
    Diff (header inner div):
    ```diff
    -<div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-full">
    +<div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-wrap items-center justify-between h-full gap-2">
    ```

    File: `dealdrop/src/components/dashboard/AddProductDialog.tsx`
    Diff (DialogContent):
    ```diff
    -<DialogContent className="sm:max-w-md">
    +<DialogContent className="max-w-[90vw] sm:max-w-md">
    ```

    After all fixes are applied:
    ```bash
    cd dealdrop && npm run lint && npm run build
    ```

    Both must pass. If `npm run build` fails because of a Tailwind class typo, fix the typo (do NOT introduce new vocabulary to "work around" the failure — that's a sign the proposed fix from Task 1 was wrong).

    Update the audit table in 07-VERIFICATION.md by changing the column header from "Proposed fix" to "Fix shipped" (and update each row's content if the actual fix differed from the proposal):

    ```markdown
    | Viewport | Component | Observed break | Fix shipped |
    |----------|-----------|----------------|-------------|
    | 320px | Header | Sign In button text overflows logo | `flex-wrap` + `gap-2` on inner div |
    ```
  </action>
  <verify>
    <automated># Run from repo root. Build/lint inside dealdrop/, audit-table grep against repo-root .planning path.
cd dealdrop &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; ! grep -rE '@container|@theme' src/components &amp;&amp; cd .. &amp;&amp; grep -q 'Fix shipped' .planning/phases/07-polish-deployment/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - For each row in the Task 1 audit table, exactly one fix is applied to the corresponding component file
    - All fixes use existing `sm:`/`md:`/`lg:` prefixes (no new breakpoints, no `@container`, no `@theme`)
    - `npm run lint` clean
    - `npm run build` green
    - 07-VERIFICATION.md table now reads "Fix shipped" with concrete class changes per row
    - Each modified file has a minimal diff (no whole-file rewrites; no unrelated changes)
  </acceptance_criteria>
  <done>
    All breaks from Task 1 fixed; build + lint green; audit table updated with shipped fixes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Second audit walk (regression / completeness)</name>
  <files>(no files modified directly — operator-driven; artifacts updated via <how-to-verify> steps)</files>
  <read_first>
    - .planning/phases/07-polish-deployment/07-VERIFICATION.md (the audit table from Task 1+2 — to confirm coverage)
    - The components modified in Task 2 (verify the diff applied as intended)
  </read_first>
  <action>Operator-driven checkpoint. Follow the numbered steps in <how-to-verify> verbatim. Each step has an explicit expected output / observed value. The acceptance_criteria block at the bottom of this task is the gate; do NOT mark this task done unless every acceptance row is satisfied.</action>
  <what-built>
    Task 2 applied the proposed fixes. Now we re-walk to confirm: (a) the fixes actually work, (b) the fixes did not introduce NEW breaks, (c) no remaining breaks were missed in pass 1.
  </what-built>
  <how-to-verify>
    Step A — Restart dev server:
    ```bash
    cd dealdrop && npm run dev
    ```

    Step B — Repeat the same walk from Task 1 at 320 / 375 / 768 viewports. For each, confirm:
    - The breaks recorded in pass 1 are gone.
    - No new breaks were introduced by the fixes.

    Step C — Result decision:
    - **Zero new breaks at 320px** → audit complete. Append a final note to the table: "**Pass 2: zero new breaks at 320px. Audit complete.**"
    - **One or more new breaks** → loop back to Task 2 with these new rows added to the audit table. Each subsequent pass becomes "Pass 3", "Pass 4", etc. (Realistic budget: typically 1-2 passes total.)

    Step D — Stop dev server.

    Step E — Final regression sweep:
    ```bash
    cd dealdrop && npm run test && npm run lint && npm run build
    ```

    All green. The full vitest suite is unchanged by Tailwind class tweaks (the existing tests assert on roles/text, not on layout classes); zero regressions expected.

    Step F — Confirm git state:
    ```bash
    cd dealdrop && git diff --stat src/components/
    ```

    Each modified file should show a small diff (1-5 lines). NO file should have a multi-dozen-line refactor diff. If any file does, that's a sign opportunistic-refactor crept in — revert that file and reapply the minimal fix.
  </how-to-verify>
  <acceptance_criteria>
    - Pass 2 produces ZERO new breaks at 320px
    - VERIFICATION.md table has "Pass 2: zero new breaks" note
    - Full vitest suite green
    - `npm run lint` clean
    - `npm run build` green
    - `git diff --stat src/components/` shows minimal per-file diffs (no refactor sprawl)
    - Zero unrelated changes outside the components named in the audit table
  </acceptance_criteria>
  <resume-signal>Type "approved" once second walk is clean and full suite is green. If new breaks observed, type "loop: <N> new breaks" to re-run Task 2.</resume-signal>
  <verify>
    <automated>MISSING — checkpoint task; verification is operator-driven via <how-to-verify> + <acceptance_criteria>; no automated command applies</automated>
  </verify>
  <done>All <acceptance_criteria> rows satisfied; <resume-signal> typed by operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries — POL-04 is a UI/CSS audit; no data flows are modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | DoS via opportunistic refactor (scope creep) | dealdrop/src/components/* | mitigate | Acceptance criteria explicitly forbids new abstractions / container queries / @theme blocks. `git diff --stat` check in Task 3 catches refactor sprawl. >6 breaks at 320px triggers a deviation flag (CONTEXT.md D-05 budget). |
| T-07-08 | Information Disclosure via leftover debug throw | ProductGrid.tsx | mitigate | Task 1's temporary `throw new Error('test')` for error-fallback walk MUST be reverted in Task 1's acceptance criteria. Task 3's `git diff` review catches an un-reverted throw. |
</threat_model>

<verification>
- `cd dealdrop && npm run test && npm run lint && npm run build` — all green after fixes
- `cd dealdrop && grep -rE '@container|@theme' src/components` — zero matches (no new abstractions)
- `cd dealdrop && grep -rE "throw new Error\\('test'\\)" src/components` — zero matches (debug throw reverted)
- `git diff --stat src/components/` — minimal per-file diffs
- 07-VERIFICATION.md POL-04 table populated with shipped fixes + "Pass 2: zero new breaks" note
- The executor MUST enumerate the actual set of modified files in SUMMARY.md so the audit can compare against the `files_modified` superset declared in frontmatter.
</verification>

<success_criteria>
- POL-04 audit complete: every observed break at 320 / 375 / 768 has a corresponding "Fix shipped" row in 07-VERIFICATION.md
- Zero new breaks on second walk at 320px
- Full suite green
- Zero refactor sprawl (small per-file diffs, no new abstractions)
- Total break count <= 6 at 320px (or deviation flagged and approved before tweaking)
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deployment/07-04-SUMMARY.md` capturing:
- Number of audit passes (typically 1-2)
- Final break count and fix count (should match — N breaks, N fixes)
- Per-file diff stat (e.g. `Header.tsx +1/-1`, `AddProductDialog.tsx +1/-1`)
- Confirmation of zero refactor sprawl, zero new abstractions
- Test/lint/build status
- The audit table contents (copied from 07-VERIFICATION.md POL-04 section)
</output>
