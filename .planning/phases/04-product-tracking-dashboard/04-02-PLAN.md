---
phase: 04-product-tracking-dashboard
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql
  - dealdrop/src/types/database.ts
autonomous: false
requirements: [DASH-08]
must_haves:
  truths:
    - "`products.last_scrape_failed_at` column exists in the linked Supabase project (vhlbdcsxccaknccawfdj)"
    - "`dealdrop/src/types/database.ts` reflects the new column so `ProductCard` can read it without a TS error"
    - "Partial index `products_last_scrape_failed_at_idx` exists for Phase 6 cron query efficiency"
  artifacts:
    - path: "dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql"
      provides: "ALTER TABLE products ADD COLUMN last_scrape_failed_at TIMESTAMPTZ NULL + partial index"
      contains: "last_scrape_failed_at timestamptz"
    - path: "dealdrop/src/types/database.ts"
      provides: "Regenerated type with last_scrape_failed_at on products Row/Insert/Update"
      contains: "last_scrape_failed_at"
  key_links:
    - from: "dealdrop/src/components/dashboard/ProductCard.tsx (Wave 2)"
      to: "products.last_scrape_failed_at column"
      via: "Tables<'products'>"
      pattern: "last_scrape_failed_at"
---

<objective>
Ship the Phase 4 schema change that powers DASH-08 ("tracking failed" badge). This plan adds one nullable timestamp column + partial index, pushes the migration to the linked Supabase project, and regenerates TypeScript types so downstream components (Wave 2) can read the column without compile errors.

Purpose: Without the column + regenerated types, `ProductCard` will reference a field that doesn't exist in `Tables<'products'>` and `tsc --noEmit` will fail. Build-time type check would pass because types don't reach out to the live DB — creating a false-positive verification state (per schema_push_requirement in planning context).

Per checker W4 (filename aligns with RESEARCH.md + PATTERNS.md): the migration file is `0004_add_last_scrape_failed_at.sql` (not the previous `0004_last_scrape_failed_at.sql`).

Per checker B3: Task 2 (`supabase db push`) is marked `autonomous: false` at the plan level because the verify step requires an environment precondition (`SUPABASE_ACCESS_TOKEN`) that may not be present at plan-execution time on non-TTY environments. The verify command is timeout-bounded (60s) to prevent indefinite hangs on interactive auth prompts.

Output:
- Migration file `dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql`
- Column + partial index present in the linked Supabase project
- `dealdrop/src/types/database.ts` regenerated to include `last_scrape_failed_at: string | null`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-product-tracking-dashboard/04-CONTEXT.md
@.planning/phases/04-product-tracking-dashboard/04-RESEARCH.md
@.planning/phases/04-product-tracking-dashboard/04-PATTERNS.md
@dealdrop/supabase/migrations/0001_init_schema.sql
@dealdrop/supabase/migrations/0002_enable_rls.sql
@dealdrop/src/types/database.ts

<interfaces>
<!-- Existing schema context so executor sees the surrounding migrations. -->

Existing migration numbering (verified by ls dealdrop/supabase/migrations/):
- 0001_init_schema.sql
- 0002_enable_rls.sql
- 0003_enable_extensions.sql
- 0004_add_last_scrape_failed_at.sql  <-- THIS PLAN adds (W4 fix: filename aligns with RESEARCH.md)

Existing database.ts `products` Row type (dealdrop/src/types/database.ts:50-60):
```typescript
products: {
  Row: {
    created_at: string
    currency: string
    current_price: number
    id: string
    image_url: string | null
    name: string
    updated_at: string
    url: string
    user_id: string
  }
  // Insert + Update follow same shape
}
```

Phase 1 Plan 01-04 precedent:
- Linked project ID: `vhlbdcsxccaknccawfdj` (dealdrop-dev, Tokyo region)
- Push command succeeded cleanly on Free tier
- Type regen command: `supabase gen types typescript --project-id vhlbdcsxccaknccawfdj > src/types/database.ts`
- Non-TTY auth path: `SUPABASE_ACCESS_TOKEN` environment variable (surfaces a deviation checkpoint if missing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0004_add_last_scrape_failed_at.sql</name>
  <files>dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql</files>
  <read_first>
    - dealdrop/supabase/migrations/0001_init_schema.sql (CREATE INDEX idiom to mirror)
    - dealdrop/supabase/migrations/0002_enable_rls.sql (header-comment style to mirror — "Source:" + "Note:" prefix)
    - .planning/phases/04-product-tracking-dashboard/04-RESEARCH.md §"Complete migration (DASH-08 data source)" (verbatim SQL)
    - .planning/phases/04-product-tracking-dashboard/04-PATTERNS.md §"supabase/migrations/0004_add_last_scrape_failed_at.sql" (pattern assignment)
  </read_first>
  <action>
    Create `dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` with exactly this content (verbatim from 04-RESEARCH.md §"Complete migration"):

    ```sql
    -- File: dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql
    -- DASH-08: track whether the most recent scrape for a product failed.
    -- Phase 4 adds the column + renders the badge when non-null.
    -- Phase 6 cron writes the timestamp on failure; clears to NULL on next success.

    alter table public.products
      add column last_scrape_failed_at timestamptz null;

    -- No default; nullable by design. NULL = scraping OK (or never attempted yet).

    create index products_last_scrape_failed_at_idx
      on public.products (last_scrape_failed_at)
      where last_scrape_failed_at is not null;
    -- Partial index: Phase 6 cron can efficiently find still-failing products without
    -- scanning the full table. Uses ~0 space for healthy products.
    ```

    Do NOT add a `DEFAULT now()` — the column must default to NULL (means "never failed or not yet attempted"). Do NOT add a CHECK constraint. Do NOT alter any other table or column.
  </action>
  <verify>
    <automated>test -f dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql &amp;&amp; grep -c "last_scrape_failed_at timestamptz null" dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql | xargs test 1 = </automated>
  </verify>
  <acceptance_criteria>
    - `test -f dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` exits 0
    - `grep -c "alter table public.products" dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` → 1
    - `grep -c "last_scrape_failed_at timestamptz null" dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` → 1
    - `grep -c "create index products_last_scrape_failed_at_idx" dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` → 1
    - `grep -c "where last_scrape_failed_at is not null" dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` → 1
    - `grep -c "default" dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` → 0 (no DEFAULT clause — must be nullable)
    - `grep -c "DASH-08" dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` → 1
  </acceptance_criteria>
  <done>Migration file exists with exact SQL from RESEARCH.md. Nullable column, partial index, header comments match Phase 1 migration style.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migration to linked Supabase project (bounded; precondition-gated)</name>
  <files>(remote Supabase state only — no local file changes)</files>
  <read_first>
    - **B3 environment precondition (CHECK FIRST):** run `echo "${SUPABASE_ACCESS_TOKEN:-MISSING}"` — this MUST print a non-empty value that is NOT the literal "MISSING". If it prints "MISSING" or is empty, STOP and surface as a `checkpoint:human-action` — the user needs to export `SUPABASE_ACCESS_TOKEN` in the shell where the executor runs. Do NOT attempt `supabase db push` without this precondition satisfied; the CLI will block on an interactive auth prompt that hangs indefinitely in non-TTY environments.
    - dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql (the migration being pushed; confirm Task 1 wrote it)
    - .planning/STATE.md §"Accumulated Context → Decisions → [Phase 01]: Plan 01-04 migrations pushed cleanly" (precedent: push is a known-working path on this project)
    - .planning/phases/04-product-tracking-dashboard/04-RESEARCH.md §"Runtime State Inventory → Migration note" (authoritative push + regen commands)
  </read_first>
  <action>
    B3 FIX: this task is `autonomous: false` at the plan level because the precondition check may require user intervention. Proceed ONLY if the `<read_first>` environment precondition check (`SUPABASE_ACCESS_TOKEN` non-empty) passes.

    Run the Supabase CLI push command with a bounded timeout so it cannot hang the executor. The linked project ID is `vhlbdcsxccaknccawfdj` (per Phase 1 Plan 01-04; STATE.md confirms link is active).

    ```
    cd dealdrop && timeout 60 npx supabase db push 2>&1 | tee /tmp/supabase-push.log
    STATUS=$?
    # Accept exit 0 OR an "already applied / up to date" message as success.
    if [ $STATUS -ne 0 ]; then
      if grep -q "already applied\|remote database is up to date" /tmp/supabase-push.log; then
        echo "push succeeded (already applied)"
      else
        echo "push FAILED — see /tmp/supabase-push.log for details"
        exit $STATUS
      fi
    fi
    ```

    The `timeout 60` cap prevents a non-TTY interactive-auth prompt from hanging the executor indefinitely. If the command times out (exit 124), surface as a checkpoint — do NOT proceed to Task 3, which depends on the column existing in the remote DB.

    After push, verify the column exists by running:
    ```
    cd dealdrop && timeout 30 npx supabase db remote query --project-id vhlbdcsxccaknccawfdj "select column_name from information_schema.columns where table_schema='public' and table_name='products' and column_name='last_scrape_failed_at';"
    ```
    (If `db remote query` subcommand is unavailable on this CLI version, fall back to `timeout 30 npx supabase db diff` and verify the diff is empty — migration applied.)

    This task is BLOCKING per the schema_push_requirement: every subsequent task that reads `last_scrape_failed_at` (Wave 2 `ProductCard`) will silently appear to pass type-check without this push, because TypeScript types in `src/types/database.ts` are generated, not introspected live. The push must happen BEFORE Task 3 (regen) so the regen pulls the updated schema.
  </action>
  <verify>
    <automated>cd dealdrop &amp;&amp; timeout 60 npx supabase db push 2&gt;&amp;1 | tee /tmp/supabase-push.log; EC=$?; test $EC -eq 0 || grep -q "already applied\|remote database is up to date" /tmp/supabase-push.log</automated>
  </verify>
  <acceptance_criteria>
    - Environment precondition: `SUPABASE_ACCESS_TOKEN` is non-empty in the executor environment (verified in `<read_first>` step). If missing, this task must surface as a blocking checkpoint — NOT silently skipped.
    - Command `cd dealdrop && timeout 60 npx supabase db push` completes within 60 seconds (no indefinite hang in non-TTY environments).
    - Exit code is 0 OR the captured log contains "already applied" / "remote database is up to date".
    - `cd dealdrop && timeout 30 npx supabase db diff 2>&1` reports "No schema changes" (or equivalent no-pending-changes message) within 30 seconds.
    - No `0004_add_last_scrape_failed_at.sql` in the output of `supabase db diff` as an unapplied migration.
    - If `timeout` exit code is 124 (timed out), task is marked FAILED and surfaces as a checkpoint — executor must not claim success on a timeout.
  </acceptance_criteria>
  <done>Migration successfully pushed within the 60-second bound; remote schema contains `last_scrape_failed_at` column on `public.products`; partial index exists; precondition (SUPABASE_ACCESS_TOKEN) was satisfied before the command ran.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate database.ts types</name>
  <files>dealdrop/src/types/database.ts</files>
  <read_first>
    - dealdrop/src/types/database.ts (current shape — verify `products.Row` does NOT contain `last_scrape_failed_at` before regen)
    - .planning/phases/04-product-tracking-dashboard/04-RESEARCH.md §"Runtime State Inventory → Migration note" (authoritative regen command)
  </read_first>
  <action>
    Run the Supabase type-generation command, overwriting `dealdrop/src/types/database.ts`:

    ```
    cd dealdrop && timeout 60 npx supabase gen types typescript --project-id vhlbdcsxccaknccawfdj > src/types/database.ts
    ```

    This OVERWRITES the file. The generated file must contain `last_scrape_failed_at: string | null` on the `products.Row`, `products.Insert` (with `?` since it's nullable), and `products.Update` interfaces.

    If the command fails (access token expired, network error), STOP and surface as a checkpoint — do NOT hand-edit `database.ts` silently, because the next regen in a future phase will clobber manual edits. The Assumption A5 fallback in RESEARCH.md allows manual editing as a last resort, but only after surfacing the CLI failure.

    After generation, verify that only the `products` Row/Insert/Update shapes changed and no other table or type was removed (a sanity check against the generator regressing unrelated types).
  </action>
  <verify>
    <automated>grep -c "last_scrape_failed_at" dealdrop/src/types/database.ts | xargs -I{} test {} -ge 3 &amp;&amp; cd dealdrop &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -c "error TS" | xargs -I{} test {} = 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "last_scrape_failed_at" dealdrop/src/types/database.ts` → ≥ 3 (appears in Row, Insert, Update)
    - `grep -c "last_scrape_failed_at: string | null" dealdrop/src/types/database.ts` → ≥ 1 (nullable on Row)
    - `grep -c "\"products\"" dealdrop/src/types/database.ts` → ≥ 1 (products table still present — generator didn't regress)
    - `grep -c "\"price_history\"" dealdrop/src/types/database.ts` → ≥ 1 (other table still present)
    - `grep -c "export type Tables" dealdrop/src/types/database.ts` → ≥ 1 (Tables<> helper still present)
    - `cd dealdrop && npx tsc --noEmit` exits 0 (no TypeScript errors introduced)
    - `cd dealdrop && npm run build 2>&1 | tail -5` succeeds (build still green with new types)
  </acceptance_criteria>
  <done>`database.ts` regenerated; `last_scrape_failed_at` on products Row/Insert/Update as nullable; `tsc --noEmit` and `npm run build` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine → Supabase remote | `supabase db push` writes DDL against production project (dealdrop-dev is the dev project; no prod live yet). Auth via `SUPABASE_ACCESS_TOKEN`. |
| generated types → application code | `database.ts` is machine-generated from live schema; compile-time contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T (Tampering) | `src/types/database.ts` | mitigate | Task 3 regenerates from live schema via `supabase gen types`. Manual edit path is a documented fallback only (requires surfacing the CLI failure first). Acceptance criterion asserts other tables still present to detect regressions. |
| T-04-05 | I (Information Disclosure) | Column values | accept | `last_scrape_failed_at` is a timestamp only — no PII, no secrets. RLS policies from Phase 1 already scope SELECT on `products` to `user_id = auth.uid()`. New column inherits existing RLS. |
| T-04-06 | D (Denial of Service) | Partial index build | accept | `CREATE INDEX` with `WHERE last_scrape_failed_at IS NOT NULL` on an empty table (dev DB has 0 rows per RESEARCH.md Runtime State Inventory) runs in <100ms. No lock held on production data. |
| T-04-07 | E (Elevation of privilege) | Migration bypass of RLS | accept | DDL runs as service role via CLI — this is the correct path per Phase 1 Plan 01-04 precedent. No runtime code reads service role key in Phase 4. |
| T-04-35 | D (Denial of Service) | Non-TTY push hang | mitigate (B3) | Verify wraps `supabase db push` in `timeout 60`. Precondition check on `SUPABASE_ACCESS_TOKEN` in `<read_first>` surfaces a checkpoint if missing. No indefinite hang path exists. |
</threat_model>

<verification>
- Migration file exists at `dealdrop/supabase/migrations/0004_add_last_scrape_failed_at.sql` (W4 filename) and matches RESEARCH.md §"Complete migration" verbatim
- `cd dealdrop && timeout 30 npx supabase db diff` reports no pending changes (migration applied), completes within 30 seconds
- `cd dealdrop && npx tsc --noEmit` exits 0
- `cd dealdrop && npm run build` succeeds
- `grep -c "last_scrape_failed_at" dealdrop/src/types/database.ts` ≥ 3
</verification>

<success_criteria>
- `products.last_scrape_failed_at` column exists in linked Supabase project with partial index `products_last_scrape_failed_at_idx`
- `dealdrop/src/types/database.ts` regenerated and includes `last_scrape_failed_at: string | null` on Row / Insert / Update
- `tsc --noEmit` + `npm run build` both pass
- B3 fix: Task 2 verify commands are bounded by `timeout 60` and gated on the `SUPABASE_ACCESS_TOKEN` precondition
- W4 fix: all 8 occurrences of the migration filename use `0004_add_last_scrape_failed_at.sql`
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-tracking-dashboard/04-02-SUMMARY.md`
</output>
