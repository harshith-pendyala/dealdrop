---
phase: 01-foundation-database
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - dealdrop/package.json
  - dealdrop/package-lock.json
  - dealdrop/tsconfig.json
  - dealdrop/next.config.ts
  - dealdrop/app/layout.tsx
  - dealdrop/.gitignore
  - dealdrop/.env.example
  - dealdrop/.env.local
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-07
  - FND-08
must_haves:
  truths:
    - "All 7 required env var names are documented in dealdrop/.env.example (committed, no real values)"
    - "dealdrop/.env.local exists locally (uncommitted) with placeholder values that satisfy Zod shape"
    - "`@/*` path alias resolves to both `./*` and `./src/*` so downstream plans can import from `@/lib/env`"
    - "App metadata reads 'DealDrop — Universal Price Tracker' with no 'Create Next App' remnants"
    - "`dealdrop/next.config.ts` exposes `images.remotePatterns` permitting https/http wildcards for scraped product images"
    - "All Phase 1 runtime + dev dependencies installed (package.json + package-lock.json updated)"
  artifacts:
    - path: "dealdrop/.env.example"
      provides: "Committed template listing 7 env var names (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, FIRECRAWL_API_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL, CRON_SECRET)"
      contains: "NEXT_PUBLIC_SUPABASE_URL="
    - path: "dealdrop/.env.local"
      provides: "Local (gitignored) env file with placeholder values good enough to pass Zod at build time until Plan 03 paste-in"
      contains: "CRON_SECRET="
    - path: "dealdrop/tsconfig.json"
      provides: "Updated `@/*` paths array including `./src/*`"
      contains: '"@/*": ["./*", "./src/*"]'
    - path: "dealdrop/next.config.ts"
      provides: "images.remotePatterns wildcard for https + http"
      contains: "remotePatterns"
    - path: "dealdrop/app/layout.tsx"
      provides: "DealDrop metadata replacing Create Next App placeholders"
      contains: "DealDrop"
    - path: "dealdrop/package.json"
      provides: "Runtime deps: @supabase/supabase-js, @supabase/ssr, @t3-oss/env-nextjs, zod, server-only; dev dep: supabase"
      contains: "@supabase/ssr"
    - path: "dealdrop/.gitignore"
      provides: "Keeps `.env*` ignored but whitelists `.env.example` so it can be committed"
      contains: "!.env.example"
  key_links:
    - from: "dealdrop/tsconfig.json paths"
      to: "Plan 02 imports `@/lib/env` and `@/lib/supabase/*`"
      via: "paths alias `@/*` resolving both `./` and `./src/*`"
      pattern: '"@/\\*": \\["\\./\\*", "\\./src/\\*"\\]'
    - from: "dealdrop/.env.local"
      to: "@t3-oss/env-nextjs Zod schema in Plan 02"
      via: "All 7 variable names present with non-empty values; CRON_SECRET length ≥ 32"
      pattern: "CRON_SECRET=.{32,}"
    - from: "dealdrop/.gitignore whitelist"
      to: "git commit of `.env.example` in this plan"
      via: "`!.env.example` exception after the `.env*` glob"
      pattern: "!\\.env\\.example"
---

<objective>
Wave 0 scaffolding — install Phase 1 dependencies, update `tsconfig.json` path alias to reach `src/`, swap metadata in the root layout, add `images.remotePatterns` to `next.config.ts`, create the committed `.env.example` template, create a gitignored `.env.local` with placeholder values that satisfy the Zod env schema introduced in Plan 02, and whitelist `.env.example` in `.gitignore`.

Purpose: Every subsequent Phase 1 plan (env validation, Supabase clients, Shadcn init, migrations) imports from `@/lib/*`, reads validated env vars, or needs the dependency tree in place. This plan blocks all others in Phase 1 (Wave 0).

Output:
- `dealdrop/package.json` + `dealdrop/package-lock.json` updated with 5 runtime deps + 1 dev dep
- `dealdrop/tsconfig.json` paths expanded to include `./src/*`
- `dealdrop/next.config.ts` rewritten to include `images.remotePatterns`
- `dealdrop/app/layout.tsx` metadata replaced with DealDrop copy
- `dealdrop/.env.example` created (committed)
- `dealdrop/.env.local` created (gitignored, placeholder values)
- `dealdrop/.gitignore` appended with `!.env.example` whitelist
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@dealdrop/CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-database/01-CONTEXT.md
@.planning/phases/01-foundation-database/01-RESEARCH.md
@.planning/phases/01-foundation-database/01-PATTERNS.md
@.planning/phases/01-foundation-database/01-VALIDATION.md
@dealdrop/package.json
@dealdrop/tsconfig.json
@dealdrop/next.config.ts
@dealdrop/app/layout.tsx
@dealdrop/.gitignore

<interfaces>
<!-- Locked constants from RESEARCH.md that downstream plans consume. -->

Required env variable names (7 total — FND-02):
  NEXT_PUBLIC_SUPABASE_URL        // client-exposed URL
  NEXT_PUBLIC_SUPABASE_ANON_KEY   // client-exposed anon key
  SUPABASE_SERVICE_ROLE_KEY       // SERVER-ONLY (never NEXT_PUBLIC_)
  FIRECRAWL_API_KEY               // SERVER-ONLY
  RESEND_API_KEY                  // SERVER-ONLY
  RESEND_FROM_EMAIL               // SERVER-ONLY (must look like email)
  CRON_SECRET                     // SERVER-ONLY (min length 32)

Dependency set (runtime, install command from RESEARCH.md § "Standard Stack — Installation"):
  @supabase/supabase-js   ^2.103.3
  @supabase/ssr           ^0.10.2
  @t3-oss/env-nextjs      ^0.13.11
  zod                     ^4.3.6
  server-only             ^0.0.1

Dev dependency:
  supabase                ^2.92.1  // Supabase CLI — exposes `npx supabase`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 1 npm dependencies</name>
  <files>dealdrop/package.json, dealdrop/package-lock.json</files>
  <read_first>
    - dealdrop/package.json (current dependency tree — 3 runtime deps, 8 devDeps)
    - .planning/phases/01-foundation-database/01-RESEARCH.md § "Standard Stack — Installation" (installation block starting ~line 133)
    - .planning/phases/01-foundation-database/01-PATTERNS.md § "dealdrop/package.json (config — deps + scripts) — MODIFY" (version table)
  </read_first>
  <action>
Run from `/Users/harshithpendyala/Documents/DealDrop/dealdrop/` (D-02 locks cwd):

```bash
cd dealdrop
npm install @supabase/supabase-js@^2.103.3 @supabase/ssr@^0.10.2 @t3-oss/env-nextjs@^0.13.11 zod@^4.3.6 server-only@^0.0.1
npm install -D supabase@^2.92.1
```

Notes:
- DO NOT modify the `"scripts"` block. `"lint": "eslint"` is already Next.js 16–correct (FND-07 is satisfied by the existing scaffold per 01-PATTERNS.md).
- DO NOT pre-install `lucide-react`, `sonner`, `clsx`, or `tailwind-merge` — Shadcn adds those in Plan 05.
- DO NOT pin `next`, `react`, or `react-dom` — those are already correct.
- If npm emits a peer-dependency warning for `zod@4` vs `@t3-oss/env-nextjs@0.13.11`, proceed — per RESEARCH.md § "Standard Stack — Core" the peer range accepts zod v4.
- Do NOT run `npm audit fix --force` — can downgrade pinned versions.

Expected diff in `dealdrop/package.json`:
- `"dependencies"` gains 5 entries (alphabetical): `@supabase/ssr`, `@supabase/supabase-js`, `@t3-oss/env-nextjs`, `server-only`, `zod`
- `"devDependencies"` gains 1 entry: `supabase`

Commit these two files separately from the config/file-content edits in tasks 2–6 so the lockfile update is easy to review.
  </action>
  <verify>
    <automated>cd dealdrop && grep -q '"@supabase/ssr"' package.json && grep -q '"@supabase/supabase-js"' package.json && grep -q '"@t3-oss/env-nextjs"' package.json && grep -q '"zod"' package.json && grep -q '"server-only"' package.json && grep -q '"supabase"' package.json && test -f package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - `cd dealdrop && grep -c '"@supabase/ssr"' package.json` returns at least 1
    - `cd dealdrop && grep -c '"@supabase/supabase-js"' package.json` returns at least 1
    - `cd dealdrop && grep -c '"@t3-oss/env-nextjs"' package.json` returns at least 1
    - `cd dealdrop && grep -c '"zod"' package.json` returns at least 1
    - `cd dealdrop && grep -c '"server-only"' package.json` returns at least 1
    - `cd dealdrop && grep -A 10 '"devDependencies"' package.json | grep -q '"supabase"'`
    - `dealdrop/package-lock.json` exists and contains `"@supabase/ssr"` (grep-verifiable)
    - `dealdrop/node_modules/@supabase/ssr/package.json` exists after install
    - `dealdrop/node_modules/@t3-oss/env-nextjs/package.json` exists after install
    - `cd dealdrop && grep -q '"lint": "eslint"' package.json` (unchanged; scripts block untouched)
  </acceptance_criteria>
  <done>Six new dependencies installed (5 runtime + 1 dev); `package.json` + `package-lock.json` updated; no existing scripts or pinned versions changed.</done>
</task>

<task type="auto">
  <name>Task 2: Expand tsconfig.json path alias to reach src/</name>
  <files>dealdrop/tsconfig.json</files>
  <read_first>
    - dealdrop/tsconfig.json (current `paths` — line 21-23, maps `@/*` to `./*` only)
    - .planning/phases/01-foundation-database/01-RESEARCH.md § "Pitfall 9: `@/*` path alias doesn't reach `src/`"
    - .planning/phases/01-foundation-database/01-PATTERNS.md § "dealdrop/tsconfig.json (config — path aliases) — MODIFY"
    - .planning/phases/01-foundation-database/01-CONTEXT.md § "Integration Points" (D-03 locks `dealdrop/src/lib/*`)
  </read_first>
  <action>
Edit `dealdrop/tsconfig.json` — change ONLY the `paths` mapping. Keep every other compilerOption byte-identical (`strict: true`, `target: "ES2017"`, `moduleResolution: "bundler"`, `plugins: [{"name": "next"}]`, etc.).

Before:
```json
"paths": {
  "@/*": ["./*"]
}
```

After:
```json
"paths": {
  "@/*": ["./*", "./src/*"]
}
```

Why: Plan 02 creates `dealdrop/src/lib/env.ts` and `dealdrop/src/lib/supabase/{server,browser,admin}.ts`; Plan 05 creates `dealdrop/src/lib/utils.ts` via Shadcn. Without the `./src/*` entry, every `import { env } from '@/lib/env'` fails and Shadcn would silently write `./lib/utils.ts` at the project root, violating D-03.

Do NOT:
- Add `baseUrl` — not needed; `paths` are resolved relative to tsconfig location.
- Change `include`/`exclude` arrays.
- Remove the first `"./*"` entry — `app/`, `components/` still resolve via that entry.
  </action>
  <verify>
    <automated>grep -q '"@/\*": \["\./\*", "\./src/\*"\]' dealdrop/tsconfig.json && grep -q '"strict": true' dealdrop/tsconfig.json && grep -q '"moduleResolution": "bundler"' dealdrop/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@/\*": \["\./\*", "\./src/\*"\]' dealdrop/tsconfig.json` returns 1
    - `grep -c '"strict": true' dealdrop/tsconfig.json` returns 1 (unchanged)
    - `grep -c '"name": "next"' dealdrop/tsconfig.json` returns 1 (plugins unchanged)
    - `grep -c '"moduleResolution": "bundler"' dealdrop/tsconfig.json` returns 1 (unchanged)
    - `grep -c '"target": "ES2017"' dealdrop/tsconfig.json` returns 1 (unchanged)
    - `cd dealdrop && npx tsc --noEmit 2>&1 | head -20` exits 0 (no TS errors from existing scaffold)
  </acceptance_criteria>
  <done>`paths."@/*"` now contains both `./*` and `./src/*`; no other compiler options altered; `tsc --noEmit` still passes on existing scaffold.</done>
</task>

<task type="auto">
  <name>Task 3: Replace app/layout.tsx metadata with DealDrop copy</name>
  <files>dealdrop/app/layout.tsx</files>
  <read_first>
    - dealdrop/app/layout.tsx (current metadata on lines 15-18)
    - .planning/phases/01-foundation-database/01-RESEARCH.md § "Pattern 7: Metadata Replacement"
    - .planning/phases/01-foundation-database/01-PATTERNS.md § "dealdrop/app/layout.tsx (meta — SEO + root HTML) — MODIFY"
    - .planning/phases/01-foundation-database/01-CONTEXT.md § "Existing Code Insights — Reusable Assets" ("replace metadata only, don't rebuild")
  </read_first>
  <action>
Edit `dealdrop/app/layout.tsx`. Replace ONLY the `metadata` export (lines 15-18). Keep the Geist font wiring (lines 1-13) exactly and keep the `RootLayout` function body (lines 20-33) exactly — including the `h-full antialiased` classes on `<html>` and `min-h-full flex flex-col` on `<body>`.

Replace:
```typescript
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
```

With:
```typescript
export const metadata: Metadata = {
  title: "DealDrop — Universal Price Tracker",
  description: "Track products from any e-commerce site. Get email alerts the moment the price drops.",
};
```

Do NOT (Phase 1 scope boundary — per 01-PATTERNS.md § "Pattern 7" and 01-CONTEXT.md § "Not in scope for this phase"):
- Add `<Toaster />` or any Sonner provider — deferred to Phase 7.
- Add `<ThemeProvider>` / `next-themes` — CSS media query path chosen in CONTEXT.md discretion.
- Change `<html>` or `<body>` class lists — Plan 05 (Shadcn) will merge `bg-background text-foreground` if needed via globals.css token flow.
- Replace the Geist font setup.
- Add OpenGraph / Twitter meta — Phase 7 polish.
  </action>
  <verify>
    <automated>grep -q 'DealDrop — Universal Price Tracker' dealdrop/app/layout.tsx && ! grep -q 'Create Next App' dealdrop/app/layout.tsx && grep -q 'font-geist-sans' dealdrop/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DealDrop' dealdrop/app/layout.tsx` returns at least 1
    - `grep -c 'Create Next App' dealdrop/app/layout.tsx` returns 0
    - `grep -c 'Generated by create next app' dealdrop/app/layout.tsx` returns 0
    - `grep -c 'font-geist-sans' dealdrop/app/layout.tsx` returns 1 (Geist wiring preserved)
    - `grep -c 'font-geist-mono' dealdrop/app/layout.tsx` returns 1 (Geist wiring preserved)
    - `grep -c 'export default function RootLayout' dealdrop/app/layout.tsx` returns 1 (component preserved)
    - `grep -c "h-full antialiased" dealdrop/app/layout.tsx` returns 1 (html classes preserved)
    - `grep -c "min-h-full flex flex-col" dealdrop/app/layout.tsx` returns 1 (body classes preserved)
  </acceptance_criteria>
  <done>layout.tsx metadata reads DealDrop copy; Geist fonts, RootLayout function, and HTML/body class lists untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Configure next.config.ts images.remotePatterns wildcard</name>
  <files>dealdrop/next.config.ts</files>
  <read_first>
    - dealdrop/next.config.ts (current minimal config — 8 lines)
    - .planning/phases/01-foundation-database/01-RESEARCH.md § "Pattern 6: next.config.ts — images.remotePatterns"
    - .planning/phases/01-foundation-database/01-PATTERNS.md § "dealdrop/next.config.ts — MODIFY"
    - .planning/phases/01-foundation-database/01-CONTEXT.md § "Specific Ideas" (permissive wildcard justified for v1 portfolio)
  </read_first>
  <action>
Replace the full contents of `dealdrop/next.config.ts` with the verified RESEARCH.md pattern (keep `import type` style — matches scaffold convention):

```typescript
// Source: node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Permissive allowlist — scraped product images come from any e-commerce domain.
    // Hardening (strict allowlist) is a Phase 7 concern.
    remotePatterns: [
      { protocol: "https", hostname: "**" },
      { protocol: "http", hostname: "**" }, // some legacy retailers still serve HTTP images
    ],
  },
};

export default nextConfig;
```

Do NOT:
- Use deprecated `images.domains: [...]` (removed in v15, confirmed removed in v16).
- Add `experimental`, `serverActions`, or `output` keys — not needed in Phase 1.
- Break the `import type { NextConfig }` convention.
- Add `transpilePackages` — Supabase works without it.
  </action>
  <verify>
    <automated>grep -q 'remotePatterns' dealdrop/next.config.ts && grep -q 'hostname: "\*\*"' dealdrop/next.config.ts && grep -q 'export default nextConfig' dealdrop/next.config.ts && grep -q 'import type { NextConfig }' dealdrop/next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'remotePatterns' dealdrop/next.config.ts` returns 1
    - `grep -c 'protocol: "https"' dealdrop/next.config.ts` returns 1
    - `grep -c 'protocol: "http"' dealdrop/next.config.ts` returns 1
    - `grep -c 'hostname: "\*\*"' dealdrop/next.config.ts` returns 2 (https + http)
    - `grep -c 'export default nextConfig' dealdrop/next.config.ts` returns 1
    - `grep -c 'images.domains' dealdrop/next.config.ts` returns 0 (deprecated API not introduced)
    - `cd dealdrop && npx tsc --noEmit 2>&1 | head -20` exits 0
  </acceptance_criteria>
  <done>next.config.ts exports a NextConfig object with `images.remotePatterns` covering both https and http wildcards; no deprecated `images.domains` used.</done>
</task>

<task type="auto">
  <name>Task 5: Whitelist .env.example in .gitignore</name>
  <files>dealdrop/.gitignore</files>
  <read_first>
    - dealdrop/.gitignore (current line 34: `.env*` glob ignores all env files)
    - .planning/phases/01-foundation-database/01-PATTERNS.md § "dealdrop/.gitignore — KEEP UNCHANGED" (describes the whitelist option chosen here)
    - .planning/phases/01-foundation-database/01-CONTEXT.md § Decisions D-04 (`.env.example` must be committed)
  </read_first>
  <action>
Append a single line to `dealdrop/.gitignore` immediately after the `.env*` entry (current line 34) to re-include `.env.example`. The file already has an `# env files (can opt-in for committing if needed)` comment above `.env*` on line 33 — insert the whitelist after line 34.

Patch the file so it contains (context — lines 33-35):
```
# env files (can opt-in for committing if needed)
.env*
!.env.example
```

Do NOT:
- Remove the `.env*` glob — it must still ignore `.env.local` and any future `.env.production.local`.
- Reorder any other sections.
- Add `!.env.example.local` or other variants.

Rationale (01-PATTERNS.md): without the whitelist, `git add dealdrop/.env.example` in Task 6 would be a silent no-op because of the `.env*` glob. The explicit `!.env.example` line documents intent.
  </action>
  <verify>
    <automated>grep -q '^!\.env\.example$' dealdrop/.gitignore && grep -q '^\.env\*$' dealdrop/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^!\.env\.example$' dealdrop/.gitignore` returns 1
    - `grep -c '^\.env\*$' dealdrop/.gitignore` returns 1 (the glob is NOT removed — local env still ignored)
    - `grep -c '^/node_modules$' dealdrop/.gitignore` returns 1 (unrelated rules preserved)
    - `cd dealdrop && git check-ignore -v .env.example; echo exit=$?` exits nonzero (file is NOT ignored)
    - `cd dealdrop && git check-ignore -v .env.local; echo exit=$?` exits 0 (file IS still ignored)
  </acceptance_criteria>
  <done>`.gitignore` preserves the `.env*` glob and adds `!.env.example` so the committed template is tracked while `.env.local` stays ignored.</done>
</task>

<task type="auto">
  <name>Task 6: Create .env.example and .env.local with 7 required variables</name>
  <files>dealdrop/.env.example, dealdrop/.env.local</files>
  <read_first>
    - .planning/phases/01-foundation-database/01-RESEARCH.md § "Code Examples — .env.example"
    - .planning/phases/01-foundation-database/01-PATTERNS.md § "dealdrop/.env.example" and "dealdrop/.env.local"
    - .planning/phases/01-foundation-database/01-CONTEXT.md § Decisions D-04
  </read_first>
  <action>
Create TWO files at `dealdrop/`. Both contain exactly the same 7 variable names in the same order. `.env.example` has empty values (committed template). `.env.local` has placeholder values that pass Zod validation so `npm run build` succeeds locally before the developer pastes real Supabase keys in Plan 03.

**File 1: `dealdrop/.env.example` (committed, empty values):**
```bash
# Supabase — public, browser-exposed
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-only (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=

# Phase 3 — scraping
FIRECRAWL_API_KEY=

# Phase 6 — email + cron
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CRON_SECRET=
```

**File 2: `dealdrop/.env.local` (gitignored, placeholder values that satisfy the Zod schema added in Plan 02):**
```bash
# Supabase — public, browser-exposed
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-anon-key-replace-in-plan-03

# Supabase — server-only (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=placeholder-service-role-key-replace-in-plan-03

# Phase 3 — scraping
FIRECRAWL_API_KEY=placeholder-firecrawl-key-replace-in-phase-3

# Phase 6 — email + cron
RESEND_API_KEY=placeholder-resend-key-replace-in-phase-6
RESEND_FROM_EMAIL=alerts@placeholder.dealdrop.local
CRON_SECRET=placeholder-cron-secret-minimum-32-characters-long-ok
```

Critical constraints (from 01-RESEARCH.md § "Pattern 2" + "Pitfalls"):
- `NEXT_PUBLIC_SUPABASE_URL` must start with `https://` to satisfy `z.string().url()` in Plan 02.
- `RESEND_FROM_EMAIL` must be a parseable email (`z.string().email()`).
- `CRON_SECRET` must be at least 32 chars (`z.string().min(32)`); the placeholder above is 48 chars.
- `SUPABASE_SERVICE_ROLE_KEY` MUST NOT have the `NEXT_PUBLIC_` prefix — that would inline it into the browser bundle (Pitfall #4).
- Do NOT add extra variables beyond the 7 listed; Zod `runtimeEnv` in Plan 02 enumerates exactly these.

Developer note (for the SUMMARY.md, not the files themselves): the placeholder values let `npm run build` succeed in CI/local before a real Supabase project exists. Plan 03 will replace them with real values from the Supabase Dashboard. The placeholders are harmless — no network call is wired up in Phase 1 beyond a debug Server Component (Plan 04) that would simply return 0 counts against an unreachable host or fail with a clear message.
  </action>
  <verify>
    <automated>test -f dealdrop/.env.example && test -f dealdrop/.env.local && grep -c '^NEXT_PUBLIC_SUPABASE_URL=' dealdrop/.env.example && grep -c '^NEXT_PUBLIC_SUPABASE_ANON_KEY=' dealdrop/.env.example && grep -c '^SUPABASE_SERVICE_ROLE_KEY=' dealdrop/.env.example && grep -c '^FIRECRAWL_API_KEY=' dealdrop/.env.example && grep -c '^RESEND_API_KEY=' dealdrop/.env.example && grep -c '^RESEND_FROM_EMAIL=' dealdrop/.env.example && grep -c '^CRON_SECRET=' dealdrop/.env.example && grep -E '^CRON_SECRET=.{32,}' dealdrop/.env.local</automated>
  </verify>
  <acceptance_criteria>
    - `dealdrop/.env.example` exists
    - `dealdrop/.env.local` exists
    - `.env.example` contains all 7 variable names with `=` and NO values after `=` (e.g. `grep -c '^NEXT_PUBLIC_SUPABASE_URL=$' dealdrop/.env.example` returns 1)
    - `.env.local` contains all 7 variable names with non-empty values after `=`
    - `.env.local` `NEXT_PUBLIC_SUPABASE_URL` value starts with `https://` (grep `^NEXT_PUBLIC_SUPABASE_URL=https://` returns 1)
    - `.env.local` `CRON_SECRET` value is at least 32 characters (regex `^CRON_SECRET=.{32,}$` matches)
    - `.env.local` `RESEND_FROM_EMAIL` value contains `@` (regex `^RESEND_FROM_EMAIL=.*@.*\..*` matches)
    - `grep -c 'NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY' dealdrop/.env.local` returns 0 (no public prefix on service role)
    - `grep -c 'NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY' dealdrop/.env.example` returns 0
    - `cd dealdrop && git status --porcelain .env.example` shows `?? .env.example` or `A  .env.example` (tracked)
    - `cd dealdrop && git check-ignore .env.local; echo exit=$?` returns exit 0 (ignored)
  </acceptance_criteria>
  <done>Both env files exist. Committed template has the 7 required var names with blank values. Local file has placeholder values that will satisfy Zod validation when Plan 02's env schema lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git repository ↔ developer machine | `.env.local` must NOT cross this boundary (gitignored); `.env.example` crosses (no secret values). |
| Next.js server ↔ browser bundle | Server-only env vars (`SUPABASE_SERVICE_ROLE_KEY`, `FIRECRAWL_API_KEY`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `CRON_SECRET`) must not be prefixed `NEXT_PUBLIC_`. Enforced in env schema (Plan 02); this plan only sets up the file contracts. |
| Next.js image optimizer ↔ external retailer domains | `images.remotePatterns` wildcard allows any origin; image optimizer ingests, resizes, caches server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.local` committed to git | mitigate | `.gitignore` keeps `.env*` glob; `!.env.example` whitelist is single-file and explicit. `git check-ignore .env.local` verified in Task 5 acceptance. |
| T-01-02 | Information Disclosure | Real service-role key inlined into browser bundle via wrong prefix | mitigate | Task 6 acceptance asserts zero grep matches for `NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY` in both env files. Reinforced by `server-only` on admin client in Plan 02. |
| T-01-03 | Tampering | Weak CRON_SECRET placeholder used in production | accept (low, Phase 1 only) | Placeholder is 48 chars — above the 32-char Zod floor. Plan 03 replaces with `openssl rand -hex 32` output; Phase 7 deploy task re-verifies the Vercel env. |
| T-01-04 | Information Disclosure (SSRF) | Permissive `images.remotePatterns` wildcard proxies arbitrary external URLs | accept (portfolio scope) | RESEARCH.md documents Next.js 16 image optimizer blocks private IPs internally; Phase 7 hardening can tighten allowlist. CONTEXT.md § "Specific Ideas" justifies permissive v1. |
| T-01-05 | Denial of Service | Missing env var causes server to start and fail mid-request | mitigate | Plan 02's Zod schema throws at import time; `.env.local` placeholder values ensure build passes until real keys arrive. |
</threat_model>

<verification>
After all six tasks complete, run from the repo root:

```bash
cd dealdrop
npx tsc --noEmit                 # existing scaffold still type-checks with expanded paths
npm run lint                      # ESLint CLI runs (FND-07 spot-check)
grep -q 'DealDrop' app/layout.tsx # metadata present
grep -q 'remotePatterns' next.config.ts # image config present
grep -q '!.env.example' .gitignore # whitelist present
git check-ignore .env.local; echo "local ignored: exit=$?"  # expect exit 0
git check-ignore .env.example; echo "example NOT ignored: exit=$?" # expect exit 1
```

`npm run build` is NOT expected to succeed at end of this plan — the Zod env schema lands in Plan 02 (Wave 1). Build-time validation negative test is the Phase Gate in Plan 04.
</verification>

<success_criteria>
- All 6 tasks pass their acceptance criteria
- `dealdrop/package.json` contains all 5 runtime deps + 1 dev dep
- `dealdrop/tsconfig.json` `paths."@/*"` maps to both `./*` and `./src/*`
- `dealdrop/app/layout.tsx` metadata reads DealDrop; Geist fonts + RootLayout preserved
- `dealdrop/next.config.ts` has `images.remotePatterns` with https + http wildcards
- `dealdrop/.env.example` exists, committed (git-tracked), has 7 var names with empty values
- `dealdrop/.env.local` exists, gitignored, has 7 var names with Zod-valid placeholder values
- `dealdrop/.gitignore` contains `!.env.example` line
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database/01-01-SUMMARY.md` documenting:
- Packages installed (exact versions resolved in package-lock.json)
- Env var names documented in `.env.example`
- Placeholder values used in `.env.local` (note they are Zod-valid, real values come in Plan 03)
- Any deviations from this plan
</output>
