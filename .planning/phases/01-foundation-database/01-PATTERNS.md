# Phase 1: Foundation & Database - Pattern Map

**Mapped:** 2026-04-18
**Files analyzed:** 21 (12 new, 9 modified/touched)
**Analogs found:** 6 in-repo analogs / 15 reference-implementation analogs (all new-category files)
**Greenfield note:** Phase 1 is foundation-setup in a near-empty Next.js scaffold. Most new files have NO in-repo analog — they establish first-of-kind patterns. For those, the "analog" is a verified code example inside `01-RESEARCH.md`. The planner MUST cite both the file path to create AND the RESEARCH.md section the excerpt comes from.

## File Classification

| New/Modified File | New/Mod | Role | Data Flow | In-Repo Analog | Match Quality | Source of Pattern |
|-------------------|---------|------|-----------|----------------|---------------|-------------------|
| `dealdrop/package.json` | MODIFY | config (deps + scripts) | n/a | `dealdrop/package.json` (self) | exact (self) | RESEARCH.md § "Installation" + "package.json lint script" |
| `dealdrop/tsconfig.json` | MODIFY | config (paths) | n/a | `dealdrop/tsconfig.json` (self) | exact (self) | RESEARCH.md § "Pitfall 9" + "Recommended Project Structure" |
| `dealdrop/next.config.ts` | MODIFY | config (images) | request-response (image proxy) | `dealdrop/next.config.ts` (self, minimal) | exact (self) | RESEARCH.md § "Pattern 6: next.config.ts — images.remotePatterns" |
| `dealdrop/app/layout.tsx` | MODIFY | meta (SEO + root HTML) | render | `dealdrop/app/layout.tsx` (self) | exact (self) | RESEARCH.md § "Pattern 7: Metadata Replacement" |
| `dealdrop/app/globals.css` | MODIFY | styling (theme tokens) | n/a | `dealdrop/app/globals.css` (self) | exact (self) | RESEARCH.md § "Pattern 5: Shadcn UI v4 Init — app/globals.css after init" |
| `dealdrop/eslint.config.mjs` | KEEP | config (lint) | n/a | `dealdrop/eslint.config.mjs` (self) | exact (self, unchanged) | No change needed per CONTEXT.md |
| `dealdrop/.gitignore` | KEEP | config (VCS) | n/a | `dealdrop/.gitignore` (self, already ignores `.env*`) | exact (self, unchanged) | No change needed |
| `dealdrop/.env.local` | NEW | config (secrets, local only) | n/a | none (first of kind) | no analog | RESEARCH.md § ".env.example" (value-filled variant) |
| `dealdrop/.env.example` | NEW | config (env template, committed) | n/a | none | no analog | RESEARCH.md § "Code Examples — .env.example" |
| `dealdrop/src/lib/env.ts` | NEW | schema (runtime env validation) | build-time validation | none | no analog | RESEARCH.md § "Pattern 2: @t3-oss/env-nextjs Validated Env Schema" |
| `dealdrop/src/lib/supabase/server.ts` | NEW | client factory (RSC + Server Actions) | request-response (cookie-scoped) | none | no analog | RESEARCH.md § "Pattern 1: Three-Client Supabase Factory — server.ts" |
| `dealdrop/src/lib/supabase/browser.ts` | NEW | client factory (Client Components) | request-response (browser) | none | no analog | RESEARCH.md § "Pattern 1: Three-Client Supabase Factory — browser.ts" |
| `dealdrop/src/lib/supabase/admin.ts` | NEW | client factory (service-role, server-only) | admin/batch | none | no analog | RESEARCH.md § "Pattern 1: Three-Client Supabase Factory — admin.ts" |
| `dealdrop/src/lib/utils.ts` | NEW (auto) | utility (cn() class merger) | n/a | none | no analog | Generated by `npx shadcn@latest init` |
| `dealdrop/src/types/database.ts` | NEW (auto) | types (generated DB row types) | n/a | none | no analog | Generated by `npx supabase gen types typescript --linked` |
| `dealdrop/proxy.ts` | NEW | middleware-like (session refresh, Node runtime) | request-response (every request) | none | no analog | RESEARCH.md § "Pattern 3: Next.js 16 proxy.ts (Phase 1 Stub)" |
| `dealdrop/components.json` | NEW (auto) | config (Shadcn) | n/a | none | no analog | Generated by `npx shadcn@latest init`; fields locked in RESEARCH.md § "Pattern 5" |
| `dealdrop/components/ui/button.tsx` | NEW (auto) | UI primitive | render | none | no analog | Generated by `npx shadcn@latest add button` |
| `dealdrop/supabase/config.toml` | NEW (auto) | config (Supabase local) | n/a | none | no analog | Generated by `npx supabase init` |
| `dealdrop/supabase/migrations/0001_init_schema.sql` | NEW | schema (DDL) | transactional DDL | none | no analog | RESEARCH.md § "Pattern 4 — 0001_init_schema.sql" |
| `dealdrop/supabase/migrations/0002_enable_rls.sql` | NEW | schema (RLS policies) | transactional DDL | none | no analog | RESEARCH.md § "Pattern 4 — 0002_enable_rls.sql" |
| `dealdrop/supabase/migrations/0003_enable_extensions.sql` | NEW | schema (extensions) | transactional DDL | none | no analog | RESEARCH.md § "Pattern 4 — 0003_enable_extensions.sql" |

---

## Pattern Assignments

### `dealdrop/package.json` (config — deps + scripts) — MODIFY

**In-repo state (to modify):** `/Users/harshithpendyala/Documents/DealDrop/dealdrop/package.json`
**Current lines (1-26):** scripts block already has `"lint": "eslint"` (Next.js 16–correct, FND-07 already satisfied). Only `next`/`react`/`react-dom` in `dependencies`; Tailwind/TypeScript/ESLint in `devDependencies`.

**Changes required:**

1. Add app deps (runtime):
   ```bash
   # Run from inside dealdrop/
   npm install @supabase/supabase-js @supabase/ssr @t3-oss/env-nextjs zod server-only
   ```
   Source: RESEARCH.md § "Standard Stack — Core" (versions verified 2026-04-18).

2. Add dev dep:
   ```bash
   npm install -D supabase
   ```
   Source: RESEARCH.md § "Standard Stack — Development / Tooling".

3. Optionally add a `gen:types` script (recommended by CONTEXT.md D-03 / discretion note):
   ```json
   "scripts": {
     "dev": "next dev",
     "build": "next build",
     "start": "next start",
     "lint": "eslint",
     "gen:types": "supabase gen types typescript --linked > src/types/database.ts"
   }
   ```
   Source: RESEARCH.md § "Code Examples — package.json lint script".

4. `lint: "eslint"` line is **already correct** — do NOT change to `next lint` (removed in v16, RESEARCH.md § "Pitfall 0" / "State of the Art"). Do NOT change to `eslint .` unless ESLint flat-config defaults are insufficient — current form matches scaffold.

5. UI deps (`lucide-react`, `sonner`) will be installed automatically by `npx shadcn@latest init` / `add` — do NOT pre-install.

**Reference versions (for pinning or verification):**

| Package | Target range |
|---------|--------------|
| `@supabase/supabase-js` | `^2.103.3` |
| `@supabase/ssr` | `^0.10.2` |
| `@t3-oss/env-nextjs` | `^0.13.11` |
| `zod` | `^4.3.6` |
| `server-only` | `^0.0.1` |
| `supabase` (dev) | `^2.92.1` |

---

### `dealdrop/tsconfig.json` (config — path aliases) — MODIFY

**In-repo state:** `/Users/harshithpendyala/Documents/DealDrop/dealdrop/tsconfig.json` lines 21-23:
```json
"paths": {
  "@/*": ["./*"]
}
```

**Change required (RESEARCH.md § "Pitfall 9: `@/*` path alias doesn't reach `src/`"):**

Update to allow `@/*` to resolve into both `./` (for `app/`, `components/`) and `./src/*` (for `lib/`, `types/`):

```json
"paths": {
  "@/*": ["./*", "./src/*"]
}
```

Keep every other compilerOption. Keep `strict: true`, `target: "ES2017"`, `moduleResolution: "bundler"`.

**Why:** CONTEXT.md D-03 locks `dealdrop/src/lib/*` and `dealdrop/src/types/database.ts`. Without this path, `import { env } from '@/lib/env'` inside `src/lib/supabase/server.ts` fails to resolve. Every Supabase client file assumes this alias works.

---

### `dealdrop/next.config.ts` (config — image optimizer allowlist) — MODIFY

**In-repo state:** `/Users/harshithpendyala/Documents/DealDrop/dealdrop/next.config.ts` is 8 lines, no `images` block.

**Replace entire file with pattern from RESEARCH.md § "Pattern 6":**

```typescript
// Source: node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    // Permissive allowlist — scraped product images come from any e-commerce domain.
    // Hardening (strict allowlist) is a Phase 7 concern.
    remotePatterns: [
      { protocol: 'https', hostname: '**' },
      { protocol: 'http',  hostname: '**' }, // some legacy retailers still serve HTTP images
    ],
  },
}

export default nextConfig
```

**Gotchas (RESEARCH.md § "State of the Art" + "Pitfalls"):**
- Do NOT use `images.domains: [...]` — deprecated in v15, confirmed in v16.
- Keep TypeScript import style `import type` (matches existing convention from scaffold).
- Preserve the default export shape exactly — Next.js config only reads `export default`.

---

### `dealdrop/app/layout.tsx` (meta — SEO + root HTML) — MODIFY

**In-repo state:** `/Users/harshithpendyala/Documents/DealDrop/dealdrop/app/layout.tsx` lines 15-18:
```typescript
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
```

**Change required (RESEARCH.md § "Pattern 7: Metadata Replacement" + FND-08):**

Replace only the `metadata` export (lines 15-18). Keep the Geist font wiring (lines 5-13) and the `RootLayout` function (lines 20-33) exactly as they are — CONTEXT.md § "Reusable Assets" says "replace metadata only, don't rebuild."

```typescript
export const metadata: Metadata = {
  title: "DealDrop — Universal Price Tracker",
  description: "Track products from any e-commerce site. Get email alerts the moment the price drops.",
};
```

**Do NOT (in Phase 1):**
- Add `<Toaster />` or any Sonner wiring — deferred to Phase 7 per RESEARCH.md § "Standard Stack — Supporting (UI toolkit)".
- Add `next-themes` `<ThemeProvider>` — CONTEXT.md discretion chose CSS-media-query path (no JS, no FOUC).
- Change the `<html>` or `<body>` class lists — Shadcn init may append `bg-background text-foreground` to body (RESEARCH.md § "Pattern 5 — globals.css after init `@layer base`"); coordinate with globals.css changes but keep the HTML structure.

---

### `dealdrop/app/globals.css` (styling — theme tokens) — MODIFY via `shadcn init`

**In-repo state:** `/Users/harshithpendyala/Documents/DealDrop/dealdrop/app/globals.css` is 26 lines: `@import "tailwindcss"`, two hex CSS vars `--background`/`--foreground`, a tiny `@theme inline` block, `prefers-color-scheme` dark override, and a body rule.

**Change required (RESEARCH.md § "Pattern 5: Shadcn UI v4 Init — app/globals.css after init"):**

`npx shadcn@latest init` will **overwrite/augment** this file. The output will include:
- `@import "tailwindcss";`
- Optional `@import "tw-animate-css";`
- `@custom-variant dark (&:is(.dark *));`
- `@theme inline { … full token list … }` — OKLCH palette for zinc base
- `:root { --radius: 0.5rem; --background: …; --foreground: …; --primary: …; … }`
- `.dark { --background: …; … }`
- `@layer base { * { @apply border-border outline-ring/50; } body { @apply bg-background text-foreground; } }`

**Merge decision locked by CONTEXT.md discretion:**

For "system preference, no toggle" (no `next-themes`, no JS): wrap the Shadcn-generated `.dark` block in a `prefers-color-scheme` media query:

```css
@media (prefers-color-scheme: dark) {
  :root {
    /* paste the contents of Shadcn's `.dark { … }` token overrides here */
  }
}
```

Source: RESEARCH.md § "Pattern 5 — Existing globals.css merge concern" (option 3 of 3). Rationale: no JS, no FOUC, matches Shadcn init defaults, matches current scaffold's approach to dark mode.

**Pre-flight checklist:**
1. Back up existing `globals.css` before running `shadcn init`.
2. Run `shadcn init` with the locked choices (new-york / zinc / `cssVariables: true` / keep alias defaults).
3. After init, wrap `.dark { … }` in `@media (prefers-color-scheme: dark)` or keep the `.dark` class selector if a future toggle is likely (but CONTEXT.md says no toggle in v1).
4. Preserve `--font-geist-sans` / `--font-geist-mono` tokens if Shadcn overwrote them — layout.tsx still sets those CSS custom properties on `<html>`.

---

### `dealdrop/eslint.config.mjs` (config — lint) — KEEP UNCHANGED

**In-repo state:** `/Users/harshithpendyala/Documents/DealDrop/dealdrop/eslint.config.mjs` lines 1-18 is a flat-config using `defineConfig` from `eslint/config`, extending `eslint-config-next/core-web-vitals` and `eslint-config-next/typescript`, with `globalIgnores([...])`.

**Action: none.** No Phase 1 requirement touches this file. Leave as is. If Shadcn components later produce lint warnings, defer to Phase 7 polish.

---

### `dealdrop/.gitignore` — KEEP UNCHANGED

**In-repo state:** `/Users/harshithpendyala/Documents/DealDrop/dealdrop/.gitignore` already ignores `.env*` (line 34). `.env.local` will NOT be committed. `.env.example` is committed because the gitignore line specifically globs `.env*` — need to verify `.env.example` is either whitelisted or committed via explicit `git add -f`.

**Action required at task level (not a file modification):** When committing `.env.example`, either:
- Use `git add -f dealdrop/.env.example`, OR
- Add an explicit un-ignore: `!.env.example` to `.gitignore` (prefer this; less surprising).

If planner chooses the un-ignore path, add one line after line 34:
```
!.env.example
```

---

### `dealdrop/.env.local` (config — local secrets, NOT committed) — NEW, no analog

**Source pattern:** RESEARCH.md § "Code Examples — .env.example" (same structure, with real values filled in).

**Content template (7 vars total, per FND-02 and RESEARCH.md § "Pattern 2"):**

```bash
# Supabase — public, browser-exposed
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key from Supabase Dashboard>

# Supabase — server-only (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=<service-role key from Supabase Dashboard>

# Phase 3 — scraping
FIRECRAWL_API_KEY=<Firecrawl dashboard key>

# Phase 6 — email + cron
RESEND_API_KEY=<Resend dashboard key>
RESEND_FROM_EMAIL=alerts@<your-verified-domain>
CRON_SECRET=<openssl rand -hex 32>
```

**Critical (RESEARCH.md § "Pitfall 4 + Pitfall 7"):**
- `SUPABASE_SERVICE_ROLE_KEY` MUST NOT have `NEXT_PUBLIC_` prefix. If you prefix it, Next.js inlines it into the browser bundle.
- All 7 vars must be set before `npm run build` — `@t3-oss/env-nextjs` throws at build if any is missing.
- `CRON_SECRET` min-length 32 chars is enforced by the Zod schema.

---

### `dealdrop/.env.example` (config — committed template) — NEW, no analog

**Source pattern:** RESEARCH.md § "Code Examples — .env.example" (lines 813-830 of 01-RESEARCH.md, reproduced exactly — values blank):

```bash
# Supabase — public, browser-exposed
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-only (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=

# Phase 3 — scraping
FIRECRAWL_API_KEY=

# Phase 6 — email + cron
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CRON_SECRET=
```

**Rule:** Empty values only — never commit real keys. Committed to document var names for future contributors / Vercel env-var setup in Phase 7.

---

### `dealdrop/src/lib/env.ts` (schema — env validation) — NEW, no analog

**Source pattern:** RESEARCH.md § "Pattern 2: @t3-oss/env-nextjs Validated Env Schema" (full code example in RESEARCH.md lines 349-381).

**Full file content to create (copy verbatim from RESEARCH.md):**

```typescript
// Source: https://env.t3.gg/docs/nextjs
import { createEnv } from '@t3-oss/env-nextjs'
import { z } from 'zod'

export const env = createEnv({
  server: {
    SUPABASE_SERVICE_ROLE_KEY: z.string().min(1),
    FIRECRAWL_API_KEY: z.string().min(1),
    RESEND_API_KEY: z.string().min(1),
    RESEND_FROM_EMAIL: z.string().email(),
    CRON_SECRET: z.string().min(32), // enforce length to discourage weak secrets
  },
  client: {
    NEXT_PUBLIC_SUPABASE_URL: z.string().url(),
    NEXT_PUBLIC_SUPABASE_ANON_KEY: z.string().min(1),
  },
  runtimeEnv: {
    SUPABASE_SERVICE_ROLE_KEY: process.env.SUPABASE_SERVICE_ROLE_KEY,
    FIRECRAWL_API_KEY: process.env.FIRECRAWL_API_KEY,
    RESEND_API_KEY: process.env.RESEND_API_KEY,
    RESEND_FROM_EMAIL: process.env.RESEND_FROM_EMAIL,
    CRON_SECRET: process.env.CRON_SECRET,
    NEXT_PUBLIC_SUPABASE_URL: process.env.NEXT_PUBLIC_SUPABASE_URL,
    NEXT_PUBLIC_SUPABASE_ANON_KEY: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
  },
  skipValidation: !!process.env.SKIP_ENV_VALIDATION,
  emptyStringAsUndefined: true,
})
```

**Consumed by:** every file that used to read `process.env.*`. In Phase 1 that's `src/lib/supabase/server.ts`, `src/lib/supabase/browser.ts`, `src/lib/supabase/admin.ts`. All three must `import { env } from '@/lib/env'` rather than touching `process.env` directly.

**Gotchas (RESEARCH.md § "Pitfall 7: Missing env var silently caught"):**
- The `runtimeEnv` object must enumerate every var by explicit key. Do NOT try `runtimeEnv: process.env` — Next.js inlines `NEXT_PUBLIC_*` at build time; the library must see each key as a static reference.
- Zod v4 is the locked version (RESEARCH.md § "Standard Stack"). Import as `import { z } from 'zod'` — API for `.string()`, `.email()`, `.url()`, `.min()` is compatible with v3 for these simple schemas.
- `emptyStringAsUndefined: true` treats `SOMEVAR=` (no value) as missing — matches `.env.example` semantics.

---

### `dealdrop/src/lib/supabase/server.ts` (client factory — RSC/Server Actions) — NEW, no analog

**Source pattern:** RESEARCH.md § "Pattern 1: Three-Client Supabase Factory — server.ts" (lines 271-301 of 01-RESEARCH.md).

**Full file content:**

```typescript
// Source: https://supabase.com/docs/guides/auth/server-side/nextjs (adapted for Next.js 16 async cookies)
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { env } from '@/lib/env'

export async function createClient() {
  const cookieStore = await cookies() // Next.js 16: MUST await — sync access removed

  return createServerClient(
    env.NEXT_PUBLIC_SUPABASE_URL,
    env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // Called from a Server Component — setting cookies is not allowed.
            // The proxy.ts session refresh handles the actual cookie writes.
          }
        },
      },
    }
  )
}
```

**Critical (RESEARCH.md § "Pitfall 2: Synchronous cookies/headers"):**
- Must be `async function` + `await cookies()`. v15 tutorials skipping `await` will break at first request.
- Must use `@supabase/ssr` `createServerClient`, NOT `@supabase/auth-helpers-nextjs` (deprecated, RESEARCH.md § "Pitfall 5").
- Imports `env` from `@/lib/env` — DO NOT read `process.env.*` directly here.

---

### `dealdrop/src/lib/supabase/browser.ts` (client factory — Client Components) — NEW, no analog

**Source pattern:** RESEARCH.md § "Pattern 1: Three-Client Supabase Factory — browser.ts" (lines 307-317 of 01-RESEARCH.md).

**Full file content:**

```typescript
// Source: https://supabase.com/docs/guides/auth/server-side/creating-a-client
import { createBrowserClient } from '@supabase/ssr'
import { env } from '@/lib/env'

export function createClient() {
  return createBrowserClient(
    env.NEXT_PUBLIC_SUPABASE_URL,
    env.NEXT_PUBLIC_SUPABASE_ANON_KEY
  )
}
```

**Why no `server-only` here:** This IS meant for Client Components. Only `NEXT_PUBLIC_*` vars are referenced, so no secret leakage risk.

**Caller pattern (informational):** Phase 2 auth modal will call `createClient()` from this file inside a `"use client"` component to run `supabase.auth.signInWithOAuth(...)`.

---

### `dealdrop/src/lib/supabase/admin.ts` (client factory — service-role) — NEW, no analog

**Source pattern:** RESEARCH.md § "Pattern 1: Three-Client Supabase Factory — admin.ts" (lines 323-339 of 01-RESEARCH.md).

**Full file content:**

```typescript
// Source: https://nextjs.org/docs/app/guides/data-security (server-only DAL pattern)
import 'server-only' // MUST be the first line — throws at bundle time if imported into a Client Component
import { createClient } from '@supabase/supabase-js'
import { env } from '@/lib/env'

export function createAdminClient() {
  return createClient(
    env.NEXT_PUBLIC_SUPABASE_URL,
    env.SUPABASE_SERVICE_ROLE_KEY, // server-only env var, never NEXT_PUBLIC_
    {
      auth: {
        autoRefreshToken: false,
        persistSession: false,
      },
    }
  )
}
```

**Critical (RESEARCH.md § "Pitfall 4: SERVICE_ROLE_KEY leak into client bundle"):**
- Line 1 MUST be `import 'server-only'` — no imports above it. This converts any accidental Client Component import graph into a build error.
- Uses `createClient` from **`@supabase/supabase-js`**, NOT from `@supabase/ssr` — admin has no cookies to juggle.
- `autoRefreshToken: false` + `persistSession: false` — admin is stateless; no cookie or storage adapter.
- Imports `env.SUPABASE_SERVICE_ROLE_KEY` — it's in the `server:` partition of `env.ts`, so accessing it from Client Component code would fail anyway.

---

### `dealdrop/src/lib/utils.ts` (utility — cn() helper) — NEW (auto-generated)

**Source pattern:** Auto-generated by `npx shadcn@latest init`. DO NOT hand-write.

After init, this file will contain (Shadcn canonical):

```typescript
import { type ClassValue, clsx } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

**Notes:**
- Shadcn will also `npm install clsx tailwind-merge` on init. No extra install needed.
- Every Shadcn UI component imports `cn` from `@/lib/utils`. If tsconfig `@/*` path isn't updated to include `./src/*`, Shadcn will write to `./lib/utils.ts` at root instead — CONTEXT.md D-03 locks the `src/lib/` path, so the tsconfig fix (see tsconfig section above) MUST land first, or the `components.json` `aliases.utils` must be set to whatever path Shadcn chose. Easier: update tsconfig first.

---

### `dealdrop/src/types/database.ts` (types — generated Supabase types) — NEW (auto-generated)

**Source pattern:** Generated by `npx supabase gen types typescript --linked > src/types/database.ts`.

**Workflow (from RESEARCH.md § "Supabase CLI workflow"):**

```bash
cd dealdrop
npx supabase init
npx supabase link --project-ref <project-ref>
# After migrations/0001..0003 created and pushed:
npx supabase db push
npx supabase gen types typescript --linked > src/types/database.ts
```

**The generated file** exports a large `Database` type with `public.Tables.products` and `public.Tables.price_history` entries (each with `Row`, `Insert`, `Update` variants).

**Commit policy (CONTEXT.md discretion):** generated file is committed to git and regenerated manually after any migration. No git hook, no CI automation.

**Dependency:** Must run AFTER migrations 0001-0003 are pushed to the linked Supabase project. If run before, types will be empty (`public.Tables = { [k: string]: never }`).

---

### `dealdrop/proxy.ts` (middleware-like — Node runtime request proxy) — NEW, no analog

**Source pattern:** RESEARCH.md § "Pattern 3: Next.js 16 proxy.ts (Phase 1 Stub)" (lines 394-412 of 01-RESEARCH.md).

**Full file content (Phase 1 no-op pass-through):**

```typescript
// Source: node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export async function proxy(request: NextRequest) {
  // Phase 2 will: create Supabase client bound to request/response cookies,
  // call supabase.auth.getClaims() to refresh the session, and propagate
  // Set-Cookie headers to the response.
  return NextResponse.next()
}

export const config = {
  matcher: [
    // Exclude static assets and Next.js internals
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:png|jpg|jpeg|gif|webp|svg)$).*)',
  ],
}
```

**Critical (RESEARCH.md § "Pitfall 1: Deprecated middleware.ts"):**
- Filename MUST be `proxy.ts` (at the same level as `app/`, NOT in `app/`). `middleware.ts` is silently ignored in v16.
- Exported function MUST be named `proxy` (not `middleware`).
- Runtime is Node.js — `edge` runtime is NOT supported here.
- `request` parameter type is `NextRequest`; return type is `NextResponse`.
- Phase 2 will replace the body with Supabase `getClaims()`-based session refresh (NOT `getUser()`, per RESEARCH.md § "State of the Art").

---

### `dealdrop/components.json` (config — Shadcn) — NEW (auto-generated)

**Source pattern:** Written by `npx shadcn@latest init`. CONTEXT.md discretion locks these values (RESEARCH.md § "Pattern 5 — components.json resulting fields"):

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "app/globals.css",
    "baseColor": "zinc",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

**Init prompts — answer with these values:**

| Prompt | Answer |
|--------|--------|
| style | `new-york` |
| base color | `zinc` |
| css variables | `yes` |
| components path | keep default `@/components` |
| utils path | keep default `@/lib/utils` |
| ui path | keep default `@/components/ui` |
| icon library | `lucide` |

**Critical (RESEARCH.md § "Pitfall 6: Shadcn + Tailwind v3 on v4 project"):**
- `tailwind.config` MUST be empty string `""` for Tailwind v4 — v4 uses `@theme` in CSS, not a JS config file.
- Do NOT hand-create `tailwind.config.js` — if it exists after init, delete it.
- The init command needs `tsconfig.json` `@/*` paths updated first (see tsconfig section) — otherwise Shadcn may write to root `./lib/` instead of `./src/lib/`.

---

### `dealdrop/components/ui/button.tsx` (UI primitive) — NEW (auto-generated)

**Source pattern:** Generated by `npx shadcn@latest add button`. DO NOT hand-write.

**Verification after generation (RESEARCH.md § "Shadcn Verification (FND-06)"):**

Drop into a throwaway page to confirm theme tokens compile:

```typescript
import { Button } from '@/components/ui/button'
export default function Test() {
  return <Button variant="default">Test</Button>
}
```

Expected: renders with `bg-primary text-primary-foreground`, rounded per `--radius: 0.5rem`, zinc-based palette.

**Why `button` specifically:** Smallest component that exercises the full `cn()` + CSS-variable + Tailwind-v4 theme pipeline. Phase 2 will add `input`, `dialog`, `dropdown-menu`, etc. as needed.

**Phase 1 may also auto-install `lucide-react` and `clsx`/`tailwind-merge`** — Shadcn does this silently when a component requires them.

---

### `dealdrop/supabase/config.toml` (config — Supabase local CLI) — NEW (auto-generated)

**Source pattern:** Generated by `npx supabase init`. DO NOT hand-write.

**Notes:**
- This file configures the local Supabase stack (ports, Studio URL, auth provider toggles for local dev). Phase 1 may not use `supabase start` locally — remote DB push is sufficient — so `config.toml` can be left with defaults.
- Commit `supabase/config.toml` to git (standard Supabase workflow).
- Do NOT commit `supabase/.temp/` or `supabase/.branches/` (add to `.gitignore` if `supabase init` doesn't add them automatically).

---

### `dealdrop/supabase/migrations/0001_init_schema.sql` (schema — tables + indexes + trigger) — NEW, no analog

**Source pattern:** RESEARCH.md § "Pattern 4 — 0001_init_schema.sql" (lines 424-467 of 01-RESEARCH.md).

**Full migration content (copy verbatim):**

```sql
-- Source: Supabase RLS docs + DealDrop REQUIREMENTS.md DB-01..DB-07
-- Creates the two core tables. RLS is enabled + policies written in 0002.

-- products
create table public.products (
  id            uuid         primary key default gen_random_uuid(),
  user_id       uuid         not null references auth.users(id) on delete cascade,
  url           text         not null,
  name          text         not null,
  current_price numeric      not null,
  currency      text         not null,
  image_url     text,
  created_at    timestamptz  not null default now(),
  updated_at    timestamptz  not null default now(),

  constraint products_user_url_unique unique (user_id, url),
  constraint products_current_price_positive check (current_price > 0)
);

create index products_user_id_idx on public.products (user_id);

-- price_history
create table public.price_history (
  id          uuid         primary key default gen_random_uuid(),
  product_id  uuid         not null references public.products(id) on delete cascade,
  price       numeric      not null check (price > 0),
  currency    text         not null,
  checked_at  timestamptz  not null default now()
);

create index price_history_product_id_idx   on public.price_history (product_id);
create index price_history_checked_at_idx   on public.price_history (checked_at desc);

-- updated_at trigger for products
create or replace function public.set_updated_at()
returns trigger language plpgsql as $$
begin new.updated_at = now(); return new; end;
$$;

create trigger products_set_updated_at
before update on public.products
for each row execute function public.set_updated_at();
```

**Requirement coverage:**
- DB-01: products columns ✓
- DB-02: `products_user_url_unique unique (user_id, url)` ✓
- DB-03: `products_current_price_positive check (current_price > 0)` ✓
- DB-04: price_history columns ✓

**Gotchas (RESEARCH.md § "Pitfall 10"):** `gen_random_uuid()` is available in Supabase by default. If it ever errors on a fresh Postgres, prepend `create extension if not exists pgcrypto;`.

---

### `dealdrop/supabase/migrations/0002_enable_rls.sql` (schema — RLS + policies) — NEW, no analog

**Source pattern:** RESEARCH.md § "Pattern 4 — 0002_enable_rls.sql" (lines 471-526 of 01-RESEARCH.md).

**Full migration content (copy verbatim):**

```sql
-- Source: https://supabase.com/docs/guides/database/postgres/row-level-security
-- Note: (select auth.uid()) is used instead of auth.uid() for per-statement caching.
-- Supabase benchmarks show up to ~95% performance improvement.

alter table public.products      enable row level security;
alter table public.price_history enable row level security;

-- products: owner-only access
create policy "products_select_own"
  on public.products for select
  to authenticated
  using ((select auth.uid()) = user_id);

create policy "products_insert_own"
  on public.products for insert
  to authenticated
  with check ((select auth.uid()) = user_id);

create policy "products_update_own"
  on public.products for update
  to authenticated
  using ((select auth.uid()) = user_id)
  with check ((select auth.uid()) = user_id);

create policy "products_delete_own"
  on public.products for delete
  to authenticated
  using ((select auth.uid()) = user_id);

-- price_history: ownership-chain via products
create policy "price_history_select_own"
  on public.price_history for select
  to authenticated
  using (
    product_id in (
      select id from public.products where user_id = (select auth.uid())
    )
  );

create policy "price_history_insert_own"
  on public.price_history for insert
  to authenticated
  with check (
    product_id in (
      select id from public.products where user_id = (select auth.uid())
    )
  );

-- No UPDATE / DELETE policies for price_history from the user side.
-- Cron Route Handler uses service-role key, bypasses RLS, and handles cron inserts + cleanup.
-- Cascade delete from products handles the cleanup on product removal.
```

**Requirement coverage:**
- DB-05: Four `products` policies (select/insert/update/delete) each gated by `(select auth.uid()) = user_id` ✓
- DB-06: Two `price_history` policies via ownership-chain IN-subquery ✓

**Critical (RESEARCH.md § "Pitfall 3"):**
- This migration MUST be applied in the same `supabase db push` as 0001 — NEVER push 0001 alone and leave policies for a later push. There's a window where tables exist with no RLS and data could leak.
- Use `(select auth.uid())` not bare `auth.uid()` — 95% perf difference at scale.
- `price_history` has NO user-side UPDATE/DELETE policies — that's intentional (cron + cascade handle those cases).

---

### `dealdrop/supabase/migrations/0003_enable_extensions.sql` (schema — pg_cron, pg_net) — NEW, no analog

**Source pattern:** RESEARCH.md § "Pattern 4 — 0003_enable_extensions.sql" (lines 530-537 of 01-RESEARCH.md).

**Full migration content:**

```sql
-- Source: https://supabase.com/docs/guides/database/extensions
-- Phase 6 will use these. Phase 1 enables them so migrations stay linear.

create extension if not exists pg_cron;
create extension if not exists pg_net;
```

**Requirement coverage:**
- FND-04: `pg_cron` + `pg_net` enabled ✓

**Gotchas (RESEARCH.md § "Pitfall 8" + Open Question #1):**
- On Supabase Free tier, SQL-based extension enablement may fail. If it does, fall back to Supabase Dashboard → Database → Extensions → toggle `pg_cron` and `pg_net` on. Either path satisfies FND-04; SQL-in-migration is preferred for reproducibility.
- `pg_net` alone is not enough, and `pg_cron` alone is not enough — BOTH must be enabled, or Phase 6 cron-from-DB HTTP calls will fail.

**Optional defensive add (RESEARCH.md § "Pitfall 10"):** If 0001 has any `gen_random_uuid()` issues in the future, add `create extension if not exists pgcrypto;` at the top of this migration. Current Supabase has it by default — not required, but harmless.

---

## Shared Patterns

Cross-cutting conventions applied across multiple Phase 1 files. Planner should reference these from task <action> fields wherever they apply.

### Shared Pattern 1: Import `env` from `@/lib/env`, never touch `process.env`

**Source file (of pattern):** `dealdrop/src/lib/env.ts` (once created)
**Applies to:** `src/lib/supabase/server.ts`, `src/lib/supabase/browser.ts`, `src/lib/supabase/admin.ts`, and any future file that needs a secret.

**Rule:** No file in `dealdrop/` except `env.ts` itself should reference `process.env.*`.

```typescript
import { env } from '@/lib/env'

// ✓ Correct
env.SUPABASE_SERVICE_ROLE_KEY

// ✗ Forbidden — bypasses build-time validation
process.env.SUPABASE_SERVICE_ROLE_KEY
```

**Enforcement:** Validation covered by RESEARCH.md § "Pitfall 7" — Zod throws at import time if any var is missing.

---

### Shared Pattern 2: `import 'server-only'` on any module touching server-only secrets

**Source file:** `dealdrop/src/lib/supabase/admin.ts` (once created — uses `import 'server-only'` as line 1)
**Applies to:** any file in Phase 1+ that reads `env.SUPABASE_SERVICE_ROLE_KEY`, `env.FIRECRAWL_API_KEY`, `env.RESEND_API_KEY`, or `env.CRON_SECRET`.

**Rule (RESEARCH.md § "Pitfall 4"):**

```typescript
import 'server-only' // first line, before any other import
// ...rest of module
```

**Enforcement:** Bundler throws a build error if the module is reachable from any `"use client"` component's import graph.

---

### Shared Pattern 3: Next.js 16 async Request APIs — always `await cookies()/headers()/params`

**Source file:** `dealdrop/src/lib/supabase/server.ts` (once created — demonstrates `const cookieStore = await cookies()`)
**Applies to:** any Phase 1+ file touching `cookies()`, `headers()`, `params`, `searchParams`.

**Rule (RESEARCH.md § "Pitfall 2" + "State of the Art"):**

```typescript
// ✓ Correct
const cookieStore = await cookies()
const headerList = await headers()
const { id } = await params

// ✗ Forbidden — throws at runtime in Next.js 16
const cookieStore = cookies()
```

**Enforcement:** Next.js 16 removed sync compat; runtime TypeError on first request otherwise.

---

### Shared Pattern 4: Supabase client choice — three clients, never ad-hoc `createClient`

**Source files (once created):**
- Server-side (RSC, Server Actions): `@/lib/supabase/server` → `createClient()` (async)
- Browser (Client Components): `@/lib/supabase/browser` → `createClient()` (sync)
- Admin / cron (service-role): `@/lib/supabase/admin` → `createAdminClient()` (sync, server-only)

**Rule:** Every Supabase call in the app imports from ONE of these three files. Never call `createClient()` from `@supabase/supabase-js` directly in a feature file; never call `createServerClient()` from `@supabase/ssr` anywhere outside these three factory files.

---

### Shared Pattern 5: SQL policy predicate form — `(select auth.uid())` not `auth.uid()`

**Source file:** `dealdrop/supabase/migrations/0002_enable_rls.sql` (once created)
**Applies to:** every RLS policy created in Phase 1+.

**Rule (RESEARCH.md § "State of the Art"):**

```sql
-- ✓ Correct — initPlan cached once per statement
using ((select auth.uid()) = user_id)

-- ✗ Slow — re-evaluates per row
using (auth.uid() = user_id)
```

**Enforcement:** Review all policy CREATE statements for bare `auth.uid()` before `db push`.

---

### Shared Pattern 6: Migration atomicity — RLS in same migration as table

**Source file:** `dealdrop/supabase/migrations/0001_init_schema.sql` + `0002_enable_rls.sql` (pushed together)
**Applies to:** any future migration that creates a user-scoped table.

**Rule (RESEARCH.md § "Pitfall 3"):** When you `supabase db push`, 0001 and 0002 must land in the same push. Do NOT push 0001 alone to a live DB, wait, then push 0002 — there's an exposure window. Either combine into one `.sql` file OR push them together.

---

### Shared Pattern 7: File naming — `proxy.ts` not `middleware.ts`

**Source file:** `dealdrop/proxy.ts` (once created)
**Applies to:** any Next.js 16 request-interception file.

**Rule (RESEARCH.md § "Pitfall 1"):**
- Filename: `proxy.ts` at project root (same level as `app/`).
- Exported function: `export async function proxy(request: NextRequest)` — NOT `middleware`.
- Runtime: Node.js only (no `edge`).

---

### Shared Pattern 8: Path alias expectation — `@/lib/*`, `@/components/*`, `@/types/*`

**Source file:** `dealdrop/tsconfig.json` (once modified to `"@/*": ["./*", "./src/*"]`)
**Applies to:** all new code in Phase 1+.

**Rule:**
- Project code imports use `@/lib/env`, `@/lib/supabase/server`, `@/lib/utils`, `@/components/ui/button`, `@/types/database`.
- Never use relative paths like `../../lib/env` across package boundaries.
- Shadcn `components.json` aliases must match (`@/components`, `@/lib/utils`, etc.).

---

## No Analog Found

**All files in this phase that are "NEW" have no in-repo analog** — Phase 1 is foundation init in a bare scaffold. The planner should NOT search for analogs; instead, for each NEW file, consume the matching RESEARCH.md code example section.

| File | Role | Data Flow | Reason no in-repo analog | Reference |
|------|------|-----------|--------------------------|-----------|
| `dealdrop/.env.local` | config | n/a | no `.env*` in repo yet | RESEARCH.md § "Code Examples — .env.example" |
| `dealdrop/.env.example` | config | n/a | no `.env*` in repo yet | RESEARCH.md § "Code Examples — .env.example" |
| `dealdrop/src/lib/env.ts` | schema | build-time validation | first env schema | RESEARCH.md § "Pattern 2" |
| `dealdrop/src/lib/supabase/server.ts` | client factory | cookie-scoped request-response | first Supabase client | RESEARCH.md § "Pattern 1 — server.ts" |
| `dealdrop/src/lib/supabase/browser.ts` | client factory | browser | first Supabase client | RESEARCH.md § "Pattern 1 — browser.ts" |
| `dealdrop/src/lib/supabase/admin.ts` | client factory | admin/batch | first Supabase client | RESEARCH.md § "Pattern 1 — admin.ts" |
| `dealdrop/src/lib/utils.ts` | utility | n/a | auto-generated by Shadcn init | `npx shadcn@latest init` |
| `dealdrop/src/types/database.ts` | types | n/a | auto-generated by Supabase CLI | `npx supabase gen types typescript --linked` |
| `dealdrop/proxy.ts` | request proxy | request-response | first Next.js 16 proxy | RESEARCH.md § "Pattern 3" |
| `dealdrop/components.json` | config | n/a | auto-generated by Shadcn init | `npx shadcn@latest init` |
| `dealdrop/components/ui/button.tsx` | UI primitive | render | auto-generated by Shadcn add | `npx shadcn@latest add button` |
| `dealdrop/supabase/config.toml` | config | n/a | auto-generated by Supabase CLI | `npx supabase init` |
| `dealdrop/supabase/migrations/0001_init_schema.sql` | schema DDL | transactional DDL | first migration | RESEARCH.md § "Pattern 4 — 0001" |
| `dealdrop/supabase/migrations/0002_enable_rls.sql` | schema DDL | transactional DDL | first migration | RESEARCH.md § "Pattern 4 — 0002" |
| `dealdrop/supabase/migrations/0003_enable_extensions.sql` | schema DDL | transactional DDL | first migration | RESEARCH.md § "Pattern 4 — 0003" |

**All modifies have a self-analog** — the file to modify already exists and is the canonical reference for its own structure. See per-file sections above for exact change sites.

---

## Metadata

**Analog search scope:**
- `/Users/harshithpendyala/Documents/DealDrop/dealdrop/` (full app tree, excluding `node_modules/`, `.next/`)
- Confirmed scanned: `app/`, root config files (`package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `postcss.config.mjs`, `.gitignore`), `public/`

**Files scanned in repo (non-generated):** 9 source/config files
- `dealdrop/package.json`
- `dealdrop/tsconfig.json`
- `dealdrop/next.config.ts`
- `dealdrop/eslint.config.mjs`
- `dealdrop/postcss.config.mjs`
- `dealdrop/.gitignore`
- `dealdrop/app/layout.tsx`
- `dealdrop/app/page.tsx`
- `dealdrop/app/globals.css`

**Files NOT found (confirming greenfield status):**
- No `dealdrop/src/` tree
- No `dealdrop/lib/` directory
- No `dealdrop/supabase/` directory
- No `dealdrop/proxy.ts` or `middleware.ts`
- No `dealdrop/components/` directory
- No `dealdrop/components.json`
- No `.env.local`, `.env.example`
- No migrations, no type files

**Pattern extraction date:** 2026-04-18
**Researcher output used:** `.planning/phases/01-foundation-database/01-RESEARCH.md` (71 KB, 1100+ lines)
**Context used:** `.planning/phases/01-foundation-database/01-CONTEXT.md`

**Key insight for planner:**

Every code excerpt in this PATTERNS.md is already a direct copy from RESEARCH.md. The planner's task <action> fields should reference BOTH:
1. The absolute path of the file to create/modify, AND
2. The section of 01-RESEARCH.md that contains the verified reference implementation.

Do NOT re-derive patterns — all seven research sections ("Pattern 1" through "Pattern 7" in RESEARCH.md) are production-ready, version-verified (as of 2026-04-18), and directly applicable.
