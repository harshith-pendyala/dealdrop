---
phase: 02-authentication-landing
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md
  - .planning/phases/02-authentication-landing/02-SMOKE-TEST.md
autonomous: true
requirements:
  - AUTH-08
  - AUTH-04
  - POL-01

must_haves:
  truths:
    - "REQUIREMENTS.md Traceability table shows POL-01 assigned to Phase 2 (not Phase 7) — D-13 documented in plan artifacts."
    - "REQUIREMENTS.md Traceability table shows AUTH-04 split across Phase 2 (hook) and Phase 4 (trigger) — D-07 documented."
    - "An `AUTH-08-OPS-CHECKLIST.md` document exists in the phase directory with explicit step-by-step URIs the user must register in Google Cloud Console + Supabase Auth Dashboard."
    - "A `02-SMOKE-TEST.md` document exists mirroring the manual smoke checklist from Plan 04 Task 3, formatted so the user can print / tick it off during verification."
    - "Phase 7 scope note in REQUIREMENTS.md documents that POL-01 (Sonner) is handled in Phase 2, so Phase 7 planning does not re-cover it."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated Traceability table reflecting D-07 (AUTH-04 split) and D-13 (POL-01 moved to Phase 2)"
      contains: "POL-01 | Phase 2"
    - path: ".planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md"
      provides: "Step-by-step operator checklist for Google Cloud Console + Supabase Auth redirect URI registration"
      contains: "vhlbdcsxccaknccawfdj"
    - path: ".planning/phases/02-authentication-landing/02-SMOKE-TEST.md"
      provides: "Manual smoke test reference (mirrors Plan 04 Task 3 + adds full check sequence)"
      contains: "Sign in to DealDrop"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/02-authentication-landing/02-CONTEXT.md D-07 + D-13"
      via: "Traceability table rows for AUTH-04 and POL-01"
      pattern: "AUTH-04.*Phase 2.*Phase 4"
    - from: ".planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md"
      to: ".planning/phases/02-authentication-landing/02-RESEARCH.md OAuth Redirect URI Registration section"
      via: "Same URIs, same pattern"
      pattern: "supabase.co/auth/v1/callback"
---

<objective>
Ship Phase 2's documentation deliverables: (1) update `REQUIREMENTS.md` Traceability table to reflect D-07 (AUTH-04 split) + D-13 (POL-01 moved to Phase 2 from Phase 7), (2) produce the AUTH-08 ops checklist so the user can register OAuth redirect URIs in Google Cloud Console and Supabase Auth Dashboard before running the Plan 04 Task 3 smoke test, (3) produce a standalone smoke-test document the user can print and tick off. All three are pure documentation — no code, no builds, no dependencies on other plans.

Purpose: This plan unblocks Plan 04's human-verify checkpoint (which depends on OAuth being configured externally) and closes two planning-context handoff items (D-07 and D-13) so downstream phases inherit a clean traceability table. Running this in Wave 1 (parallel with Plans 01 and 02) means the user can start the AUTH-08 ops work in a browser tab WHILE Plans 01-04 are executing, completely overlapping the external-dependency cost.
Output: 1 updated file + 2 new files, all in `.planning/` — zero changes to `dealdrop/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-landing/02-CONTEXT.md
@.planning/phases/02-authentication-landing/02-RESEARCH.md
@.planning/phases/02-authentication-landing/02-VALIDATION.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md Traceability — AUTH-04 split (D-07) + POL-01 moved (D-13)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state) — the Traceability table at the bottom showing one Phase per requirement
    - .planning/phases/02-authentication-landing/02-CONTEXT.md D-07 — exact wording: "AUTH-04 shows as 'Phase 2 (hook) / Phase 4 (trigger)'"
    - .planning/phases/02-authentication-landing/02-CONTEXT.md D-13 — exact wording: "POL-01 moves from Phase 7 → Phase 2"
    - .planning/ROADMAP.md Phase 7 scope line (currently mentions `POL-01, POL-02, ...`) — Phase 7 planning will later need to know Sonner is already done
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` in-place. Two atomic changes:

    **Change 1 — Traceability table AUTH-04 row** (around line 174 of the current REQUIREMENTS.md):

    Current:
    ```
    | AUTH-04 | Phase 2 | Pending |
    ```

    New:
    ```
    | AUTH-04 | Phase 2 (hook) / Phase 4 (trigger) | Pending | D-07 split — Phase 2 ships `useAuthModal()` + `openAuthModal()` hook; Phase 4 wires Add Product form to call it |
    ```

    Note: This adds a 4th column (Notes) only to this row. If the table header currently has 3 columns `| Requirement | Phase | Status |`, update the header to `| Requirement | Phase | Status | Notes |` and add an empty `|  |` cell (or short context note) to every other row so the markdown table stays valid. If that is too invasive, alternative: add the D-07 note as a footnote paragraph below the table and keep the row itself as `| AUTH-04 | Phase 2 (hook) / Phase 4 (trigger) | Pending |`. Prefer the footnote approach to minimize blast radius. Example footnote:

    ```
    **Notes:**
    - **AUTH-04:** Split across phases per [Phase 2 CONTEXT.md D-07](phases/02-authentication-landing/02-CONTEXT.md). Phase 2 exports `useAuthModal()` + `openAuthModal()` hook infrastructure; Phase 4 wires the Add Product form submit-while-logged-out path to call it. Requirement is considered SATISFIED when both phases complete.
    ```

    **Change 2 — Traceability table POL-01 row** (currently `| POL-01 | Phase 7 | Pending |`):

    Current:
    ```
    | POL-01 | Phase 7 | Pending |
    ```

    New:
    ```
    | POL-01 | Phase 2 | Pending |
    ```

    Add a second footnote paragraph:
    ```
    - **POL-01:** Moved forward from Phase 7 to Phase 2 per [Phase 2 CONTEXT.md D-13](phases/02-authentication-landing/02-CONTEXT.md). Sonner toast infrastructure (`<Toaster />` in root layout + `npx shadcn@latest add sonner`) is installed in Phase 2 because the sign-out flow requires the "Signed out" toast (D-12). Phase 7 Polish scope should exclude Sonner setup.
    ```

    **Phase 2 requirement coverage** — add this row (or update if present) to make this phase's requirements explicit:
    Ensure the traceability table contains all of: AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, AUTH-08 → Phase 2; HERO-01, HERO-02, HERO-03, HERO-04, HERO-05 → Phase 2; POL-01 → Phase 2 (moved). The current file already has AUTH-01..08 and HERO-01..05 pointing at Phase 2 — the only edits are AUTH-04 and POL-01.

    Preserve everything else in REQUIREMENTS.md byte-for-byte:
    - Header + requirement list (FND, DB, AUTH, HERO, TRACK, DASH, CHART, CRON, EMAIL, POL, DEP sections)
    - Deferred + Out-of-Scope sections
    - Coverage footer sentence
    - The "Last updated" date line may be updated to 2026-04-18 with a note like "— AUTH-04 split + POL-01 move documented (Phase 2 CONTEXT)".
  </action>
  <verify>
    <automated>
      grep -q "POL-01 | Phase 2" .planning/REQUIREMENTS.md && ! grep -q "POL-01 | Phase 7" .planning/REQUIREMENTS.md && grep -E "(AUTH-04).*Phase 2.*\(hook\)|AUTH-04.*Phase 2 / Phase 4" .planning/REQUIREMENTS.md && grep -q "D-07\|D-13" .planning/REQUIREMENTS.md && grep -q "76/76\|75/76\|Coverage" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <done>
    - `grep "POL-01 | Phase 2" .planning/REQUIREMENTS.md` returns a match
    - `grep "POL-01 | Phase 7" .planning/REQUIREMENTS.md` returns NOTHING
    - AUTH-04 row references both Phase 2 and Phase 4 (as `Phase 2 (hook) / Phase 4 (trigger)` or equivalent)
    - Footnote paragraphs reference D-07 and D-13
    - Coverage line preserved (still says 76/76 v1 requirements mapped — the move doesn't change coverage count)
    - All other requirement→phase mappings unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AUTH-08 ops checklist (Google Cloud Console + Supabase Auth Dashboard)</name>
  <files>.planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/02-authentication-landing/02-RESEARCH.md §"OAuth Redirect URI Registration (AUTH-08)" lines 595-631 — verified Google + Supabase registration flow
    - .planning/phases/02-authentication-landing/02-RESEARCH.md "Assumption A6" — Supabase project ref `vhlbdcsxccaknccawfdj`
    - .planning/STATE.md — confirms project ref "vhlbdcsxccaknccawfdj (dealdrop-dev, Tokyo)"
    - .planning/phases/02-authentication-landing/02-RESEARCH.md "Open Question 2" — Google "Testing" vs "Production" mode tradeoff
  </read_first>
  <action>
    Create `.planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md` with this exact content:

    ```markdown
    # AUTH-08 Operator Checklist — OAuth Redirect URI Registration

    **Audience:** The user / developer (not Claude). These steps require access to two external dashboards that no agent can reach.
    **When to run:** Before attempting Plan 04 Task 3's manual OAuth smoke test. Can run in parallel with Plans 01-04 code work.
    **Time estimate:** ~15 minutes, once.

    **Supabase project ref:** `vhlbdcsxccaknccawfdj` (dealdrop-dev, Tokyo region) — verify in the Supabase Dashboard before proceeding.

    ---

    ## Key concept (read this first — prevents the most common confusion)

    There are **two callback URIs** involved, and they go in **different places**:

    1. **Supabase's callback URI** (hosted by Supabase): `https://vhlbdcsxccaknccawfdj.supabase.co/auth/v1/callback`
       → Register this in **Google Cloud Console**.

    2. **DealDrop's callback URI** (hosted by our app): `http://localhost:3000/auth/callback` (+ Vercel variants)
       → Register this in **Supabase Auth Dashboard → URL Configuration → Redirect URLs**.

    The OAuth round-trip goes: `browser → Google → Supabase's callback → DealDrop's /auth/callback → /`.

    ---

    ## Part 1 — Google Cloud Console

    1. Open https://console.cloud.google.com/ → select the DealDrop project (or create one if first time).
    2. Navigate: **APIs & Services → Credentials**.
    3. If no OAuth 2.0 Client ID exists yet:
       - Click **Create Credentials → OAuth client ID**.
       - Application type: **Web application**.
       - Name: `DealDrop Web` (or similar).
    4. On the OAuth Client ID page, under **Authorized redirect URIs**, add exactly:
       ```
       https://vhlbdcsxccaknccawfdj.supabase.co/auth/v1/callback
       ```
       (Replace `vhlbdcsxccaknccawfdj` with your actual Supabase project ref if different.)
    5. Under **Authorized JavaScript origins**, add:
       ```
       https://vhlbdcsxccaknccawfdj.supabase.co
       ```
    6. Save. Copy the **Client ID** and **Client Secret** — you'll paste them in Part 2.

    **Important:** Google does NOT allow wildcard redirect URIs (`https://*.vercel.app/...`) in production OAuth clients. For Vercel preview deployments:
    - **Option A (portfolio bar, recommended):** Add ONE production Vercel URL after first deploy (e.g. `https://vhlbdcsxccaknccawfdj.supabase.co/auth/v1/callback` — same Supabase URL; Supabase handles the final redirect to your Vercel preview via its own wildcard support). Test only on the production Vercel URL and on localhost.
    - **Option B:** Create a separate "Testing" OAuth client in Google that accepts arbitrary redirect URIs. Only use this for dev / preview — NEVER for the real production client.

    ---

    ## Part 2 — Supabase Auth Dashboard

    1. Open https://supabase.com/dashboard/project/vhlbdcsxccaknccawfdj → **Authentication → Providers**.
    2. Find **Google**, toggle **Enabled**.
    3. Paste the **Client ID** and **Client Secret** from Part 1 step 6. Save.
    4. Still in **Authentication**, navigate to **URL Configuration**.
    5. **Site URL:** Set to your production URL, e.g. `https://dealdrop.vercel.app` (once you know it). For localhost-only testing, leave blank or set `http://localhost:3000`.
    6. **Redirect URLs:** Add these entries (one per line, no commas):
       ```
       http://localhost:3000/auth/callback
       http://127.0.0.1:3000/auth/callback
       https://dealdrop.vercel.app/auth/callback
       https://*.vercel.app/auth/callback
       ```
       (Unlike Google, Supabase DOES support the `*.vercel.app` wildcard — use it for preview deployments.)
       Replace `dealdrop.vercel.app` with your actual production domain once you have one.
    7. Save.

    ---

    ## Verification (before running Plan 04 Task 3 smoke test)

    - [ ] In Google Cloud Console → Credentials → your OAuth client → Authorized redirect URIs shows `https://vhlbdcsxccaknccawfdj.supabase.co/auth/v1/callback`
    - [ ] In Supabase Dashboard → Authentication → Providers → Google is **Enabled** with Client ID + Secret populated
    - [ ] In Supabase Dashboard → Authentication → URL Configuration → Redirect URLs includes both `http://localhost:3000/auth/callback` AND `http://127.0.0.1:3000/auth/callback`
    - [ ] `dealdrop/supabase/config.toml` `additional_redirect_urls` uses `http://` (not `https://`) for `127.0.0.1:3000` — Plan 02 Task 2b should have already fixed this (Phase 1 REVIEW WR-03)
    - [ ] `.env.local` has valid `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` (Phase 1 Plan 01-03 already seeded these)

    If all five are checked, proceed to Plan 04 Task 3 smoke test.

    ---

    ## Troubleshooting

    | Symptom | Cause | Fix |
    |---------|-------|-----|
    | Google returns `redirect_uri_mismatch` after clicking "Continue with Google" | Google redirect URIs don't include the Supabase project callback | Part 1 step 4 — add `https://{project-ref}.supabase.co/auth/v1/callback` to Google |
    | Redirected back to `/?auth_error=1` with no session | DealDrop redirect URL not in Supabase Auth → URL Configuration | Part 2 step 6 — add `http://localhost:3000/auth/callback` to Supabase |
    | OAuth works on Vercel but fails on localhost | `supabase/config.toml` has `https://127.0.0.1:3000` typo | Plan 02 Task 2b fixes this; re-verify `grep '127.0.0.1' dealdrop/supabase/config.toml` shows `http://` not `https://` |
    | "Invalid OAuth client" in Supabase | Client Secret mismatch between Google and Supabase | Part 2 step 3 — re-copy the Secret from Google (note: the Secret is only fully visible immediately after creation; regenerate if lost) |
    | OAuth "not verified" / "unsafe app" warning | Google OAuth app is in Testing mode | Normal for dev. Click "Advanced" → "Proceed" during testing. For production deployments, submit for Google verification (out of scope for portfolio). |
    ```

    Do not add or remove any other content. The checklist is intentionally terse for operational use.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md && grep -q "vhlbdcsxccaknccawfdj" .planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md && grep -q "supabase.co/auth/v1/callback" .planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md && grep -q "http://localhost:3000/auth/callback" .planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md && grep -q "http://127.0.0.1:3000/auth/callback" .planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md && grep -q "\\*.vercel.app" .planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md && grep -q "Troubleshooting" .planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md
    </automated>
  </verify>
  <done>
    - File exists at the correct path
    - Contains Supabase project ref `vhlbdcsxccaknccawfdj`
    - Contains both Supabase callback URI (`supabase.co/auth/v1/callback`) and DealDrop callback URIs (`localhost:3000/auth/callback` + `127.0.0.1:3000/auth/callback`)
    - Mentions the `*.vercel.app` wildcard for Supabase (not Google)
    - Has a Troubleshooting section with common failure modes
  </done>
</task>

<task type="auto">
  <name>Task 3: Create printable 02-SMOKE-TEST.md (mirrors Plan 04 Task 3 but formatted for direct user use)</name>
  <files>.planning/phases/02-authentication-landing/02-SMOKE-TEST.md</files>
  <read_first>
    - .planning/phases/02-authentication-landing/02-VALIDATION.md §"Manual Smoke Test Checklist (Primary Validation)" lines 85-121 — existing checklist text
    - .planning/phases/02-authentication-landing/02-UI-SPEC.md §"Copywriting Contract" — exact locked strings the smoke test verifies
    - .planning/phases/02-authentication-landing/02-CONTEXT.md D-15 — env-validation chain closure
    - .planning/phases/01-foundation-database/01-VERIFICATION.md §"Human Verification Required" — the 3 Phase 1 deferred items this phase closes
  </read_first>
  <action>
    Create `.planning/phases/02-authentication-landing/02-SMOKE-TEST.md` — a standalone, self-contained smoke test that duplicates (with minor formatting polish) the content in Plan 04 Task 3 and 02-VALIDATION.md. Having a separate file means the user can run the test without re-opening the full plan.

    ```markdown
    # Phase 2 Smoke Test

    > Run this AFTER all Phase 2 plans (01, 02, 03, 04) are committed and AFTER the AUTH-08 ops checklist is complete. If Plan 04 Task 3 was executed as part of `/gsd-execute-phase`, this file is a permanent record of the same test; re-run it here for a fresh localhost verification before `/gsd-verify-work`.

    ## Pre-flight

    - [ ] `.env.local` has all 7 env vars populated (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `FIRECRAWL_API_KEY`, `RESEND_API_KEY`, `CRON_SECRET`, `RESEND_FROM_EMAIL`).
    - [ ] [AUTH-08-OPS-CHECKLIST.md](AUTH-08-OPS-CHECKLIST.md) — all 5 verification checkboxes passed.

    ## Build + type check (closes D-15)

    ```bash
    cd dealdrop
    npm run lint        # expected: exit 0
    npx tsc --noEmit    # expected: exit 0
    npm run build       # expected: exit 0; route tree includes /, /auth/callback, ƒ Proxy (Middleware)
    ```

    - [ ] All three commands exit 0.

    ### D-15 env-validation chain negative case

    ```bash
    cd dealdrop
    cp .env.local .env.local.bak
    grep -v '^CRON_SECRET=' .env.local.bak > .env.local
    npm run build      # EXPECTED: FAILS with "Invalid environment variables: [{ path: ['CRON_SECRET'] ... }]"
    mv .env.local.bak .env.local
    npm run build      # expected: exit 0
    ```

    - [ ] First build fails with Zod error naming `CRON_SECRET`.
    - [ ] Second build (after restore) exits 0.
    - [ ] This closes Phase 1 VERIFICATION human_verification[0] — record this in the Phase 2 VERIFICATION artifact: **"Phase 1 env-validation chain verified at Phase 2 build time via auth code path"**.

    ## Auth happy-path (browser required)

    ```bash
    cd dealdrop && npm run dev
    # Open http://localhost:3000 in a browser
    ```

    - [ ] Hero visible: tagline exactly `Never miss a price drop`
    - [ ] Subtitle matches: `Paste any product URL. We'll check the price daily and email you the moment it drops.`
    - [ ] Three feature cards render with Lucide icons in accent (`text-primary`):
      - [ ] `Multi-site support` (Globe icon)
      - [ ] `Instant email alerts` (BellRing icon)
      - [ ] `Price history` (LineChart icon)
    - [ ] Footer credit visible: `Made with love`
    - [ ] Header: `DealDrop` wordmark left, `Sign in` button (default/accent variant) right
    - [ ] Click `Sign in` — Shadcn Dialog opens with:
      - [ ] Title exactly `Sign in to DealDrop`
      - [ ] Description exactly `Sign in to start tracking prices`
      - [ ] Single full-width `Continue with Google` button (no Google "G" mark, no privacy links)
      - [ ] ESC closes the dialog; ring-outside click closes; returning focus to `Sign in` button
    - [ ] Click `Continue with Google`:
      - [ ] Button disables + shows Loader2 spinner
      - [ ] Browser redirects to `accounts.google.com`
    - [ ] Complete Google sign-in:
      - [ ] Browser redirects through `{project-ref}.supabase.co/auth/v1/callback`
      - [ ] Lands at `http://localhost:3000/` (or `http://127.0.0.1:3000/`)
      - [ ] DashboardShell visible: heading `Welcome back` + placeholder copy
      - [ ] Header now shows `Sign out` (outline variant)
      - [ ] Hero is gone
    - [ ] Reload page → DashboardShell still visible (proves AUTH-07 session refresh)
    - [ ] DevTools → Application → Cookies → `sb-*` cookies present

    ## Sign-out flow

    - [ ] Click `Sign out`:
      - [ ] Button disables, label swaps to `Signing out…`
      - [ ] Page redirects to `/`, Hero visible again
      - [ ] Sonner toast appears top-center with text exactly `Signed out`
      - [ ] URL cleans back to `/` (no `?signed_out=1` remaining)
    - [ ] DevTools → Application → Cookies → `sb-*` cookies cleared

    ## Error-path

    - [ ] Directly visit `http://localhost:3000/?auth_error=1`:
      - [ ] Sonner error toast `Sign in failed. Please try again.`
      - [ ] URL cleans to `/`

    ## Responsive (HERO-05)

    - [ ] Resize viewport to 320–639px:
      - [ ] Feature cards stack to 1 column
      - [ ] Tagline clamps from 48px to 32px (`text-3xl`)
      - [ ] Header compresses but remains usable; no horizontal scroll
    - [ ] Resize to ≥640px: grid returns to 3 columns.

    ## Closes Phase 1 deferred visual checks

    - [ ] OS dark-mode toggle → Dialog, FeatureCards, Buttons render correct zinc tokens in both light + dark.
    - [ ] Tab-navigate through `Sign in` button — focus ring visible (Shadcn `focus-visible:ring-3`).
    - [ ] These two check closes 01-VERIFICATION human_verification[2] (Shadcn Button visual) and partial of [0] (env-validation chain — already closed in D-15 negative-case above).

    ## AUTH-04 infra contract (grep-level)

    ```bash
    grep -r "openAuthModal\|useAuthModal" dealdrop/src/components
    ```

    - [ ] At least 2 matches (AuthModalProvider definition + SignInButton consumer). Phase 4 will add a 3rd when it wires the Add Product form.

    ## Sign-off

    If every checkbox above is ticked, Phase 2 is done. Proceed to `/gsd-verify-work`.

    If any checkbox fails, record which one + symptoms, then either:
    - Run `/gsd-debug <phase>` to investigate, or
    - Run `/gsd-plan-phase --gaps` after verification produces a VERIFICATION.md
    ```
  </action>
  <verify>
    <automated>
      test -f .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Never miss a price drop" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Sign in to DealDrop" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Sign in to start tracking prices" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Continue with Google" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Multi-site support" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Instant email alerts" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Price history" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Made with love" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Signed out" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "Sign in failed. Please try again." .planning/phases/02-authentication-landing/02-SMOKE-TEST.md && grep -q "D-15\|env-validation chain" .planning/phases/02-authentication-landing/02-SMOKE-TEST.md
    </automated>
  </verify>
  <done>
    - File exists at `.planning/phases/02-authentication-landing/02-SMOKE-TEST.md`
    - Contains all locked copy strings verbatim
    - References D-15 closure and AUTH-08 ops checklist
    - Includes D-15 negative-case env test (remove CRON_SECRET → build fails → restore → build passes)
    - References Phase 1 deferred items (01-VERIFICATION human_verification[0] and [2])
    - AUTH-04 grep check present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces no code artifacts and introduces no new runtime trust surfaces. The only files modified are `.planning/` markdown documents.

However, the AUTH-08 ops checklist INSTRUCTS the user on operations that DO cross trust boundaries:

| Boundary | Description |
|----------|-------------|
| User local environment → Google Cloud Console | User enters OAuth Client Secret into Supabase (out of repo) |
| User local environment → Supabase Auth Dashboard | User configures redirect URLs (out of repo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Information disclosure | OAuth Client Secret in unprotected channel | accept | The user copies the Secret from Google Console to Supabase via their own browser session — the Secret never enters the repo, logs, or any Claude-managed artifact. The checklist explicitly does NOT ask the user to paste the secret into any file in the repo. |
| T-02-22 | Elevation of privilege | Checklist instructing wrong URIs | mitigate | URIs are derived from 02-RESEARCH.md §"OAuth Redirect URI Registration" which was verified against live Supabase docs. Supabase project ref `vhlbdcsxccaknccawfdj` is sourced from STATE.md (recorded by Phase 1 Plan 01-03). |
| T-02-23 | Tampering | User skips or misreads a checklist step | accept | Plan 04 Task 3 includes a pre-flight check that AUTH-08 was completed; if skipped, OAuth will fail with `redirect_uri_mismatch` which the troubleshooting table diagnoses. Portfolio bar. |

**No auth-touching code surfaces in this plan.** The checklist contains instructions; the code paths that consume those URIs live in Plans 02 (callback route) and 03 (AuthModal `redirectTo`).
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` Traceability table shows POL-01 at Phase 2 (not Phase 7) and AUTH-04 split across Phase 2 + Phase 4.
- `.planning/phases/02-authentication-landing/AUTH-08-OPS-CHECKLIST.md` exists and contains the Supabase project ref.
- `.planning/phases/02-authentication-landing/02-SMOKE-TEST.md` exists and mirrors every locked copy string from UI-SPEC Copywriting Contract.
- All three files are tracked by git (use `git status` after edits — no .gitignore traps for `.planning/`).
- No changes to `dealdrop/` in this plan.
</verification>

<success_criteria>
The user can (a) configure OAuth externally without re-reading 02-RESEARCH.md's dense prose, (b) run the smoke test from a single document, and (c) consume a REQUIREMENTS.md that accurately reflects D-07 + D-13 so Phase 7 planning later knows Sonner is already shipped.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-landing/02-05-SUMMARY.md` recording:
- Diff summary of REQUIREMENTS.md (what rows changed, what footnotes added)
- Path + line count of AUTH-08-OPS-CHECKLIST.md
- Path + line count of 02-SMOKE-TEST.md
- Confirmation that Phase 7 scope downstream will need to exclude POL-01 (pointer for Phase 7 planner)
</output>
