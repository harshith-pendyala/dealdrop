---
phase: 02-authentication-landing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dealdrop/package.json
  - dealdrop/package-lock.json
  - dealdrop/components/ui/dialog.tsx
  - dealdrop/components/ui/card.tsx
  - dealdrop/components/ui/sonner.tsx
autonomous: true
requirements:
  - AUTH-05
  - POL-01
  - HERO-02

must_haves:
  truths:
    - "A Shadcn Dialog primitive is available for the AuthModal (consumed by Plan 03)."
    - "A Shadcn Card primitive is available for FeatureCard (consumed by Plan 04)."
    - "A Shadcn Sonner Toaster wrapper is available for mount (consumed by Plan 04)."
    - "The `sonner` npm package is installed so `toast()` calls compile (consumed by Plan 03 AuthToastListener and SignOutButton)."
  artifacts:
    - path: "dealdrop/components/ui/dialog.tsx"
      provides: "Shadcn Dialog primitive (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription)"
      contains: "DialogContent"
    - path: "dealdrop/components/ui/card.tsx"
      provides: "Shadcn Card primitive"
      contains: "Card"
    - path: "dealdrop/components/ui/sonner.tsx"
      provides: "Shadcn Sonner wrapper exporting Toaster"
      contains: "Toaster"
    - path: "dealdrop/package.json"
      provides: "sonner listed in dependencies"
      contains: "sonner"
  key_links:
    - from: "dealdrop/components/ui/dialog.tsx"
      to: "@radix-ui/react-dialog"
      via: "Shadcn wrapper re-exports Radix primitives"
      pattern: "@radix-ui/react-dialog"
    - from: "dealdrop/components/ui/sonner.tsx"
      to: "sonner package"
      via: "Toaster wrapper imports from 'sonner'"
      pattern: "from [\"']sonner[\"']"
---

<objective>
Install the three Shadcn primitives Phase 2 consumes (Dialog, Card, Sonner) and the `sonner` npm package — without modifying any app code. This plan is intentionally pure tooling so Plans 03 and 04 have their primitives ready on disk with zero risk of generation-time surprises blocking downstream work.

Purpose: Isolating `npx shadcn@latest add` into Wave 1 (parallel with proxy/callback work) prevents any CLI deviations from leaking into feature code. If the Shadcn CLI auto-fixes something (as it did in Phase 1 Plan 01-05 with the `_shadcn-test` folder rename, the `cn` helper path move, and the globals.css rewrite), the surprise is caught here instead of during AuthModal implementation.
Output: Three new files in `dealdrop/components/ui/` (dialog.tsx, card.tsx, sonner.tsx) + `sonner` in package.json dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-landing/02-CONTEXT.md
@.planning/phases/02-authentication-landing/02-RESEARCH.md
@.planning/phases/02-authentication-landing/02-PATTERNS.md
@.planning/phases/02-authentication-landing/02-UI-SPEC.md
@.planning/phases/01-foundation-database/01-05-SUMMARY.md
@dealdrop/components/ui/button.tsx
@dealdrop/components.json
@dealdrop/package.json

<interfaces>
<!-- Existing Shadcn primitive shape — the three new primitives must match this style. -->
<!-- From dealdrop/components/ui/button.tsx (Phase 1 Plan 01-05): -->

```typescript
// Named export, cva variants, imports cn from '@/lib/utils'
import { cn } from "@/lib/utils"
// ...
function Button({ className, variant, size, asChild, ...props }) { ... }
export { Button, buttonVariants }
```

Post-install import paths downstream plans will consume:
- `import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription } from '@/components/ui/dialog'`
- `import { Card } from '@/components/ui/card'`
- `import { Toaster } from '@/components/ui/sonner'`
- `import { toast } from 'sonner'` (raw sonner package — the Shadcn wrapper only exports Toaster)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sonner npm package + Shadcn Dialog/Card/Sonner primitives</name>
  <files>dealdrop/package.json, dealdrop/package-lock.json, dealdrop/components/ui/dialog.tsx, dealdrop/components/ui/card.tsx, dealdrop/components/ui/sonner.tsx</files>
  <read_first>
    - dealdrop/package.json — confirm current deps (already has @radix-ui/react-dialog 1.1.15 per RESEARCH.md; no other new Radix installs expected)
    - dealdrop/components.json — locked preset (new-york/zinc/0.5rem/cssVariables/lucide); DO NOT modify
    - dealdrop/components/ui/button.tsx — existing Shadcn primitive; new primitives must share the same file style (named exports, cn helper, cva variants where applicable)
    - .planning/phases/01-foundation-database/01-05-SUMMARY.md — Phase 1 Shadcn init deviations (Rule 1 auto-fix for `_shadcn-test` folder; globals.css rewrite; cn helper moved to src/lib/utils.ts); expect CLI to behave similarly here and DO NOT panic if it reports auto-fixes
    - .planning/phases/02-authentication-landing/02-PATTERNS.md §14 "Shadcn CLI installs" — install commands + no-hand-edit rule
    - .planning/phases/02-authentication-landing/02-RESEARCH.md "To Install in Phase 2" section — verified commands
    - .planning/phases/02-authentication-landing/02-UI-SPEC.md §"Design System" and §"Registry Safety" — primitives to install; shadcn-only, no third-party registries
  </read_first>
  <action>
    Run these commands in sequence from the `dealdrop/` directory. Do NOT edit the generated files — the Shadcn CLI owns them.

    ```bash
    cd dealdrop
    npm install sonner
    npx shadcn@latest add dialog --yes
    npx shadcn@latest add card --yes
    npx shadcn@latest add sonner --yes
    ```

    Notes:
    - `@radix-ui/react-dialog@1.1.15` is already in node_modules per 02-RESEARCH.md — `add dialog` will NOT install new deps, it only generates `components/ui/dialog.tsx`.
    - The `--yes` flag accepts all prompts non-interactively; the Phase 1 Plan 01-05 pattern was `--defaults --force -b radix` because `shadcn` 4.3 dropped interactive prompts. If `--yes` doesn't exist on this CLI version, fall back to `--force` (the CLI prints the supported flags if you pass an invalid one).
    - If any of the three `npx shadcn@latest add <primitive>` commands report "Rule 1 auto-fix" style adjustments (like the `_shadcn-test` → `shadcn-test` rename in Phase 1), that is expected — the CLI self-heals; just capture the output in the summary.
    - DO NOT touch `dealdrop/components.json` or `dealdrop/app/globals.css` — both are locked from Phase 1.
    - DO NOT add anything to `dealdrop/app/layout.tsx` in this task — mounting `<Toaster />` is done in Plan 04 (where the AuthModalProvider wrap happens together).
    - Verify that the new primitives use the same preset as `button.tsx` (new-york zinc; `cn` from `@/lib/utils`; named exports). If any generated file uses `@/src/lib/utils` (wrong prefix) or `lib/utils` (no alias), the Shadcn init may be confused — re-check `components.json` and re-run the failing `add` command.
  </action>
  <verify>
    <automated>
      cd dealdrop && test -f components/ui/dialog.tsx && test -f components/ui/card.tsx && test -f components/ui/sonner.tsx && grep -q '"sonner"' package.json && grep -q '@radix-ui/react-dialog' components/ui/dialog.tsx && grep -q 'from "sonner"' components/ui/sonner.tsx && grep -q 'from "@/lib/utils"' components/ui/card.tsx && npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - `dealdrop/components/ui/dialog.tsx` exists and imports from `@radix-ui/react-dialog`
    - `dealdrop/components/ui/card.tsx` exists and imports `cn` from `@/lib/utils`
    - `dealdrop/components/ui/sonner.tsx` exists and re-exports a `Toaster` component from the `sonner` package
    - `sonner` is listed in `package.json` dependencies
    - `npx tsc --noEmit` from `dealdrop/` exits 0 (types compile)
    - No edits were made to `components.json`, `globals.css`, `layout.tsx`, or `button.tsx`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted package contents (sonner) installed |
| Shadcn registry → local components/ui/ | Templated TSX pulled from Shadcn registry; generated code becomes part of our codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | sonner npm package | accept | Portfolio bar; sonner is a widely-used library with >500k weekly downloads. `package-lock.json` locks the resolved version + integrity hash. No supply-chain scanning beyond Vercel's standard audit. |
| T-02-02 | Tampering | Shadcn-generated primitives | mitigate | Only use the shadcn official registry (per UI-SPEC Registry Safety section). Never hand-edit generated files (per UI-SPEC) — re-run the CLI for changes, preventing drift that could hide injected code. |
| T-02-03 | Spoofing | Shadcn registry source | accept | shadcn CLI pulls from its own registry by default; no third-party registry configured. |

**No auth-touching runtime surfaces in this plan.** This is pure tooling/installation. All STRIDE surface attached to auth (session fixation, CSRF, open redirect, code interception, service-role exposure) is addressed in Plans 02 and 03 where the actual auth code path lives.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 in `dealdrop/`.
- `dealdrop/components.json` unchanged (locked preset).
- `dealdrop/app/globals.css` unchanged (locked from Phase 1).
- `dealdrop/app/layout.tsx` unchanged (Plan 04 handles layout wiring).
- Three new primitives match the style of `dealdrop/components/ui/button.tsx` (named exports, `cn` from `@/lib/utils`).
- `sonner` appears exactly once in `package.json` dependencies (not devDependencies).
</verification>

<success_criteria>
All four artifacts (dialog.tsx, card.tsx, sonner.tsx, sonner-in-package.json) exist on disk. Phase 1 deferred Shadcn Button visual check is NOT addressed by this plan — it naturally closes in Plan 04 when Hero, FeatureCard, Header, and buttons first render on `/`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-landing/02-01-SUMMARY.md` recording:
- Exact Shadcn CLI version used and any auto-fixes it reported
- Exact sonner version installed (for the Assumptions Log A1 — "sonner latest version is 2.x")
- Confirmation that `components.json` + `globals.css` + `layout.tsx` were not touched
- `tsc --noEmit` exit status
</output>
