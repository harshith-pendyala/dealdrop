---
phase: 09-resend-env-config
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - dealdrop/.env.example
  - dealdrop/README.md
autonomous: true
requirements: [EMAIL-01, EMAIL-05]
nyquist_compliant: true
tags: [docs, env, readme, portfolio]

must_haves:
  truths:
    - ".env.example lists RESEND_TEST_RECIPIENT= (empty value, optional) with a one-line comment explaining when to set it"
    - "README.md is no longer create-next-app scaffold copy — has a real DealDrop intro paragraph"
    - "README.md contains a section listing every required and optional env var with one-line descriptions"
    - "README.md contains an Email recipient modes section explaining the one-env-var flip from test-recipient mode to production mode (EMAIL-05)"
    - "README.md preserves the npm run dev / build / lint commands (the only useful piece of the original scaffold)"
    - "All five existing env vars in .env.example (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, FIRECRAWL_API_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL, CRON_SECRET) remain present and unchanged in order/spelling"
  artifacts:
    - path: "dealdrop/.env.example"
      provides: "RESEND_TEST_RECIPIENT documented as optional with usage comment"
      contains: "RESEND_TEST_RECIPIENT="
      contains_2: "test mode"
    - path: "dealdrop/README.md"
      provides: "Real DealDrop README replacing scaffold copy with intro, env config, and email recipient modes section"
      contains: "DealDrop"
      contains_2: "Email recipient modes"
  key_links:
    - from: ".env.example RESEND_TEST_RECIPIENT="
      to: "env.server.ts schema (Plan 09-01)"
      via: "operator copy-paste from .env.example to .env.local"
      pattern: "RESEND_TEST_RECIPIENT"
    - from: "README.md Email recipient modes section"
      to: "operator unset action at v1.2 cutover"
      via: "explicit one-env-var flip instruction"
      pattern: "unset.*RESEND_TEST_RECIPIENT|Leave.*blank|production mode"
---

<objective>
Wave 1 documentation: update `dealdrop/.env.example` to declare `RESEND_TEST_RECIPIENT=` (empty, optional, with one-line comment) and rewrite `dealdrop/README.md` from create-next-app scaffold copy to a real DealDrop README with intro, env-config table, and an "Email recipient modes" section explaining the one-env-var flip from test-recipient mode to production mode (D-07, EMAIL-05). This plan runs in parallel with Plan 09-01 because no file overlap exists between the docs surface (`.env.example`, `README.md`) and the code surface (`src/lib/env.server.ts`).

Purpose: EMAIL-05 requires "README (or equivalent docs) explains how to flip from test-recipient mode to production mode — a one-env-var change after a domain is verified". D-07 locks "both .env.example AND README.md get updated. .env.example is the discovery surface (a developer cloning the repo sees all required + optional env vars in one place); README.md is currently scaffold copy and gets a real DealDrop intro plus a short Email modes section explaining the one-env-var flip." Side benefit: the README rewrite covers EMAIL-01 documentation (the from-address env-var is documented, even though the source-code use of `env.RESEND_FROM_EMAIL` is preserved by Plans 01-02).

Output: `.env.example` with one new commented entry under the existing `# Phase 6 — email + cron` block (Option A from 09-PATTERNS.md, chosen for minimal diff). `README.md` fully replaces scaffold with a portfolio-bar DealDrop README (~50-80 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-resend-env-config/09-CONTEXT.md
@.planning/phases/09-resend-env-config/09-PATTERNS.md
@.planning/phases/06-automated-monitoring-email-alerts/06-CONTEXT.md
@dealdrop/CLAUDE.md
@dealdrop/AGENTS.md

<interfaces>
Current `.env.example` (entire file — 14 lines, all preserved):
```
# Supabase — public, browser-exposed
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-only (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=

# Phase 3 — scraping
FIRECRAWL_API_KEY=

# Phase 6 — email + cron
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CRON_SECRET=
```

Target `.env.example` (Option A from 09-PATTERNS.md — append under existing Phase 6 block):
```
# Supabase — public, browser-exposed
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-only (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=

# Phase 3 — scraping
FIRECRAWL_API_KEY=

# Phase 6 — email + cron
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CRON_SECRET=
# When set, all price-drop alerts route to this address (test mode).
# Leave blank for production user-of-record routing.
RESEND_TEST_RECIPIENT=
```

Current `README.md` (entire file — 37 lines of create-next-app scaffold) gets fully replaced. The only piece worth preserving is the `npm run dev` / `build` / `lint` commands list.

Required README sections (per 09-PATTERNS.md §"dealdrop/README.md"):
1. Title + one-paragraph DealDrop intro (link to .planning/PROJECT.md for full context).
2. Getting Started / Development (preserve `npm run dev` / `npm run build` / `npm run lint`).
3. Environment configuration — list every env var with one-line descriptions, IN THE EXACT ORDER MATCHING `.env.example`:
   - NEXT_PUBLIC_SUPABASE_URL — Supabase project URL (browser-safe)
   - NEXT_PUBLIC_SUPABASE_ANON_KEY — Supabase anon key (browser-safe)
   - SUPABASE_SERVICE_ROLE_KEY — Supabase service-role key (server-only — never expose)
   - FIRECRAWL_API_KEY — Firecrawl API key for product scraping
   - RESEND_API_KEY — Resend API key for transactional email
   - RESEND_FROM_EMAIL — verified sender address (must pass Zod .email())
   - CRON_SECRET — bearer token for /api/cron/check-prices (≥ 32 chars)
   - RESEND_TEST_RECIPIENT (optional) — overrides the user-of-record recipient
4. Email recipient modes — three lines:
   - RESEND_TEST_RECIPIENT set → all alerts go to that single test address (demo / pre-domain-verification mode)
   - RESEND_TEST_RECIPIENT unset → alerts go to each product's tracking-user-of-record (production mode)
   - For v1.2, after Resend domain verification, unset RESEND_TEST_RECIPIENT in Vercel — zero code change required

Tone constraints:
- Portfolio-bar — informative, not exhaustive.
- No emoji unless explicitly requested (per project CLAUDE.md user-rules).
- Short. Reference PROJECT.md instead of duplicating.
- No exhaustive deployment guide; no Vercel walkthrough.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append RESEND_TEST_RECIPIENT to .env.example with usage comment (D-07, Claude's Discretion §".env.example comment style")</name>
  <files>dealdrop/.env.example</files>
  <read_first>
    - dealdrop/.env.example (read entire file — current state has 14 lines with five env-var groups separated by `# Phase N — concern` headers; new entry goes under the existing `# Phase 6 — email + cron` block per Option A in 09-PATTERNS.md)
    - .planning/phases/09-resend-env-config/09-CONTEXT.md (read §"Implementation Decisions" D-07 + Claude's Discretion §".env.example comment style")
    - .planning/phases/09-resend-env-config/09-PATTERNS.md (read §"dealdrop/.env.example" — Option A vs Option B; we choose Option A for minimal diff)
  </read_first>
  <behavior>
    - File ends with a new line `RESEND_TEST_RECIPIENT=` (no value after the `=`).
    - Two comment lines precede the new entry, explaining when to set it and the production-mode default.
    - The new entry sits under the existing `# Phase 6 — email + cron` block (Option A from 09-PATTERNS.md), not in a new dedicated `# Phase 9 — ...` block.
    - All five existing env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, FIRECRAWL_API_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL, CRON_SECRET) remain in their original order and spelling.
    - Existing `# Phase 3 — scraping` and `# Phase 6 — email + cron` headers are unchanged.
  </behavior>
  <action>
Append three lines (two comment lines + one bare key=) to the END of `dealdrop/.env.example`. The current file ends at line 14 with `CRON_SECRET=` (no trailing newline guaranteed; ensure proper newline at EOF).

After the edit, the COMPLETE file contents must be exactly:

```
# Supabase — public, browser-exposed
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-only (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=

# Phase 3 — scraping
FIRECRAWL_API_KEY=

# Phase 6 — email + cron
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CRON_SECRET=
# When set, all price-drop alerts route to this address (test mode).
# Leave blank for production user-of-record routing.
RESEND_TEST_RECIPIENT=
```

Hard constraints (per 09-PATTERNS.md "Hard constraints" for .env.example):
- The key MUST be exactly `RESEND_TEST_RECIPIENT` (D-05 spelling).
- The value MUST be empty (`RESEND_TEST_RECIPIENT=` with nothing after the `=`) — this is the "leave blank for production" UX (per `emptyStringAsUndefined: true` in env.server.ts line 29).
- Do NOT delete or reorder the existing five env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, FIRECRAWL_API_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL, CRON_SECRET).
- Do NOT add a value to any existing env var that previously had no value (`KEY=` stays `KEY=`, not `KEY=somevalue`).
- Do NOT modify the existing `# Phase 3` or `# Phase 6` headers.
- Do NOT create a new `# Phase 9 — email recipient override` header (we chose Option A for minimal diff; Option B is also acceptable per Claude's Discretion but Option A is the chosen path).
- Comment style: `# ...` (single-hash) prefix, matching the existing convention. Two comment lines, each ending with a period.
- Ensure file ends with a single trailing newline (no double-blank).
  </action>
  <verify>
    <automated>cd dealdrop && grep -F "RESEND_TEST_RECIPIENT=" .env.example && grep -F "# When set, all price-drop alerts route to this address (test mode)." .env.example && grep -F "# Leave blank for production user-of-record routing." .env.example && grep -c "^[A-Z_]\+=" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `dealdrop/.env.example` contains the literal substring `RESEND_TEST_RECIPIENT=` on its own line.
    - `dealdrop/.env.example` contains the literal substring `# When set, all price-drop alerts route to this address (test mode).`.
    - `dealdrop/.env.example` contains the literal substring `# Leave blank for production user-of-record routing.`.
    - `dealdrop/.env.example` still contains all seven original env-var keys: `NEXT_PUBLIC_SUPABASE_URL=`, `NEXT_PUBLIC_SUPABASE_ANON_KEY=`, `SUPABASE_SERVICE_ROLE_KEY=`, `FIRECRAWL_API_KEY=`, `RESEND_API_KEY=`, `RESEND_FROM_EMAIL=`, `CRON_SECRET=` (verified by `grep -c "^[A-Z_]\+=" dealdrop/.env.example` returning `8` — seven original + one new).
    - `dealdrop/.env.example` still contains the literal headers `# Phase 3 — scraping` and `# Phase 6 — email + cron`.
    - `dealdrop/.env.example` does NOT contain any value after `=` for any env var (verified by `! grep -E "^[A-Z_]+=.+" dealdrop/.env.example` — every env line has empty value).
    - `dealdrop/.env.example` line count is between 16 and 18 (original 14 + 3 added comment+key lines, with possible trailing newline).
  </acceptance_criteria>
  <done>
    `.env.example` documents the new optional `RESEND_TEST_RECIPIENT` env var with usage comments. Discovery surface complete: a developer cloning the repo sees the optional override alongside the required Phase 6 vars.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace README.md scaffold with real DealDrop README + Email recipient modes section (D-07, EMAIL-05, EMAIL-01)</name>
  <files>dealdrop/README.md</files>
  <read_first>
    - dealdrop/README.md (read entire file — current state is 37 lines of create-next-app scaffold copy; gets fully replaced)
    - dealdrop/.env.example (re-read after Task 1 — confirm the env-var list and spelling that the README must mirror)
    - .planning/PROJECT.md (read entire file — tone reference for what DealDrop is; the README intro should describe DealDrop in similar tone)
    - .planning/phases/09-resend-env-config/09-CONTEXT.md (read §"Implementation Decisions" D-07 + Claude's Discretion §"README copy length and tone"; §"Specifics" — README is a side-quest of Phase 9, do not over-scope)
    - .planning/phases/09-resend-env-config/09-PATTERNS.md (read §"dealdrop/README.md" — required sections + tone constraints)
    - dealdrop/CLAUDE.md (read fully — project conventions; no emoji rule)
    - dealdrop/AGENTS.md (read fully — repo conventions)
  </read_first>
  <behavior>
    - File contains a Title + one-paragraph DealDrop intro at the top, optionally linking to `../.planning/PROJECT.md`.
    - File contains a "Getting Started" or "Development" section with `npm run dev`, `npm run build`, `npm run lint`.
    - File contains an "Environment configuration" section listing all 8 env vars (7 required + 1 optional) IN THE SAME ORDER as `.env.example`, with one-line descriptions.
    - File contains an "Email recipient modes" section with three explicit lines covering: (a) override set → demo/test mode, (b) override unset → user-of-record/production mode, (c) v1.2 unset action.
    - No emoji anywhere in the file (per project CLAUDE.md user-rules).
    - No mention of Yarn / pnpm / Bun (the original scaffold listed them; trim to npm only since the lockfile is package-lock.json per dealdrop/CLAUDE.md).
    - File length is between 40 and 90 lines (portfolio-bar — short, informative, not exhaustive).
  </behavior>
  <action>
Replace the ENTIRE contents of `dealdrop/README.md` with the following exact markdown. This is a full rewrite — the existing 37-line scaffold is discarded.

```markdown
# DealDrop

DealDrop is a universal e-commerce price tracker. Paste any product URL, and DealDrop scrapes the product details, monitors the price daily, and sends an email alert the moment the price drops. Each user gets a private dashboard with price-history charts for every product they track.

For full project context (goal, constraints, decisions, deferred ideas), see [`.planning/PROJECT.md`](../.planning/PROJECT.md).

## Tech stack

- **Framework:** Next.js 16 (App Router) + React 19 + TypeScript strict
- **Styling:** Tailwind CSS v4 + Shadcn UI + Lucide icons
- **Backend:** Supabase (Postgres + Auth + RLS + pg_cron)
- **Scraping:** Firecrawl (structured JSON, no per-site scrapers)
- **Email:** Resend (transactional)
- **Charts:** Recharts
- **Hosting:** Vercel

## Development

```bash
npm install
npm run dev      # start dev server at http://localhost:3000
npm run build    # production build
npm run lint     # ESLint check
```

Vitest unit tests:

```bash
npx vitest run                    # full suite
npx vitest run src/lib/resend     # one module
```

## Environment configuration

Copy `.env.example` to `.env.local` and fill in the values. All server-only vars are validated by Zod at boot — missing or malformed required values fail fast (see `src/lib/env.server.ts`).

| Variable | Required | Description |
|----------|----------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | yes | Supabase project URL (browser-safe) |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | yes | Supabase anon key (browser-safe) |
| `SUPABASE_SERVICE_ROLE_KEY` | yes | Supabase service-role key (server-only — never expose to the browser) |
| `FIRECRAWL_API_KEY` | yes | Firecrawl API key for product scraping |
| `RESEND_API_KEY` | yes | Resend API key for transactional email |
| `RESEND_FROM_EMAIL` | yes | Verified sender address (bare RFC-5321 format; must pass Zod `.email()`) |
| `CRON_SECRET` | yes | Bearer token for `POST /api/cron/check-prices` (≥ 32 chars) |
| `RESEND_TEST_RECIPIENT` | no | Optional override — when set, every price-drop alert routes to this address |

## Email recipient modes

DealDrop supports two email-routing modes, controlled by a single optional env var.

- **Test-recipient mode** — set `RESEND_TEST_RECIPIENT=demo@example.com`. Every price-drop alert (regardless of which user added the product) routes to that single address. Useful before a custom domain is verified, or for portfolio demos where the alert inbox should be a single address you control.
- **Production mode** — leave `RESEND_TEST_RECIPIENT` unset (or blank). Each price-drop alert delivers to the email of the user who added the tracked product (resolved server-side via Supabase admin auth).

To flip from test-recipient mode to production mode (planned for v1.2 after a custom domain is verified in Resend): unset `RESEND_TEST_RECIPIENT` in Vercel env (or remove the line from `.env.local`) and redeploy. No code change is required at the cutover.

A one-time `console.warn` fires at module load when the override is active so you can confirm in Vercel function logs / dev terminal that test-recipient mode is on.

## Project planning

This repo follows the [GSD](https://github.com/) (Get Shit Done) workflow. Roadmap, phase plans, and implementation summaries live under `.planning/`. Start with [`.planning/PROJECT.md`](../.planning/PROJECT.md) and [`.planning/ROADMAP.md`](../.planning/ROADMAP.md).
```

Hard constraints (per 09-PATTERNS.md "Hard constraints" for README.md):
- The README is a side-quest of Phase 9 — do NOT expand its scope to a full deployment manual or Vercel walkthrough.
- Do NOT replace `.planning/PROJECT.md` or `.planning/REQUIREMENTS.md` content — link to them only.
- Do NOT touch any other Markdown file in the repo.
- No emoji (per dealdrop/CLAUDE.md user-rules — "Only use emojis if the user explicitly requests it").
- The env-var list MUST be in the same order as `.env.example` (post-Task 1): NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, FIRECRAWL_API_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL, CRON_SECRET, RESEND_TEST_RECIPIENT.
- The "Email recipient modes" section MUST cover all three D-07 cases: override set, override unset, and v1.2 cutover instruction.
- Tone: portfolio-bar — informative, not exhaustive. No fluff.
- File length: 40-90 lines (target ~70).
  </action>
  <verify>
    <automated>cd dealdrop && grep -F "# DealDrop" README.md && grep -F "Email recipient modes" README.md && grep -F "RESEND_TEST_RECIPIENT" README.md && grep -F "RESEND_FROM_EMAIL" README.md && grep -F "npm run dev" README.md && grep -F "npm run build" README.md && grep -F "npm run lint" README.md && (! grep -E "create-next-app|bootstrapped" README.md) && wc -l README.md</automated>
  </verify>
  <acceptance_criteria>
    - `dealdrop/README.md` first line is exactly `# DealDrop` (the title heading).
    - `dealdrop/README.md` contains the literal substring `Email recipient modes` (the EMAIL-05 documentation section).
    - `dealdrop/README.md` contains the literal substring `RESEND_TEST_RECIPIENT` at least 4 times (env-table row + Email recipient modes section explanation + cutover instruction + Test-recipient mode bullet).
    - `dealdrop/README.md` contains the literal substring `RESEND_FROM_EMAIL` (EMAIL-01 documentation — from-address is documented even though source already env-sources it).
    - `dealdrop/README.md` contains all 8 env-var names: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `FIRECRAWL_API_KEY`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `CRON_SECRET`, `RESEND_TEST_RECIPIENT`.
    - `dealdrop/README.md` contains the literal substrings `npm run dev`, `npm run build`, `npm run lint`.
    - `dealdrop/README.md` does NOT contain `create-next-app` or `bootstrapped` (scaffold-copy markers): `! grep -E "create-next-app|bootstrapped" dealdrop/README.md`.
    - `dealdrop/README.md` does NOT contain `yarn dev`, `pnpm dev`, or `bun dev` (the scaffold listed all four; only npm is correct for this repo per dealdrop/CLAUDE.md): `! grep -E "yarn dev|pnpm dev|bun dev" dealdrop/README.md`.
    - `dealdrop/README.md` line count is between 40 and 90: `awk 'END{exit !(NR>=40 && NR<=90)}' dealdrop/README.md`.
    - `dealdrop/README.md` does NOT contain emoji (per CLAUDE.md no-emoji rule). This is verified by absence of common emoji characters: `! grep -P '[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{27BF}]' dealdrop/README.md` (using ripgrep `-P` for Unicode ranges).
    - The env-var ordering in the README's table matches `.env.example` order — verified manually by reading both files; the human-verification step is implicit in the visible-on-page-load nature of a README.
  </acceptance_criteria>
  <done>
    README.md is a real DealDrop README with intro, dev commands, env-var table (8 entries, ordered to match .env.example), and an Email recipient modes section explicitly documenting the v1.2 one-env-var flip. EMAIL-05 documentation requirement satisfied. EMAIL-01 from-address has documentation surface (in env-var table). No emoji, no scaffold leftover, length 40-90 lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → developer (cloning) | `.env.example` is the discovery surface a fresh developer reads to understand required env vars. Comment accuracy matters. |
| repo → operator (deploying) | README is the documentation surface an operator reads to understand the v1.2 cutover. Wrong instructions could leave the override active in production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-13 | Information Disclosure | secrets in .env.example | mitigate | All env-var values are blank (`KEY=` not `KEY=value`). No actual secrets land in the repo. The example file's purpose is key discovery, not value distribution. |
| T-9-14 | Spoofing | misleading docs | accept | The README's accuracy is the operator's safeguard against accidentally leaving test-recipient mode active in prod. The Email recipient modes section is explicit and visible; checker review verifies it. |
| T-9-15 | Tampering | docs drift from code | mitigate | The README env-var table mirrors `.env.example`, which mirrors `env.server.ts`. All three are co-modified in this milestone. Acceptance criteria require all 8 env-var names appear in the README. |
| T-9-16 | Repudiation | unclear cutover instruction | mitigate | Email recipient modes section explicitly states "unset RESEND_TEST_RECIPIENT in Vercel env... and redeploy. No code change is required at the cutover." Operator has unambiguous v1.2 instruction. |

**Severity:** all threats LOW. Documentation-only plan, no code surface. ASVS L1 not applicable to docs (no auth, no input handling, no DB).
</threat_model>

<verification>
- `.env.example` shape verified: `cd dealdrop && grep -c "^[A-Z_]\+=" .env.example` returns 8 (seven original + new RESEND_TEST_RECIPIENT).
- `.env.example` no-values check: `! grep -E "^[A-Z_]+=.+" dealdrop/.env.example` (every env line is bare `KEY=`).
- README real-content check: `! grep -E "create-next-app|bootstrapped" dealdrop/README.md` (no scaffold leftovers).
- README env-var coverage: `for v in NEXT_PUBLIC_SUPABASE_URL NEXT_PUBLIC_SUPABASE_ANON_KEY SUPABASE_SERVICE_ROLE_KEY FIRECRAWL_API_KEY RESEND_API_KEY RESEND_FROM_EMAIL CRON_SECRET RESEND_TEST_RECIPIENT; do grep -F "$v" dealdrop/README.md > /dev/null || echo "MISSING: $v"; done` returns no output (all 8 present).
- README EMAIL-05 section present: `grep -F "Email recipient modes" dealdrop/README.md`.
- README length sanity: `awk 'END{exit !(NR>=40 && NR<=90)}' dealdrop/README.md` exits 0.
- No emoji: `! grep -P '[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{27BF}]' dealdrop/README.md`.
- No regression of dev tooling: `cd dealdrop && npm run lint` exits 0 (markdown changes do not break linter).
</verification>

<success_criteria>
- `.env.example` documents `RESEND_TEST_RECIPIENT=` (empty, optional) with a usage comment.
- All five existing env-var groups in `.env.example` are unchanged in order and spelling.
- `README.md` is a real portfolio-bar DealDrop README — no scaffold-copy leftovers.
- README contains an "Email recipient modes" section with three explicit lines (override set, override unset, v1.2 cutover).
- README env-var table covers all 8 vars in the same order as `.env.example`.
- README length is between 40 and 90 lines (portfolio-bar tone).
- No emoji, no Yarn/pnpm/Bun mentions, no exhaustive deployment guide.
- EMAIL-05 documentation requirement satisfied; EMAIL-01 from-address has documentation surface.
</success_criteria>

<output>
After completion, create `.planning/phases/09-resend-env-config/09-04-SUMMARY.md` covering:
- The exact change to `.env.example` (Option A chosen; new lines appended).
- A confirmation that the README is fully rewritten (line count before/after, scaffold-copy markers absent).
- Verification that the README env-var table matches `.env.example` order and spelling.
- Note: this plan ran in Wave 1 parallel with Plan 09-01; no file overlap was encountered.
</output>
